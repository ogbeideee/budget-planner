# Budget Planner — Delivery Roadmap

Project: client-side personal budget planner.
Stack: Next.js 16.2.12 (App Router) · React 19.2.4 · TypeScript 5 · Tailwind CSS 4 · ESLint 9 · Zustand 5 (`persist`) · Recharts 3 (route-split, `/reports` only) · Vitest 4 + Testing Library.
Persistence: browser `localStorage` (no backend) — or SQLite in the Electron main process when running as the desktop app (see `ARCHITECTURE.md`).

Every phase produces one Markdown document in `docs/`. Documents are written so a software
engineer can implement them without further clarification.

## Phase Index

| Phase | Name | Deliverable document | Exit criteria |
|-------|------|---------------------|---------------|
| 1 | Requirements & Data Specification | `PROJECT_SPEC.md` | Requirements, data model, and acceptance criteria approved; no open questions about scope |
| 2 | System Architecture | `ARCHITECTURE.md` | Folder structure, state model, and data flow specified; engineer can scaffold all files |
| 3 | UI/UX Design Specification | `UI_UX_SPEC.md` | Design tokens, page specs, component inventory, and a11y requirements specified |
| 4 | Implementation & Testing | `PHASE-04-IMPLEMENTATION.md` | Build order, verification commands, and test matrix defined; `npm run lint` and `npx tsc --noEmit` pass |

## Delivery rules

1. Each phase ends with a review of its document before the next phase begins.
2. Changes to scope after a phase is approved must be recorded in `ROADMAP.md` under
   "Change log" and the affected phase document updated.
3. A phase is complete only when its exit criteria are met (see table above).

## Current state (2026-08-05)

### Status

- All four phases are delivered. The app is feature-complete per `PROJECT_SPEC.md`
  (incl. FR-18 income planning) and is in stabilization: QA passes only, no feature work.
- Verification gates — all green at the latest commit:
  `npx tsc --noEmit` · `npm run lint` · `npm run test` (807 tests / 64 files) ·
  `npm run build` (all 6 routes statically prerendered).
- State schema **version 4** (learned classification rules, 6A): `validateAppState`
  (`lib/validate.ts`) accepts 1–4 and migrates; `CURRENT_STORAGE_VERSION = 4`
  (`lib/storage.ts`).
- Statement classification (3D + 6A + 6B): `lib/statementClassify.ts` — ordered
  `CLASSIFICATION_RULES` (learned 6A rules first, then built-ins: refund → loan →
  tax → bank-fee → interest → savings → internal-transfer → merchant-gateway →
  transfer → income-salary → expense categories; word-boundary matching), fallback
  to the keyword-suggestion system for expenses ONLY when the matched keyword is
  present as a whole word (6B — no "BUSINESS"→Transport, no "FOODSTUFF"→Food);
  generic "Transfer to JOHN DOE" stays a low-confidence transfer; type (kind) and
  category stay separate (transfers/bank-fees have no category).
- Desktop persistence: SQLite (`better-sqlite3`) in the Electron main process at
  `<userData>/budget-planner.sqlite3`; browser-era data auto-migrates on first launch
  with a pre-migration backup (2026-08-05).
- Desktop features: native menu + shortcuts, native file dialogs (import/export),
  desktop notifications, automatic file backups (`<userData>/backups/`, newest 30),
  restore-backup workflow, open backup / reveal data folder (2026-08-05).
- Startup & identity: splash screen, root loading screen, proper brand icon (designed
  mark in `scripts/make-icon.mjs`, multi-size ICO), real version info (About card +
  native dialog), auto-update scaffold (`electron/updater.cjs`, feed-configurable,
  inert by default), NSIS installer branding (2026-08-05).

### Architecture & folder structure

Client-only Next.js App Router app; all logic is `"use client"`; no backend, no API routes,
no server state. Single zustand store (`store/useAppStore.ts`) with `persist` middleware;
derived math lives in pure selectors (`lib/selectors.ts`, `lib/finance.ts`).

```
app/
  layout.tsx                # fonts, <html lang="en">, <AppShell>
  page.tsx                  # /  Planner (primary workspace)
  todo/page.tsx             # /todo    actionable recommendations
  history/page.tsx          # /history chronological ledger (timeline)
  reports/page.tsx          # /reports analytical charts + CSV export
  settings/page.tsx         # /settings categories, currency, theme, recurring rules, backups, import/export
  upcoming/page.tsx         # /upcoming future expenses + funding urgency
  error.tsx                 # fatal error boundary -> RecoveryPanel
  globals.css               # Tailwind entry + @theme design tokens
components/
  shell/    AppShell, Sidebar, BottomNav, Header, PageHeader, nav
  ui/       Button, Card, Input, Select, Modal, Drawer, ConfirmDialog, Disclosure,
            EmptyState, Toast(+Host), MonthPicker, Slider, ProgressBar, SectionHeading,
            PageSkeleton, AnimatedNumber, IconPicker, IconValue, icons
  planner/  PlannerView, SummaryCards, Hero, BudgetStatusBand, reviewBudgets,
            BudgetList, BudgetForm, BudgetRow, PriorityBadge,
            AllocationDrawer, QuickAddExpense, DeferredSection,
            InsightsPanel, ExpenseBreakdown (Reports only), BudgetSuggestions,
            IncomeModal
  history/  HistoryView, TransactionList
  txn/      TransactionForm, TransactionRow, TransactionFilters, RecurrenceForm
  upcoming/ UpcomingView, FutureExpenseForm
  reports/  ReportsView, ChartCard, chartStyles, IncomeExpenseChart, SpendingTrendChart,
            SavingsChart, BudgetUtilizationChart, TopCategoriesChart, ExpectedVsActualChart,
            IncomeSourceChart, IncomeTrendChart, MonthlyOverview, FinancialInsights,
            CategoryAnalysisChart, CashFlowChart, ForecastCard, Recommendations
  todo/     TodoView          insights/ InsightList
  settings/ SettingsView, SettingsNav, ProfilePanel, AppearancePanel,
            BudgetPreferencesPanel, CategoryManager, CategoryModal,
            IncomeSourcesPanel, IncomeSourceModal, DataBackupsPanel, AboutPanel,
            BackupsManager, categoryColors, iconLibrary
  theme/    ThemeToggle       recovery/ RecoveryPanel
  charts/   BarChart           # custom animated CSS chart (Planner)
lib/                          # pure, test-covered (17 test files)
  types.ts date.ts money.ts finance.ts selectors.ts allocation.ts
  insights.ts todo.ts predictions.ts recommendations.ts reportTrends.ts monthStats.ts
  budgetHealth.ts accents.ts timeline.ts upcoming.ts recurrence.ts categorize.ts
  seed.ts validate.ts storage.ts theme.ts scrollLock.ts ids.ts
store/
  useAppStore.ts              # state + actions, persist middleware
  useToastStore.ts            # transient toast queue (not persisted)
hooks/
  useMonth, useToast, useTheme, useAppearance, usePlannerStatus, useRecurring,
  useAnimatedNumber, useChartColors, useReducedMotion
public/                       # favicon.ico only
tests: components/*.test.tsx · lib/__tests__/ · store/__tests__/
```

### Feature inventory

| Area | Current state |
|------|---------------|
| Income system | Standalone month-scoped `IncomePlan` `{id, month, name, icon, expectedAmount, receivedAmount}`; `IncomeModal` edits per source with live Difference status; icons via `IconPicker` (favourites/recents + vector set); `lib/finance.ts` `monthFinance()` is the single source of truth — Received = `max(ledger income, Σ plan.received)`, Net, Remaining, Projected, savings rate. Reports income charts (sources, expected vs actual, trend) use plan data |
| Budget planner | `/` workspace: summary cards, month stats, needs-funding (obligation-driven `fundingGaps` list with Fund button + focus/scroll), allocated budget list (priority, over-budget pills), allocation sliders (sub-unit step fix), budget health score, quick add expense, deferred expenses, insights, expense breakdown, recommendations, monthly income; responsive Budget attention bar (`@container` — 2 messages wide, 1 + View more below 896 px container, truncation instead of overlap) |
| To-Do | `/todo` aggregates actionable items (`lib/todo.ts`, same predicates as insights), capped at 5; summary line ("N items needs attention · 0 completed · N high priority"), All/Needs attention/Completed filter, compact tone-tinted rows with priority chips + teal ghost "Resolve", "You're all caught up." section with View Planner when nothing else is pending |
| Timeline | `/history` chronological ledger: grouped buckets (Today/Yesterday/Last week/Earlier/Upcoming), search, type/category filters, URL-persisted sort (`&sort=`), pagination 25/page, deferred chip, move-to-next-month |
| Upcoming & recurring | `/upcoming` groups by Overdue/Today/Tomorrow/This week/Next week/Later (timeline rail + dots); compact summary row (total, next due + countdown, this month, recurring count); All/This month/Later filter + Soonest/Latest sort; nearest expense subtly tinted; intentional empty state with add CTA; recurring rules generate month instances (`useRecurring`), exceptions tracked; funding urgency (critical/soon/low); future-expense forms with category suggestions |
| Reports | 6-month window ending at the selected month; 9 Recharts charts + insights card; exact tooltips, compact ticks, reduced-motion, empty states, responsive (`ResponsiveContainer width="100%"`); CSV export (decimal amounts + Currency column); Monthly overview with contextual ↑/↓ deltas, tinted Financial insights card + bordered observation rows, Spending breakdown (Income vs expenses + Spending by category), quiet Savings trend line |
| Settings | Theme (light/dark/system, OS-scheme listener), currency (USD/NGN formatting-only), category manager (add/edit/delete with validation, emoji-only icons, delete-reason toasts), recurring rules, backups manager, JSON import/export |
| First-run setup | One-time non-dismissable name modal ("What should we call you?" — trimmed non-empty required, Continue disabled until valid, Enter submits) when no display name is saved; persists via the shared storage seam (`settings:display-name`, no schema change); name personalizes the Planner hero greeting ("Good afternoon, Daniel 👋", nameless fallback) |
| Error handling | Fatal error boundary detects `CorruptedStateError` and renders `RecoveryPanel` (scan browser, restore from backup, import file, or start fresh); everything else gets a generic retry page |

### Persistence layer

- `localStorage` keys: `budget-planner:state` (main state, envelope `{state, version}`),
  `budget-planner:categorization` (learned mapping), `budget-planner:backup:*` (snapshots),
  `disclosure:*` / `settings:cats:*` (UI prefs), `settings:favourite-icons` / `settings:recent-icons`.
- Writes go through the zustand `persist` middleware; `lib/storage.ts` exposes
  `saveAppState`/`setWritesEnabled` for tests and guards every write against
  quota/security exceptions.
- **Migrations** (`lib/validate.ts`): v1 backfills income categories and converts tagged
  `monthlyIncome` transactions into category-bound plans; v2 rewrites those into standalone
  `IncomePlan` entries; v3 backfills the (new in v4) `learnedRules` array; legacy field
  normalization always runs; version > 4 rejected.
  Legacy and corrupt payloads are auto-snapshotted (`auto-vN`, `auto-corrupt`) before any
  parse/migration, so nothing is ever destroyed.
- **Import/export**: JSON export (`serializeExport`/`parseExportPayload`, validated on
  import), CSV export on `/reports`, restore from any valid state payload.
- **Backup/restore**: manual snapshots, automatic legacy/corrupt snapshots, `BackupsManager`
  (list/restore/delete with metadata), `scanRecoverablePayloads` for the recovery screen.

### Known issues & technical debt

1. `writesEnabled` is a hidden module-level flag in `lib/storage.ts` — disabled when
   hydration fails, re-enabled only by import/restore/reset. Intended behavior, but it is
   process-global and can surprise a second store instance in the same session.
2. `lib/scrollLock.ts` keeps a module `lockCount` with no failsafe — if modal cleanup is
   skipped by an error-boundary path, body scroll stays locked for the session.
3. Toast auto-dismiss timers in `useToastStore` are untracked — dismissing a toast early
   leaves a harmless no-op timer (the duplicate component-side timer was removed in the
   production audit).
4. No CI pipeline and no E2E tests — verification is local gates + 324 unit/integration tests.
5. `vitest.config.ts` uses ESM syntax in a CJS-loaded file (Vite 7 will require `.mjs`
   or `"type": "module"`).
6. A few migration tests cast `as unknown as AppState` when constructing legacy payloads.
   (Note: `docs/ARCHITECTURE.md` was refreshed on 2026-08-05 to match the current codebase —
   the earlier stale-component/ID/signature inaccuracies are resolved.)

### Production readiness

Health score **92/100** (audit of 2026-08-05): all gates green, zero TODO/FIXME/debugger,
324 tests, defensive persistence (validation + migration + auto-backups + recovery UI),
clean memory hygiene, a11y foundations (focus traps, aria-live toasts, reduced motion,
skip link), and consistent tokens. Desktop packaging is delivered (see next section).

### Delivered: Electron desktop app (2026-08-05)

Shipped as a Windows desktop app. Zero app-code changes — the static export is served
over a privileged `app://bundle` protocol:

1. `next.config.ts` sets `output: "export"`; all 6 routes prerender to `out/`
   (per-route `.html` + Next 16 `.txt` RSC payloads).
2. `electron/main.cjs` registers `app://` as `standard/secure/supportFetchAPI/stream`
   so localStorage, client-side routing, and `createId()` all work unchanged. The
   protocol handler serves extensionless paths as the app shell (`index.html`, i.e.
   client routes) and maps Next 16's `__next.<route>.__PAGE__.txt` prefetch requests
   to the exported payload files; path-traversal is guarded via `path.resolve`.
3. Window: 1280×800 (min 375×600), `contextIsolation` on, `sandbox` on, popups denied.
4. `--smoke` mode (also `npm run desktop:smoke`) boots the app, clicks the /history
   nav link until the router converges, and asserts title/body/no renderer errors.
5. Packaging: `npm run desktop:package` → electron-builder NSIS installer
   `dist/BudgetPlanner-Setup-0.1.0.exe` + portable `BudgetPlanner-Portable-0.1.0.exe`
   (appId `com.budgetplanner.desktop`; `dist/` is gitignored). The packaged exe passes
   the smoke test. `electron@43` + `electron-builder@26` in devDependencies; electron
   files in `electron/` (eslint-ignored, git-tracked).
6. Known gaps: executables are unsigned; runtime is fully offline (Inter is
   self-hosted by next/font at build time — no request leaves the app).

### Delivered: desktop migration step 2 — dev workflow + secure shell (2026-08-05)

Incremental upgrade of the desktop shell (no UI/business changes):

- **Dev workflow:** `npm run dev` now launches the Next.js dev server **and** Electron
  together (via `concurrently` + `wait-on`). Electron in `--dev` mode loads
  `http://localhost:3000` (`ELECTRON_DEV_URL` overridable) with a retry loop until the
  server answers (~60s cap); it skips the `out/` bundle check in dev. Dev-server smoke:
  `electron . --dev --smoke` runs the same assertions against the dev server (verified
  green: title/navigation/bridge).
- **Secure preload + IPC:** new `electron/preload.cjs` runs sandboxed
  (`contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, no remote
  module). It exposes a minimal, feature-less `window.budgetPlannerDesktop` bridge
  (`platform` + `getAppInfo()` → `ipcMain.handle("desktop:app-info")` returning
  name/version/platform/isPackaged). No desktop features yet — the channel exists as
  the future IPC seam. The smoke test now asserts the bridge is exposed and answers.
- **Scripts** (`npm run electron`, `dev`, `build`, `dist`, plus `icon` and the
  `dev:web`/`dev:electron` parts): `npm run build` → `out/` static export;
  `npm run dist` → electron-builder Windows installer + portable
  (`desktop:package` = build + dist). `concurrently` + `wait-on` added to
  devDependencies.
- **Icon:** `scripts/make-icon.mjs` (pure Node PNG/ICO encoder) generates
  `build/icon.ico` + `build/icon.png` — indigo rounded square with a white coin and
  brand ring (brand-600/700). `npm run icon` regenerates; swap the drawing whenever
  real artwork exists. NSIS switched to the assisted installer (`oneClick: false`):
  install dir choice, desktop + start-menu shortcuts, and standard uninstall support
  (Add/Remove Programs entry) out of the box.
- All gates green (tsc, lint, 324 tests, build); `dist/` installer + portable built and
  the packaged exe passes the smoke test with the bridge active.

### Delivered: desktop migration step 3 — SQLite persistence (2026-08-05)

Browser persistence replaced inside the desktop shell (web app behaviour fully
preserved; browser mode untouched):

- **SQLite in the main process:** `electron/db.cjs` — `better-sqlite3` is the only
  SQLite access; the renderer never touches the database. One `kv(key, value)` table
  (WAL, `synchronous=NORMAL`, `user_version=1` for future schema phases) at
  `<userData>/budget-planner.sqlite3` (productName "Budget Planner" set top-level so the
  app owns its profile). `better-sqlite3@13` is a runtime dependency; electron-builder
  rebuilds it for the Electron ABI at package time and unpacks it from the asar.
- **IPC storage seam:** preload exposes a synchronous `storage` bridge
  (`desktop:storage:get/set/remove/keys/needs-migration/migrate`, `sendSync` —
  deliberate: the app's storage seam is localStorage-shaped and handlers are trivial
  prepared statements; inputs validated main-side). `lib/desktop.ts` types the bridge;
  `lib/storageAdapter.ts` is the single persistence seam — bridge in Electron,
  `localStorage` in a browser (and no-op in SSR). All call sites migrated: zustand
  persist, `lib/storage.ts` (state, backups, snapshots, export/scan), categorization,
  theme bootstrap (bridge first, localStorage fallback), disclosure state, icon
  favourites/recents. A failed bridge write throws, mirroring quota errors.
- **First-launch migration with backup:** preload ships the page origin's localStorage
  to the main process when the db is empty; main writes a full backup row
  (`budget-planner:backup:migration-browser:*`, appears in BackupsManager) then the rows
  and the `migration:browser:done` marker in ONE transaction (atomic, idempotent —
  verified: 10 rows incl. backup on first launch, unchanged on relaunch).
- **Migration failure handling:** the whole migration returns a structured error instead
  of throwing across IPC; on failure the transaction rolls back (nothing written, marker
  absent, browser localStorage untouched) so the original data is preserved and the
  migration retries next launch. The main process shows a native error dialog (skipped
  in smoke mode) and both processes log it. `npm run db:check` exercises both the success
  path and a forced-failure path (error returned, 0 rows left, retry-ready).
- **Native toolchain:** machine needed Python (installed 3.12 via winget) + VS 2022 C++
  for node-gyp; `postinstall` = `electron-builder install-app-deps` keeps the local
  module matched to Electron; `npm run db:check` verifies the native module + optional
  read-only dump of a real db (`electron/scripts/dbcheck.cjs`). No temp-file scripts.
- **Verification:** smoke now asserts a full SQLite roundtrip through the bridge and the
  db file's existence under userData (dev + prod + packaged exe all green); tests
  324→330 (6 storageAdapter cases); tsc/lint/build green. Profile note: the desktop app
  now uses `Roaming\Budget Planner` (was `Roaming\budget-planner` pre-productName).

### Delivered: desktop migration step 4 — native desktop features (2026-08-05)

Web behaviour preserved; every feature is additive and desktop-only:

- **Native application menu** (`electron/menu.cjs`): File (Import `Ctrl+O`,
  Export `Ctrl+S`, Back up now `Ctrl+B`, Restore latest backup `Ctrl+Shift+B`,
  Open backup folder `Ctrl+Shift+O`, Reveal data folder `Ctrl+Shift+D`, Quit
  `Ctrl+Q`), Edit (standard clipboard roles — required for copy/paste in inputs
  on Windows), View (reload/devtools/zoom/full screen), Window, Help (About
  dialog). Actions that need renderer state send `desktop:menu:action` to the
  focused window; folder actions + About run in main only.
- **Native file dialogs + safe file IPC:** generic `dialog.open/save` and
  restricted `fs.writeText/readText` (absolute paths, `.json`-only writes,
  16 MB cap). Composite `desktop:import` (open → destructive confirm → read)
  and `desktop:export` (save dialog → atomic write) drive the Settings buttons
  and menu items. Browser mode unchanged.
- **Desktop notifications** via `desktop:notify` (guarded by
  `Notification.isSupported()`); used for backup failure + restore results.
- **Automatic file backups** (`electron/backups.cjs`): atomic temp+rename
  writes, content dedupe, prune to newest 30 in `<userData>/backups/`; schedule
  = boot (after hydration) + every 30 min + `beforeunload` flush (sync IPC,
  like the storage seam). Failures → toast + desktop notification.
- **Restore backup workflow:** file backups listed in Settings → Data
  (BackupsManager "Backup files" section): restore w/ confirmation, download
  via save dialog, delete. Menu "Restore latest backup" reads the newest file
  in main, confirms natively, restores via `importState`. File backups and
  exports share the `{ state, version }` envelope.
- **Open Backup Folder / Reveal Data Folder** via `shell` IPC; buttons in
  Settings and the File menu; real paths shown in the About/Data cards.
- **Verification:** smoke asserts menu groups + accelerators, full bridge
  surface, and a create/list/read/delete backup roundtrip (dev + prod green);
  tests 330→337 (+7 `electron/backups.test.ts`); tsc/lint/build green.

### Delivered: desktop migration step 5 — startup, identity & updates (2026-08-05)

No business-logic changes; additive and desktop-only:

- **Splash screen** (`electron/splash.cjs`): frameless, skip-taskbar, `data:`-URL
  window (inline SVG icon replica, app name, `v<version>`, animated bar; no
  preload → cannot touch app state) shown while the main window boots; closed
  and replaced by the main window on `ready-to-show`; destroyed on quit;
  skipped in `--smoke` mode.
- **Loading screen** (`app/loading.tsx`): root route renders the existing
  `PageSkeleton` while the client bundle loads and on route transitions.
- **Proper application icon** (`scripts/make-icon.mjs` rewritten): designed
  mark — indigo rounded square, diagonal brand gradient (brand-500→600→800),
  white coin with brand-700 edge shade + drop shadow, three ascending bars;
  512 px master with 4×4 supersampled AA (SDF geometry, zero dependencies);
  box-downsampled to seven ICO entries (16–256 px) + 512 px PNG. Drives the
  exe/installer/window/splash icons (`npm run icon` regenerates).
- **Version information**: `lib/version.ts` (`APP_NAME`/`APP_VERSION` from
  package.json) replaces the hard-coded "1.0" in Settings → About, which now
  also shows the desktop shell versions (Electron/Chromium) from the extended
  `getAppInfo()` payload (`versions`). Native About dialog (Help) lists app
  version, Electron/Chromium/Node, platform, update-feed state, folder paths.
- **Auto-update scaffold** (`electron/updater.cjs` + `electron-updater`):
  generic feed from `AUTO_UPDATE_URL` env or `<userData>/update-feed.txt`
  (env wins); packaged-only, inert without a feed; background check at startup
  + Help → Check for updates…; auto-download, install-on-quit, notifications.
- **Installer branding**: NSIS installer/uninstaller/header icons all use
  `build/icon.ico`; `copyright` metadata set.
- **Verification:** smoke green in prod + dev (splash/updater no-op under
  `--smoke`); packaged-exe smoke green after `npm run dist`; tests stay 337;
  tsc/lint/build green.

### Future roadmap (candidates, not committed)

- CI pipeline (lint/typecheck/test/build on push) + Playwright E2E smoke — recommended
  before or with the Electron release.
- Multi-device sync / backend — the largest scope change; explicitly out of the current
  client-side design.
- Move to IndexedDB behind the storage interface if datasets approach ~50k records
  (current localStorage quota ≈ tens of thousands of transactions, far beyond personal use).

## Change log

| Date | Change | Affected documents |
|------|--------|--------------------|
| 2026-08-27 | **OPay statement import fixed against the REAL current export — it was failing entirely (“No transactions could be detected in that file”).** The real 11-page statement was read directly from `tests/fixtures/statements/Opay/` rather than reconstructed, and it invalidated four assumptions at once. **• The file is TWO complete statements, not one** — a Wallet account (pages 1-7) and a Savings account (pages 7-11), each with its own header block and table, and the second table's money columns sit ~38pt further left. Column anchors were derived once per page with no cross-page carry, so the headerless continuation pages 8-11 fell back to gap splitting and merged the Debit/Credit/Balance triple into one unparseable cell. `rowsFromPdfPage` now threads anchors page to page AND re-anchors at every header, since a page can contain more than one. **• OPay prints `--` in whichever money column does not apply, on EVERY row.** Read as an unparseable amount it rejected the whole statement. New opt-in spec field `emptyMoneyMarkers`; an explicit marker also PROVES the columns did not shift, so the left-shift guard stands down for that row. **• The header spans two baselines** (“Balance After” above “Debit (₦) Credit (₦)”) and the currency marker tears across columns when the header is rebuilt from x-coordinates, arriving as `Balance After ₦)` and `( Channel`. Neither matched its role, so every row came back with no balance and no channel; `headerToken` now strips orphaned brackets and bare currency marks. A header BAND that stops at the first data line in each direction merges the split label into one row. **• One transaction spans a BLOCK of lines with the date/amount line in the MIDDLE** — narration and the 24-digit reference wrap both ABOVE and BELOW it. `parseOpayStatement` was discarding `rowYs` entirely (`_rowYs`) and the registry declared `wrappedLines: false`, so every wrapped line attached to the preceding transaction: two narrations merged and references split in half. Geometry is now forwarded, fragment groups END at a block boundary (>1.8× the statement's own median line height), and a new opt-in `wrappedReference` rejoins the reference halves in page order. Also fixed a latent duplication in the shared merge: a row whose own narration cell was empty had its fragments printed TWICE. **Result — exact reconciliation against the statement's own two summary blocks:** 250 transactions, 137 debits totalling ₦1,170,461.00, 113 credits totalling ₦1,170,438.62; 250/250 rows carry a reference, 178 carry a balance, 250 carry a channel. **The fixture was wired into NOTHING before today** — its manifest entry was not even in the `id` union, so the repo did not typecheck, while `lib/__tests__/opayParser.test.ts` passed against a tidy constructed header the real export never emits. New `tests/fixtures/statements/opayReal.test.ts` (17 tests) reconciles against the printed totals, and new `tests/fixtures/statements/fixtureCoverage.test.ts` fails whenever a fixture sits in the repo unread by any test — the gap that let this drift. **Fixture staleness audit:** GTCO-real and PalmPay are verified end-to-end against printed totals; **Kuda is only PARTIALLY verified** — its OCR path covers just 5 transactions (page 2 of the scan is too dark to read) and no real TEXT-LAYER Kuda statement exists in the repo, so half that parser is exercised only by representative rows. `lastVerified` / `coverage` / `coverageNote` are now declared as data on every fixture. Scoped as asked: no other institution's parser, no review UI, no categorization or duplicate-detection change — both new spec fields are opt-in and off by default, and the geometry-less (CSV/Excel) path keeps the previous behavior exactly. **Known remaining imperfection:** 5 rows at the Savings table's page boundary are skipped as “invalid date” (they are footer/summary lines, not transactions) and a few narrations still read as bare account numbers where OPay itself printed nothing else. | `ARCHITECTURE.md` §4.1 (cross-page anchors, block grouping, the two opt-in spec fields, OPay parser entry) and new §4.1a (statement format verification tiering), `tests/fixtures/statements/manifest.ts` layout notes, this file. |
| 2026-08-27 | **Import Statement: confirmed the review→import flow is per-row-first — selection is never a gate, one Import for the whole non-excluded batch; uncategorized rows are individually blocked (skipped + reported), not bucketed.** Audit requested to remove a "checkbox-select rows → bulk-assign a category → Import only the checked subset" gating step found that the flow ALREADY works this way (the earlier same-day fix removed the category block; this audit closed the remaining questions and locked the semantics in with dedicated regression tests). **Verified semantics, unchanged in code:** `importBlocked` in `ImportStatementModal.tsx` depends solely on `plan.unresolvedDuplicates > 0` (FR-23 — an unanswered possible-duplicate is the one thing that must be decided before finalizing); `planImport` (lib/statementPipeline.ts) NEVER reads the row's `selected` flag, so the Import button's count and the single `addTransactions` write are always the whole reviewed, non-excluded batch — "Import 96 transactions", not the checked subset; the per-row checkboxes + bulk bar (Select all / Assign category… / Exclude / Include / Clear selection) remain purely an OPTIONAL convenience for applying one category to many similar rows (e.g. 51 same-merchant low-confidence rows); exclusion is the only per-row reason a categorized row stays out (alongside the mechanical skips: money movements, missing dates, within-statement duplicate repeats, already-imported rows, answered skip-as-duplicates). **Uncategorized-row decision (stated):** a row with NO category is blocked individually — skipped at import, flagged inline in review with a warn "Assign" affordance, named in the footer ("N uncategorized rows will be skipped — categorize them to include them.") and in the done screen's skipped breakdown — rather than defaulted into an "Uncategorized" bucket, because `Transaction.categoryId` is REQUIRED everywhere else in the app (manual entry validates "Choose a category." and the validator rejects blank), so a bucket would contradict the data model and silently create uncategoryable ledger rows. **Tests added** (`ImportStatementModal.test.tsx`, new "per-row categories are sufficient (no bulk gating)" describe, 4 cases): a full batch imports using ONLY individually-set per-row categories with the bulk-assign control never touched (and absent from the DOM while nothing is selected); checking a row's checkbox never scopes the Import count or the write (selection is for bulk edits only); an excluded row stays out of the single-action import with the count reflecting the rest; the uncategorized row is the ONLY blocked row — visible indicator, footer report, never imported blank. Needs-review / low-confidence / duplicate flags untouched and still surfaced (filters, chips, FR-23 block unchanged). Gates green: `tsc --noEmit`, `npm run lint`, full vitest suite (modal file 85 passed), `npm run build`. | `ARCHITECTURE.md` (§4.1 flow: selection-never-scopes + uncategorized rationale), `06_PLANNER_SCREEN.md` (Import Statement header description), this file. |
| 2026-08-27 | **Recurring-pattern detection + quick-add suggestions + soft anomaly flagging (FR-25) — derived-only, additive, non-blocking; no AI/network calls.** Two new PURE modules: `lib/recurringPatterns.ts` (detects recurring patterns per category from the ledger — 3+ occurrences minimum ("two is a coincidence"), amounts chained within ±10% step-to-step, interval DETECTED as weekly/biweekly/monthly/yearly from median day-gaps rather than assumed monthly; expected amount = recency-weighted mean so price drift moves the expectation forward, and a jump beyond tolerance starts a newer run that owns the (category, cadence) id; next-date projection is calendar-aware for monthly (day-of-month anchor, clamped); `isPatternDue`/`patternsDueBetween` give due windows — grace 3 days before through 7 days ahead) and `lib/anomalies.ts` (`categoryRecentAverage` over a trailing 6-month window of expense entries + `checkAnomaly`: flags only when ≥3 prior entries exist and the candidate exceeds 2× the average — strictly HIGHER only, exactly 2× does not clear; insufficient history never produces a false positive). NEITHER persists anything — patterns and averages derive from `transactions` every read (the FR-21 streak asymmetry), so no schema bump, no migration, nothing to export/validate. UI wiring, all advisory: `RecurringSuggestions` card on the Planner between Budget Status Band and Budgets, rendered ONLY when a pattern projects into the viewed month ("Due soon" emphasis inside the due window; "Add" opens the ordinary form pre-filled via the new `TransactionPrefill` → `TransactionForm initialDraft` seed frozen at mount per §3.3a, restarted by keyed remount in PlannerView); `RecurringQuickFill` hints inside the Add Expense/Income flow ("This looks like your recurring {category} payment of ₦{amount} — add it?" with a "Fill in" button) pre-filling amount/category/note/date WITHOUT saving; `AnomalyNote` (`role="note"`, dismissible per exact category+amount pair) beside the amount input in both TransactionForm and Quick Add Expense when the typed amount clears the documented threshold. Confirmation always goes through the normal submit path; manual entries matching neither a pattern nor an anomaly behave exactly as before; rule-generated instances (`recurringRuleId`) never feed detection. Tests 1534→1564 across 104→107 files: 13 `recurringPatterns.test.ts` (incl. 3-vs-2 occurrences, drift re-weighting, newer-run takeover, month-end clamping, rule-instance exclusion), 8 `anomalies.test.ts` (3× flagged, 2× not, <3 priors not, out-of-window priors ignored), 5 `TransactionForm.suggestions.test.tsx` (pre-fill without saving then normal submit; note shown/dismissed; insufficient-history silence; transfer-tab silence), 3 `RecurringSuggestions.test.tsx` (row content, full-prefill callback with zero writes, hidden when nothing due). Gates green: `tsc --noEmit`, `npm run lint`, full vitest suite, `npm run build`. Docs: PROJECT_SPEC FR-25 + AC-47..50, ARCHITECTURE §2 + new §3.6, `06_PLANNER_SCREEN.md` page order + section, UI_UX_SPEC planner + transaction-form sections | `PROJECT_SPEC.md`, `ARCHITECTURE.md`, `06_PLANNER_SCREEN.md`, `UI_UX_SPEC.md`, this file. |
| 2026-08-27 | **Statement import no longer forces a category on every row — categorized rows import, uncategorized rows are skipped.** The import review screen was blocking the ENTIRE import until every ledger-able row had a category ("Assign a category to the highlighted rows to import." with a disabled Import button), so a 96-transaction statement with 77 uncategorized rows could not import the 19 that were already classified. The block was purely a UI gate — `planImport` (lib/statementPipeline.ts) has ALWAYS excluded uncategorized rows from `inputs` and counted them in `plan.missingCategory` (`if (row.categoryId === null) { missingCategory += 1; continue; }`), so the pipeline never needed changing. **Fix (UI + messaging only):** `importBlocked` in `ImportStatementModal.tsx` now depends solely on `plan.unresolvedDuplicates > 0` — an unanswered possible-duplicate is still the one thing that must be resolved before finalizing (FR-23: we must not guess double-count vs discard), but a missing category never blocks. The Import button reads `Import {plan.inputs.length} transaction(s)` and stays enabled; the footer explains categorized rows import now and uncategorized rows are skipped ("{n} uncategorized rows will be skipped — categorize them to include them."), and the done stage's Skipped breakdown reports the skipped-uncategorized count via `plan.missingCategory`. `Transaction.categoryId` remains REQUIRED, so an uncategorized row genuinely cannot enter the ledger — it is skipped and reported, never written blank. Tests updated: five `ImportStatementModal.test.tsx` cases that asserted the old blocking behavior now assert the Import button is ENABLED, the skip message shows, and the categorized rows import (the two rewritten cases confirm 2 transactions import while the uncategorized one is skipped); the re-import "Nothing to import" case stays enabled unchanged. Gates: all green — `tsc --noEmit`, `npm run lint`, `npm run test` (102 files, 1534 passed, 1 skipped), `npm run build`. | `ARCHITECTURE.md` (§4.1 import preview/confirm flow), `CHANGELOG.md`, this file. |
| 2026-08-27 | **Real sample bodies swapped in as fixtures; three more layout bugs found and fixed that the reconstructions had hidden.** Supersedes the earlier same-day entry, which corrected these templates against the field-level facts only — the six verbatim bodies had not reached the session then. They have now, and they invalidated three structural assumptions the reconstructions could not show: **• GTBank and Wema send HTML TABLES.** Converted to text they arrive as `| Label | : | Value |`, so every label-anchored pattern failed — label and value are separated by ` | : | `, not `: `. New `normalizeAlertBody()` flattens those rows before any matching; prose bodies pass through untouched. **• Quick Microfinance is not a table at all.** Its labels sit on their own line with the value on the NEXT (`Amount\n₦53.75`). A new shared `LV` label→value separator crosses exactly one newline, so a pattern can never reach past a blank line into an unrelated field. **• Wema was not recognised as an alert either.** Its markers assumed “transaction alert” / “has been debited” / `\bwema\b`; the real wording is “a Debit transaction recently occurred”, and the only “wema” in the body is inside `wemabank.com`, where `\b` fails on the trailing “b”. Every Wema alert would have been discarded — the same class of silent failure GTBank had. **The narration heuristic was picking the WRONG SEGMENT on real data** (the specific thing flagged for re-verification). Real debit narration is `FT_Out Fee:DAVID OSAHON OGBEIDE_8082389369_OPAY NIGERIA_Amount Transfer` — account holder, phone, merchant, type. The “longest alphabetic segment” rule returned **DAVID OSAHON OGBEIDE**, the account holder, because their name is longer than the merchant’s. Replaced with phone-anchored selection: take the first usable segment AFTER the numeric one, yielding **OPAY NIGERIA**. Position is reliable; length was a guess that happened to look right on invented data. **Two further real-data findings:** GTBank prefixes a ~30-digit bank reference to its credit narration (`100004260730180812166829083192-TRANSFER FROM …`) — left in, every transaction gets a unique categorization key and no learned rule could ever match twice, so a leading run of 8+ digits is now stripped; and Quick Microfinance puts its balance FIGURE on the line after the `Current Available Balance` label, so the balance stripper now drops the following line when a balance label carries no figure (otherwise ₦250,765.10 survived as an orphan for a later pattern to find). Both institutions truncate their own description fields mid-word (`…-DAVID OSA`, `…OGBEIDE FROM DA`); both are kept verbatim as valid-but-imperfect, and Wema’s `NIP:UFY X UNIVERSAL SERVICES LIMITED- Prime` plus GTBank’s hyphen-containing `SMS ALERT CHARGE FOR 27-JUN-26 TO 28-JUL-26` are confirmed to pass through unmangled. All six real bodies are now permanent fixtures in `lib/__tests__/emailAlertsReal.test.ts` (37 tests), pasted verbatim rather than tidied — their awkwardness is the point. The reconstructed bodies were REMOVED from the two general email suites and replaced with the real table layout, so no test can pass against a shape these banks do not send. Tier 2 (Access, UBA, Zenith, Kuda, Moniepoint, PalmPay, OPay) remains unverified guesswork and is banner-marked as such; every Tier 1 template was wrong before its samples arrived, two of them completely, so Tier 2 should be assumed wrong until proven otherwise. Security model, credential storage and the IPC boundary unchanged, as scoped. Gates: tsc/lint/1534 tests across 102 files/build green. | `15_EMAIL_PARSING.md` (tiering table with body layouts, the three layout fixes, phone-anchored narration rule), this file. |
| 2026-08-27 | **Email alert templates CORRECTED against real samples for three institutions — GTBank, Wema, Quick Microfinance Bank.** **Tiering introduced.** The registry now separates templates verified against real mail from those still built on representative guesses. Tier 1 (corrected today): **GTBank, Wema Bank, Quick Microfinance Bank**. Tier 2 (still guesses, explicitly banner-marked in the registry and the doc): Access, UBA, Zenith, Kuda, Moniepoint, PalmPay, OPay — these need the same pass when samples arrive. **Accuracy audit of the three (req 1): NONE of them was accurate; two did not exist.** • **GTBank — completely broken, worst finding.** Its `alertMarkers` were `/transaction\s+alert/`, `/\bTxn\b/`, `/\bAmt\b/`, none of which appear in a real alert (subject is “Transaction Notification”, body says “a DEBIT transaction occurred”). Every GTBank message was therefore discarded as `not-a-transaction-alert` — the feature would have silently ignored the institution entirely. Direction patterns (`Txn: Debit`), amount label (`Amt:`) and description label (`Desc:`) were all wrong as well. Proven with a reproduction before the fix. • **Wema Bank — absent from the registry.** Added. Its currency-code SUFFIX (`3,000.00 NGN`) could not be expressed by ANY previous pattern, all of which assumed a prefix. • **Quick Microfinance Bank — absent from the registry.** Added, keyed to its real sending domain. **One shared money parser (req 2).** `parseMoneyToken` + the `MONEY` regex fragment replace the old prefix-only `CUR`/`NUM` pair and normalize every confirmed shape: code prefix with decimals (`NGN 3.51`), code prefix WITHOUT decimals (`NGN 26388` — same sender, inconsistent), code suffix (`3,000.00 NGN`), symbol prefix (`₦250,000.23`), and code glued to digits (`NGN7,450.00`). Built to absorb a fourth institution: the currency marker is optional on both sides and any ISO-4217-shaped code or major symbol works. **A second bug was found while doing this:** `\bNGN\b` cannot match `NGN7,450.00` — there is no word boundary between “N” and “7” — so the boundary is now asserted on the outer side only. **Negative balances (req 3).** `parseMoneyToken` returns `{minor, negative}`, keeping the sign SEPARATE rather than folding it into the value. Two guards: `parseAlertAmount` rejects any negative token outright (a transaction amount is never negative), and the evaluator strips every `/balance|bal\s*[:.]|avail\s*bal/i` line BEFORE amount extraction — so `Available Balance : NGN -28.48` cannot be read as the amount, and neither can a positive balance. The second guard applies to every template, present and future. **GTBank direction (req 4): was NOT correct, now is.** It keyed on `Txn: Debit`, which does not exist. Direction now comes from the body (`a DEBIT/CREDIT transaction occurred`); a test asserts both fixtures share the identical generic subject and still resolve opposite directions. **Narration quality within one sender (req 5).** New `cleanNarration`: a string with no underscores is returned COMPLETELY unchanged (so Quick Microfinance’s clean credit prose and GTBank’s truncated descriptions are untouched), while an underscore composite has its leading `LABEL:` prefix, phone/reference digits and transfer-type noise (FT, Out, Fee, NIP, TRF, POS…) dropped, keeping the longest remaining alphabetic segment as the merchant. The categorization engine therefore receives `BESTAF TECHNOLOGIES` rather than the whole underscore string, which could never have matched a learned rule. **Truncated descriptions (req 6).** Treated as valid-but-imperfect: no crash, no `needs-review`, kept verbatim. **Allowlist keyed to the VERIFIED domain, not the brand (req 7).** Quick Microfinance Bank sends from **quickmart.com**, which has no relationship to its brand name — any brand-derived guess would have rejected every legitimate alert. Confirmed: matching remains exact-host-or-subdomain with **no brand-name fallback**, and tests assert both that `quickmart.com` (and its subdomains) are accepted and that brand-shaped lookalikes (`quickmicrofinance.example`, `quick-mfb.example`) and `quickmart.com.attacker.example` are refused. A brand-name fallback would have been an allowlist bypass. **Fixtures (req 8).** New `lib/__tests__/emailAlertsReal.test.ts` (28 tests) holds the six debit/credit fixtures. Superseded guessed GTBank fixtures were REMOVED from the older test files rather than left alongside, so no test can pass against a format that does not exist. **Caveat recorded in the fixture header:** the six verbatim sample bodies did not reach the session (the prompt’s paste placeholder came through literally), so every asserted VALUE is taken from the ground truth stated in the requirements while the surrounding line layout is reconstructed — the field formats are pinned, the exact layout still needs confirming against the raw mail. Security model, credential storage and the IPC boundary are unchanged, as scoped. Gates: tsc/lint/1525 tests across 102 files/build green. | `15_EMAIL_PARSING.md` (tiering, corrected formats, money parser, balance rule, narration rule, allowlist rationale), this file. |
| 2026-08-26 | **Email alert parsing (FR-24) — PARTIAL: allowlist, template registry, credential vault and draft pipeline landed; IMAP transport and UI not yet.** **Scope note up front:** this is the most security-sensitive feature in the app and it is NOT complete. Parsing, credential storage and routing are built and unit-tested (54 tests); nothing reads a mailbox yet. See `15_EMAIL_PARSING.md` § Remaining work. **Two stack conflicts flagged rather than worked around, as the prompt asked:** (1) **The browser build cannot satisfy the credential requirement at all.** `safeStorage` is Electron-only, and every store available to a web page (localStorage, IndexedDB, cookies) is readable by any script on the origin. The feature is therefore desktop-only and `lib/emailCredentials.ts` reports `{available:false, reason:"not-desktop"}` rather than degrading to a weaker store — a “best effort” fallback would look like it worked while leaving the password readable. The same refusal applies inside Electron when the OS keychain is missing (e.g. a Linux box without libsecret): there is NO plaintext or obfuscated fallback anywhere in the path. (2) **IMAP + app password chosen over Gmail OAuth.** The prompt allowed OAuth only if the codebase already had a pattern to build from; it has none (no OAuth client, no token refresh, no redirect handling, no dependency). A first OAuth implementation would ship a client secret inside a desktop binary — where it is not secret — plus a loopback listener and refresh-token storage: more attack surface than one revocable, provider-scoped app password in the OS keychain, and IMAP satisfies “not locked to one provider” directly. **Credential vault** (`electron/credentials.cjs`): `safeStorage` over the OS keychain, ciphertext at `<userData>/credentials.v1.json` (mode 0600). **The IPC surface has no read channel and must never gain one** — available/set/status/clear only; decryption happens solely in the main process at connection time, so a compromised renderer cannot obtain the password. Tests assert the secret never appears in the on-disk file, that an unavailable keychain refuses rather than falling back, and that disconnect removes the file outright. **Allowlist** (`ALERT_SENDERS`): eight institutions (GTBank, Zenith, Access, UBA, Kuda, Moniepoint, PalmPay, OPay), matched exact-host-or-subdomain and NEVER by substring — a substring match would accept `gtbank.com.attacker.example`, and there is a test for exactly that. Non-allowlisted mail is discarded at the sender check, before any parsing. **Template registry** (`ALERT_TEMPLATES`): per-institution extraction rules as DATA, interpreted by one shared evaluator, so adding a bank is an array entry plus tests rather than a new parsing function. A guard test fails if a sender is added without a template. The shipped regexes were written against representative formats, not scraped from live mail — documented as such, which is precisely why the failure mode is needs-review rather than a wrong transaction. **Parse failures surface, never vanish** (req 9): `needs-review` keeps every field that WAS read, names the missing ones, and carries a 400-char body snippet; `planImport` refuses such a row, so it cannot reach the ledger half-formed. **Engines reused, not rebuilt** (req 6): `lib/emailPipeline.ts` owns no categorization and no duplicate logic — it calls `suggestCategory` and `findDuplicateCandidates` directly, so a rule learned during a CSV import applies to an email alert and an alert mirroring a hand-typed transaction is flagged and blocks the batch. Output is `ImportRow[]` into the existing `planImport` confirm step (req 7), so nothing auto-saves. New tests: `lib/__tests__/emailAlerts.test.ts` (23), `lib/__tests__/emailPipeline.test.ts` (17), `electron/credentials.test.ts` (14) — covering four institution templates with realistic bodies, lookalike-domain rejection, malformed-to-needs-review, safeStorage round-trip, the no-plaintext-fallback refusal, and disconnect clearing everything. | **new `15_EMAIL_PARSING.md`** (security model, allowlist, template registry, how to add an institution), `PROJECT_SPEC.md` (FR-24), `ARCHITECTURE.md` (§3.3b security boundary), `CHANGELOG.md`, this file. |
| 2026-08-26 | **Learned categorization promoted to a shared engine (FR-22) — plus a serious matching bug fixed.** **Most of this already existed** and was NOT rebuilt: recording corrections (`recordCorrection`), normalization (`normalizeRuleKey` + `lib/merchant.ts` merchant extraction), persistence, pre-fill during import, override-wins re-baselining, and a Settings management panel all shipped with Prompt 6A. This change closes the real gaps. **BUG FIXED (critical):** `activeRuleFor` selected only the FIRST rule of each signal kind (`candidates.find(r => r.kind === kind)`) and then compared its key — so once a user had two merchant mappings, the second could NEVER fire and learning silently stopped after one mapping per signal. Confirmed with a failing test before the fix and regression-tested after. **Shared entry point (req 6):** `suggestCategory({description, merchant?, provider?, direction?}, rules, categories)` plus the bare-string `suggestCategoryForText`. It takes plain strings rather than a statement row, so the planned email-alert parser can call it without duplicating the learning; `activeRuleFor` is now a thin enabled-only wrapper over it and the import pipeline is unchanged. **Fuzzy matching (req 2):** token-overlap (Dice) at `FUZZY_MATCH_THRESHOLD = 0.82`. Token-based rather than character-based because statement noise arrives as extra WORDS — an edit distance would score a long shared prefix as similar even when the trailing words name a different payee. All EXACT matches across every signal are exhausted before any fuzzy match, since an exact key is the thing the user actually corrected. The threshold is deliberately high: a false positive files money under the wrong category, which is worse than asking once more. **Pre-fill vs auto-apply reconciled:** the requirement wants a single correction to pre-fill, while the existing (documented, tested) design refuses to create a rule from one ambiguous transaction. Both are satisfied via `CategorySuggestion.confident` — a strength-1 candidate PRE-FILLS the category but leaves the row flagged for review, and only an enabled rule (strength 2+) classifies without review. The existing activation threshold was not weakened. **Visible provenance (req 2):** the classifier already recorded `categoryReason: "learned:…"` but nothing ever rendered it; review rows now show a “Learned” chip beside the category select, with hover text explaining it came from a past correction and can be changed. The batch still requires explicit confirmation. **Usage tracking (req 3):** new optional `LearnedRule.lastUsedAt`, stamped via `markRulesUsed` when a pre-filled row is imported WITHOUT being overridden — corrections are learning, not usage. Nothing expires automatically, as specified. **Management (req 5):** added “Clear all” (confirmed, naming the exact count, and stating that transactions and categories are untouched); list/toggle/re-target/delete already existed. **Storage location — deviation from the prompt, explained:** mappings stay in `AppState.learnedRules` rather than moving to a new `budget-planner:categorization` key. Everything in `AppState` is validated, migrated, exported, backed up and restored together behind the one SQLite state row; a separate top-level key would sit outside all five, be missing from every export and backup, and survive a restore that rolled the ledger back — leaving mappings pointing at category ids that no longer exist. The keys that DO live outside `AppState` are the ones that must not travel with the ledger (icon favourites, disclosure state, display name, onboarding step); learned mappings reference `Category` ids, so they are ledger data. Documented as ARCHITECTURE §3.4a. No AI/ML and no network anywhere in the path; manual (non-import) transaction entry is untouched. New tests: `lib/__tests__/suggestCategory.test.ts` (19) covering exact match, every-mapping-matches (the bug), signal precedence, direction guards, deleted categories, fuzzy above and below threshold, exact-beats-fuzzy, candidate vs confident, override rewriting, flip-flop deactivation, delete/clear removing suggestions, the bare-string entry point and usage stamping; plus 3 in `LearnedRulesPanel.test.tsx` for Clear all. Gates: tsc/lint/1411 tests across 97 files/build green. | `PROJECT_SPEC.md` (FR-22, AC-42..AC-46), `ARCHITECTURE.md` (§3.4a storage rationale + the pipeline block), `09_SETTINGS_SCREEN.md` (Learned rules section), `CHANGELOG.md`, this file. |
| 2026-08-26 | **Savings streaks and badges (FR-21): cosmetic recognition on a data-driven badge architecture.** **“On track” is not redefined.** A month qualifies iff the app’s EXISTING total-spent-versus-total-budgeted calculation says so — `budgetUtilizationSeries` (lib/selectors.ts), which already sums rollover-aware effective limits. That selector is only READ; nothing about budget health changed. `monthStatus` compares the RAW totals rather than the rounded `pct`, because at 100.4% the percentage rounds to 100 and an overspend would read as on track. **Three outcomes, not two:** `on-track` / `over` / `no-data`. A month with no budgets is not a month you stayed within budget, so it can never qualify by having nothing to fail — it breaks a run exactly as an overspend does. **The month in progress never counts.** The streak describes finished months only: counting the current one would hand a free +1 to someone three days in, and would show a streak as broken mid-month for someone who will be comfortably inside their limits by the 31st. That is also what makes a brand-new user’s first month behave correctly — it contributes nothing until it ends. **Streak derived, badges stored — deliberately opposite.** `streakStats` walks the ledger backwards on every read rather than incrementing a saved counter, so editing a past month corrects the count instead of leaving a number that disagrees with the data. Badges are persisted append-only (`AppState.badges`: `id`, `earnedAt`, `value`) because an achievement must survive a later reset; streak-based criteria are measured against `longest`, never `current`, for the same reason. **Badges are data, not code.** A `BadgeDefinition` carries `{ id, name, icon, description, tier, criteria, reward }` where `criteria` is a DESCRIPTOR (`first-on-track-month` / `streak-months`) interpreted by one shared evaluator — so adding a badge is a new array entry, with no new component code and no new conditional. Launch set is four (first month, 3, 6, 12). Icons come from the shared icon library and a test asserts every badge icon actually exists in `ICON_GROUPS`. **`reward` is reserved and always `null`** on every badge; nothing reads it and there is NO reward-granting logic anywhere — the drawer tells the user in as many words that badges unlock nothing. The field is declared now so a later perk system can be layered on without migrating badge data; `EarnedBadge.value` already records the stat that earned each badge so such a layer would not need to recompute history. ARCHITECTURE §3.5 states that it must live in a NEW module that READS the field, never as behaviour added to the evaluator. **Display placement:** one more flag INSIDE the existing Budget status band (“{n}-month streak”, sparkle icon, whole row a button opening a Streak & badges drawer) rather than another dashboard card — consistent with the ongoing effort to consolidate rather than add. Hidden at 0. **Gap found while testing and fixed:** hiding the row at 0 made earned badges completely unreachable after a streak reset, so the row persists as “{n} badges earned” whenever any badge is held. Locked badges are greyed with their requirement and a progress bar, never hidden, so there is something visible to work toward. Evaluation runs once at mount from `AppShell` via `hooks/useBadges.ts`, mirroring `useRollover` — the streak only counts completed months, so it cannot change mid-session. `newlyEarnedBadges` returns only unheld ids and `grantBadges` filters again on write, so re-running every launch can never duplicate or re-grant an achievement. Schema **v8 → v9**: backfills `badges: []` and awards nothing retroactively (the derived streak recognises real history on the next evaluation, so no migration needs to invent earned records). The validator drops badge ids retired from `BADGES` and duplicates, rather than failing the load. Verified in real Chromium: a four-month seeded history rendered “4-month streak” in the band (correctly excluding the current month), persisted `first-month` + `streak-3`, and showed 2 earned / 2 locked with “4 of 6” and “4 of 12” progress. New tests: `lib/__tests__/streak.test.ts` (28) + `components/planner/StreakBadges.test.tsx` (13). Gates: tsc/lint/1389 tests across 96 files/build green. | `PROJECT_SPEC.md` (FR-21, data model, AC-37..AC-41), `ARCHITECTURE.md` (§3.5 incl. the reserved `reward` rationale), `UI_UX_SPEC.md`, `06_PLANNER_SCREEN.md`, `CHANGELOG.md`, this file. |
| 2026-08-26 | **Audit: “derived state frozen at mount” — shared `useOverridableValue` hook, and a correction to the original diagnosis.** Swept every `useState` initializer in the codebase (17 sites) looking for state derived from persisted/hydrating data. **The premise the audit started from turned out to be false, and is corrected here.** Both earlier bugs — Reports→Recommendations (which card opens by default) and Debt payoff (the extra-payment field) — were attributed to “the zustand persist store had not hydrated at first render”. It had. `lib/storageAdapter.ts` is synchronous in BOTH environments (browser `localStorage`, Electron preload bridge → SQLite), so persist finishes hydrating inside `create()` at module load, before React renders. Proven twice: a unit test reading the store immediately after `createAppStore()`, and an in-browser measurement under Electron where the exact original lazy-initializer expression was shadowed alongside the fixed one and produced the SAME correct value. **The real bug class is narrower:** a default derived once from data that keeps changing (month switch, income arriving, a budget edited) — nothing to do with hydration timing. Chasing the hydration theory would have produced `ready`-flag gating that fixes nothing, and would have churned a dozen intentional form drafts. **Audit result: 17 lazy/eager initializers examined, 2 genuine instances — both already fixed before this pass — and 0 new ones.** Classified: 6 read persisted data through the SYNCHRONOUS storage seam and are correct (`useAppearance` ×2, `BackupsManager`, `DataBackupsPanel`, `useReducedMotion`, `Disclosure` — the last already using `useSyncExternalStore`, the right primitive); 8 are intentional form drafts seeded from a prop and MUST freeze (`TransactionForm`, `RecurrenceForm`, `FutureExpenseForm`, `IncomeSourceModal`, `CategoryModal` ×2, `BudgetForm`, `IconPicker`), restarted by keyed remount rather than state syncing; 1 is in `QuickAddExpense`, which is dead code (defined, never imported); `AllocationDrawer` was checked closely and is safe — its wrapper keys the body on `target.budgetId`, so every open remounts from live data. **Shared utility (one pattern, not two):** new `hooks/useOverridableValue.ts` — `[value, set, reset]`, tracking the derived value until the user overrides it. The override is BOXED internally so a falsy override (a cleared input, “nothing expanded”, 0) counts as a real choice; an unboxed `T | null` would snap those back to the derived value, which is the same bug in a new costume. Both existing fixes were migrated onto it so the codebase has ONE pattern instead of two hand-rolled copies, and the debt page’s “Use this month’s remaining” button now calls `reset()`. No new mechanism was added for `useOnboarding`/`useDisplayName` — the only genuinely deferred stores — because they already carry a `ready` flag and their consumers already subscribe reactively; that existing flag is documented as the convention instead. **No behaviour or default value changed** beyond the derivation timing itself. New tests: `hooks/__tests__/useOverridableValue.test.tsx` (6, including one that pins synchronous hydration so the convention fails loudly if persistence ever goes async) plus 3 regression tests on each fixed site. All 7 were verified to FAIL against the old lazy-initializer pattern before being kept — a regression test that passes both ways is worthless. Gates: tsc/lint/1348 tests across 94 files/build green. | `ARCHITECTURE.md` (new §3.3a, the required convention + the corrected history), `AGENTS.md`, this file. |
| 2026-08-25 | **Debt payoff planning (FR-20): deterministic avalanche vs snowball comparison.** New capability layered on top of categories, additive throughout — a category with no linked debt behaves exactly as before. **Opt-in:** a “Track as debt” switch on the category edit form (`CategoryModal`, available for ANY category, expense or income — gatekeeping by kind would be a guess about the user’s bookkeeping) reveals current balance, interest rate and minimum payment. **Storage:** a SEPARATE `Debt` record linked one-to-one by `categoryId`, deliberately NOT folded into `Budget.limit` — a balance, a rate and a minimum describe an obligation that outlives a month, whereas a limit is one month’s spending allowance, and a budget is recreated every month. The rate is stored as integer BASIS POINTS (`aprBps`, 1250 = 12.5%) for the same reason money is stored in minor units: no float drift in persisted data. 0% is explicitly valid and expected for informal/family loans. `startingBalance` is display-only (the “N% paid off” bar), never read by the engine, and `setCategoryDebt` only raises it so paying a balance down cannot rewrite where it started. **Screen:** new `/debt` route in the sidebar’s **Analytics** section — it answers “where is this heading” like Reports and Timeline, not “what am I doing with this month’s money” like Planner, and it spans many months so it does not belong to the month picker. **Engine:** `lib/debtPayoff.ts` is pure, UI-free and unit-tested in isolation — no React, no store, no I/O, **no AI/LLM call and no network access**, therefore no per-use cost and byte-identical output on every machine (asserted by test). Per simulated month: interest accrues, minimums are paid, then the remainder cascades down the payoff order so surplus rolls into the next debt in the same month. The monthly outlay is held constant at `sum(minimums) + extra`, which is what produces the standard rolling effect in BOTH strategies. Ordering is the only difference, and its tie-breaks are total so a schedule can never depend on input order. **0% debts** accrue zero interest, keep their balance-based position under snowball, and sort last under avalanche. **Fewer than 2 debts → no comparison:** with one debt the strategies are identical by definition, so the screen shows a single projection rather than two columns of the same numbers; with none, an empty state. **Ties are surfaced honestly:** the “Costs least” badge appears on neither strategy when they come out equal — which happens for real (equal rates collapse avalanche’s tie-break onto snowball’s key; and with exactly two debts and no extra, the only money that ever moves is a freed minimum, which has nowhere else to go). A stalled plan reads “Not on this budget” rather than being given an invented payoff date; `MAX_PAYOFF_MONTHS` (600) bounds the loop. **Active plan:** `settings.debtStrategy` is a display preference only — nothing is automated and no money moves. Schema **v7 → v8**: backfills `debts: []` and `debtStrategy: “avalanche”`, flagging NO category as debt. **Bug found in real Chromium and fixed:** the extra-payment input seeded via `useState(() => suggested)` captured 0, because the initializer runs on the hydration render before the persisted store reaches the component — so the “defaults to Remaining” requirement silently did nothing. Now derived (`extraDraft ?? suggestion`, `null` = untouched), which also honours the requirement that it stay user-editable rather than locked. **`BottomNav` fixed too:** it hard-coded `grid-cols-6`, so a seventh destination would have overflowed into a second row; the column count now derives from `NAV_ITEMS.length` and `NavItem.shortLabel` carries the ~50px mobile cell (“Debt”). New tests: `lib/__tests__/debtPayoff.test.ts` (30) + `components/debt/DebtPayoffView.test.tsx` (18) + `components/settings/DebtFields.test.tsx` (10), covering a single debt, differing rates, converging equal-rate cases, a 0% debt mixed in, an underpowered extra payment, cent-level accounting, determinism, migration, orphan/duplicate cleanup and the form. Gates: tsc/lint/1335 tests across 93 files/build green. | `PROJECT_SPEC.md` (FR-20, data model, AC-31..AC-36), `ARCHITECTURE.md` (§3.1 data model, §3.4 deterministic engine + AI-extension seam), **new `14_DEBT_PAYOFF_SCREEN.md`**, `CHANGELOG.md`, this file. |
| 2026-08-25 | **Rollover budgets (FR-19): unspent funds carry forward for categories that opt in.** Opt-in per category via a “Roll over unused funds” switch on the budget edit form (limit + priority); OFF for every existing and new category, never inferred, never bulk-applied, and there is no global setting. Stored as `Category.rollover?: boolean` on the CATEGORY rather than the per-month `Budget` — budgets are created fresh each month, so a per-budget flag would silently switch itself off every month and break the chain. **Carry rule:** underspent + enabled carries `effectiveLimit − spent` on top of next month’s base limit, measured against the *effective* limit — which is what makes it compound. **An overspent month carries nothing, and never a negative balance** (see the design note below). **Cap:** accumulated carryover is capped at `ROLLOVER_CAP_MULTIPLIER = 1` × the destination month’s base limit, so the effective limit plateaus at **2× base** rather than growing into a number that means nothing. A fixed constant for now rather than a user setting; each record persists the `cap` in force, so exposing or changing it later is easy and cannot rewrite settled months. **Timing:** no backend and no scheduler, so `hooks/useRollover.ts` runs at mount from `AppShell` (mirroring `useRecurring`) and detects the transition by noticing the current month has no carryover records yet. That same check is the idempotency guard, which is why no separate “last opened month” marker exists — a marker could drift out of sync with the records it describes, whereas the records cannot disagree with themselves. **Persistence:** new `AppState.rollovers: RolloverRecord[]`, one append-only record per (category, month) carrying `amount`, the uncapped `leftover`, the `cap` used and `computedAt`. Written once and never recalculated, so a past month keeps the limit it actually had. A record is written even when nothing carries (overspent, or opted out), which seals the transition so flipping the switch mid-month cannot retroactively grant funds for a month already under way — it takes effect at the next month end. Schema **v6 → v7**: the migration backfills `rollovers: []`, opts NOBODY in and invents NO history. **Display:** `RolloverBadge` shows “+X” in the category’s own `categoryDisplay()` chip colours with the full sum as its accessible label (“$1,000.00 + $600.00 rolled over = $1,600.00”), and the row prints the “base + carry” breakdown under the effective headline figure — never a silently larger number. Percentages, over-limit badges, budget health, insights, recommendations, funding needs, the utilization chart and the history budget context all now measure against the effective limit. **Backward compatibility:** `budgetProgress`, `effectiveLimit`, `overBudgetCategories`, `budgetHealth`, `budgetUtilizationSeries`, `fundingNeeds` and `insightsFor` all take `rollovers` as an OPTIONAL argument defaulting to none, so every pre-FR-19 call site is bit-for-bit unchanged and a category that never opts in behaves exactly as before (asserted by test). **Two design decisions flagged for revisit:** (1) an overspent month carries no debt forward — deliberate, so overspending cannot compound into a shrinking budget, but trivially invertible if the intended behaviour is the opposite; (2) the cap basis is the DESTINATION month’s base limit when a budget exists there, falling back to the closing month’s, since that is the number the row shows the carry added on top of. **Known limitation, deliberate and out of scope:** editing past transactions after a carryover has been applied does NOT reconcile it — the settled record stands, and no retroactive recalculation is attempted. New tests: `lib/__tests__/rollover.test.ts` (18) and `components/planner/BudgetRollover.test.tsx` (13), covering enabled+underspent, enabled+overspent, disabled, the cap, 3+ consecutive compounding months to the plateau, idempotency, the mid-month toggle, migration and orphan cleanup. Gates: tsc/lint/1277 tests across 90 files/build green. | `PROJECT_SPEC.md` (FR-19, data model, AC-26..AC-30), `ARCHITECTURE.md` (§3.1 state, §3.2a transition), `UI_UX_SPEC.md`, `06_PLANNER_SCREEN.md`, `CHANGELOG.md`, this file. |
| 2026-08-24 | **Category icon library expanded 100 → 153 selectable icons, regrouped 10 → 14 groups (purely additive).** Before: 100 emoji across 10 category groups + 14 vector "Line icons". After: **153 emoji across 14 groups**, same 14 vector icons untouched (167 options total). Format is unchanged and deliberately emoji — category icons print as raw text in Recharts axis labels and `<option>` elements, so the picker keeps passing `vectors={false}` for categories; no new rendering path was introduced. **Regrouped** to carry the larger set: the old catch-all "Shopping & food" and "Home & utilities" split into **Food & dining** (17), **Housing** (10), **Utilities** (7) and **Connectivity** (6); **Financial** (14), **Health** (11) and **Family & education** (9) grew from the old Finance/Health/Lifestyle buckets; and four new groups were added — **Work & business** (12), **Giving** (9, covering charity, gifts and tithe/zakat given this app's usage), **Travel** (7) and a broader **Other** (8). Transport reached 11, Lifestyle & shopping 25, Nature & outdoors 7. **Additive guarantee, verified mechanically:** the previous 100 emoji were captured before the change and the new set asserted to be a strict superset — zero previously-available icons dropped, zero duplicates — so no existing category's assigned icon can have become unpickable. Separately confirmed that all migration-assigned icons (📶 🧺 💸 📦 🌐), all 8 onboarding starter suggestions and all 11 seeded default icons are still present. No stored icon value, category assignment or the category-registry work was touched. **Near-duplicates avoided (req 4):** new additions cover distinct real-world concepts (dental, visa, waste, ATM, elderly care, school supplies, servicing, ferry, crypto) rather than more variants of concepts already covered; the pre-existing instrument cluster was kept because those icons may already be assigned, but was not extended. **Picker performance (req 5)** is now guarded by `IconPicker.perf.test.tsx` (5 cases): every group stays ≤30 options so no group becomes a wall, the set stays chunked across ≥10 groups, the picker opens and renders the full set within budget, search narrows to under a third of the set and surfaces the expected match, keyword-only search works ("tithe" → Church), and a no-match query shows nothing rather than a stale grid. **Tests updated (req 6):** `iconLibrary.test.ts`'s coverage assertions caught two real regressions from the regroup — a dropped "utilities" keyword and 🍎's label changing from "Apple" to "Fruit" — both fixed by restoring the keywords and updating the one `CategoryModal.test.tsx` reference. Gates: tsc/lint/1246 tests across 88 files/build green. | `05_COMPONENT_LIBRARY.md`, `CHANGELOG.md`, this file. |
| 2026-08-24 | **One category registry: name + icon + colour behind a single call, 19 components migrated.** New `lib/categoryRegistry.ts` — `categoryDisplay(category, fallbackName?)` / `findCategoryDisplay(categories, id)` returning `{ id, name, icon, color, chip, tint, strong, missing }`. It DERIVES from the stored record (no hardcoded table), so user-created categories from Settings or onboarding resolve exactly like seeded ones with no registration step. **Migrated (all 19):** `BudgetRow`, `BudgetList` (donut segments), `BudgetSuggestions`, `AllocationDrawer`, `RecentActivity`, `DeferredSection`, `SummaryCards`, `BudgetForm`, `QuickAddExpense`, `ImportStatementModal`, `CategoryAnalysisChart`, `TransactionCard`, `ExpenseDetailsView`, `TransactionFilters`, `TransactionForm`, `FutureExpenseForm`, `UpcomingView` (its `FutureExpenseRow` now takes one `display` prop instead of `categoryName`+`categoryIcon`+a locally-derived chip), `CategoryManager`, `LearnedRulesPanel`, `SettingsView`. Every dead `categoryAccent`/`categoryColor`/`budgetRowTreatment`/`categoryLabel` import was removed — those are registry internals now. **What the audit actually found, which differs from the brief:** (a) `TopCategoriesChart` no longer exists — it was deleted in the Reports consolidation, so it was not a bug site; (b) the icon was never per-component *logic* — every component read `category.icon` off the store, so "Edi shows a tree in four places" could only ever be one stored value, and the earlier fix was a v4→v5 data migration that corrected all four at once; what WAS fragmented were the icon FALLBACKS ("💸" / "•" / none) and especially the colour, which came from three parallel helpers any component could pick from; (c) the name had already been centralised earlier in `lib/categoryDisplay.ts` with a source-scanning guard. The registry unifies all three facets regardless, which is the durable fix. **Regression tests** (`lib/__tests__/categoryRegistry.test.tsx`, 7 cases): renders Edi across BudgetRow + RecentActivity + Category analysis asserting ONE icon and no stale tree glyph; renders a lowercase-stored "internet" across the same three asserting "Internet" everywhere and no lowercase leak; plus a structural guard scanning component sources for any file re-deriving an icon or colour. That guard immediately caught **14 sites the first migration pass missed** and one more after, which is precisely the recurrence it exists to prevent. No stored id, budget calculation or data model changed. Gates: tsc/lint/1241 tests across 87 files green. | `05_COMPONENT_LIBRARY.md`, `AGENTS.md`, `CHANGELOG.md`, this file. |
| 2026-08-21 | **First-run onboarding: 3 pitch screens + required guided setup, before the dashboard.** New `components/onboarding/` (`OnboardingFlow`, `SetupFrame`, `SetupIncomeStep`, `SetupBudgetsStep`), `lib/onboarding.ts` and `store/useOnboarding.ts`. **First-run detection reuses the existing field rather than adding one:** `settings.firstRunDone` already existed and was already validated, but was seeded `true` and read nowhere — a dead flag. `createInitialState()` now seeds it **false**, so new installs onboard and every existing stored state (which carries `true`) is untouched. No schema change, no version bump, and pre-existing accounts are NOT retroactively forced through the flow. **Step position is separate state:** `onboarding:step` through the storage seam, mirroring `lib/displayName.ts` — not part of AppState, so it needs no migration and an import/restore of the ledger leaves it alone. `loadOnboardingStep` falls back to the first screen for absent/corrupt/unknown values and maps the legacy `"setup"` placeholder forward to `setup-income`. **Layout:** `AppShell` returns the onboarding tree INSTEAD of the shell — no sidebar, header, bottom nav or name modal — keeping only `TitleBar` (window stays draggable) and `ToastHost`; the container is `fixed inset-0 pt-11` so nothing renders under the 44px custom title bar. `useOnboarding` starts `ready: false` on both server and client and is seeded in AppShell's mount effect, the same hydration gate `useDisplayName` uses, so onboarding never flashes for a returning user. **Pitch (3 screens):** app-specific copy about category limits, per-category spend visibility and month-over-month comparison — no Import Statement, tier or upgrade wording anywhere (asserted by test). Back appears only after screen 1, the last screen reads "Get started", and "Skip introduction" jumps to guided setup — which has no skip of its own. Step indicator is an `<ol>` of pills with `aria-current="step"`; `05_COMPONENT_LIBRARY.md` has no stepper primitive, so this is the smallest thing built from existing tokens and is noted for adoption if one is ever added. **Guided setup (required, 2 steps):** income (≥1 source above zero) then budgets (≥2 funded), each blocking its button with an inline `role="alert"` line rather than a dialog. **Both reuse the app's real write paths** — income through `setIncomePlan`, the same action the Planner's Add Income modal calls (written immediately, so it is ordinary income data at once); budgets through `addCategory`/`addBudget` on Finish, the same actions the Settings and New-budget forms use, reusing a matching seeded category instead of duplicating the name. Suggested starter chips come from `SUGGESTED_BUDGETS`, every icon asserted to exist in `ICON_GROUPS` — the same rule the icon migrations follow. Custom categories use the shared `IconPicker` with `vectors={false}`. A running "{allocated} of {income} allocated" line is shown for context and deliberately not enforced. **Note on req 7's premise:** there is no "Archer" placeholder or demo data in the app — "Archer" appears only in `06_PLANNER_SCREEN.md`'s example copy and the reference mockup; the hero already renders the real display name. What a new install DOES get is 11 seeded categories from `DEFAULT_CATEGORIES`, which guided setup reuses rather than duplicating. Tests: 17 model/persistence cases (`lib/__tests__/onboarding.test.ts`) + 14 flow cases (`OnboardingFlow.test.tsx`) covering advance/back/skip, indicator, persistence and resume, both validation blocks, the real-action writes, multi-source income, custom category, running total and completion; one existing `validate.test.ts` assertion updated for the flipped seed. Verified on a genuinely fresh Electron profile: no sidebar or nav, title bar intact, pitch screen 1 first, and no skip control on guided setup. Suite 1203 → 1234. Gates: tsc/lint/1234 tests across 86 files/build green. | `13_ONBOARDING_FLOW.md` (new), `CHANGELOG.md`, this file. |
| 2026-08-21 | **Three independent Reports fixes.** (1) **Category capitalization root-caused, not patched per component.** `CategoryAnalysisChart` printed `row.category!.name` straight from the store (chart datum + list row) — missed by the earlier Planner-wide sweep, which is exactly the drift the brief warned about. Swept every remaining site and routed 20 more through the shared `categoryLabel`/`categoryLabelOr`: CategoryAnalysisChart, SummaryCards (drawer target + picker row), ImportStatementModal, CategoryManager (list + 2 aria-labels), LearnedRulesPanel, SettingsView (4), RecurrenceForm, TransactionFilters, TransactionForm, FutureExpenseForm, UpcomingView. Deliberately NOT routed: `localeCompare` sort keys, `categories.find` lookups, and `CategoryModal`'s form draft (that IS the stored value being edited). **To stop a third recurrence**, new `components/reports/categoryNaming.test.ts` scans component sources and fails on any `category.name` rendered without the formatter, excluding those legitimate shapes by rule rather than per-file allowlist; verified it actually catches the bug by reintroducing it and watching it fail. **Out of scope, flagged:** IncomePlan (income source) names are a separate user-typed field and are still printed raw. (2) **Duplicate section/card titles.** Only "Cash flow" was still duplicated — the "Category analysis" pair had already been resolved by the earlier consolidation, which removed that standalone section header. Checked the established pattern first: single-card sections name the concept in the header and what is plotted in the card ("Savings trend"→"Savings over time", "Spending trend"→"Monthly spending trend"), so the card was RENAMED rather than untitled: "Cash flow" → **"Money in vs out"** (subtitle "Monthly income, expenses and what's left"). (3) **Recommendations default expansion.** Was `items[0]?.key` — pure list position, so whichever budget iterated first opened. Replaced with an explicit rule: **open the highest-severity recommendation, ties broken by the largest money at stake; only warnings ever auto-open, so a clean month starts fully collapsed.** Over-budget cards now carry `impact = spent − limit`. **Found and fixed a latent bug while verifying:** the value was a lazy `useState` initializer, which runs ONCE — when the store hydrates after first render (browser mode reads localStorage asynchronously) `items` was still empty and the default never applied at all; the live probe showed every card collapsed. The open card is now DERIVED from current items, with a user click overriding and sticking. Tests: 4 new expansion cases (largest overage opens regardless of list order both ways round, nothing opens without a warning, and the rule still applies when data arrives after first render) plus the naming guard; one earlier assertion updated because it asserted the very "Cash flow" duplication this pass removed. Verified live in the running app: no lowercase "internet" anywhere, no duplicate h2 headings, and "Edi" (the larger overage) expanded while "Essentials" stays collapsed. Suite 1198 → 1203. Gates: tsc/lint/1203 tests across 84 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `07_REPORTS_SCREEN.md`, this file. |
| 2026-08-21 | **Reports: Budget utilization hidden until 2+ months of history (gating only — the chart's rendering above the threshold is untouched):** with one month of data it collapsed to a single solid bar spanning the card, reading as a stuck/broken chart rather than a trend. It is now gated on `historyDepth >= MIN_TREND_MONTHS` (2) and omitted from the layout entirely below that — no placeholder, no empty state, no reserved space. **Threshold reuse (req 1):** `historyDepth` is the SAME `monthsWithTransactions(transactions, months)` value (`lib/reportTrends.ts`) that already drove the "one month so far" banner; no second check was written. The literal 2 is now a named `MIN_TREND_MONTHS` used by both the banner and the gate, so they cannot drift. The banner itself is untouched (req 5) and remains the explicit signal for why the chart is missing. **Audit of the other months-based charts (req 4) — Budget utilization was the ONLY offender.** The discriminator is whether a series zero-fills the window or drops empty months: `budgetUtilizationSeries` uses `months.flatMap(...)` and returns `[]` for months with no budgets, so one month yields ONE data point; `monthlySeries` (Monthly spending trend), `financeSeries` (Savings over time, Income vs expenses, Cash flow) and `incomeTrendSeries` (the merged income card's "Over time" view) all use `months.map(...)` and emit a point per month with zeros where empty — a flat line/bars at zero that reads correctly as "nothing yet" and keeps the axis for scale. Those five are deliberately NOT gated, matching the brief's own criterion. Note the user's list named "Income trend" as a separate chart; it no longer exists standalone — it is the "Over time" view of the merged Income card from the previous pass, and it zero-fills, so it needs no gating either. **Known edge case, documented:** the gate counts months with TRANSACTIONS while the chart drops months without BUDGETS, so two months of transactions with budgets in only one would pass the gate and still render a single bar. Left as-is deliberately — req 1 asked for the shared threshold over a second divergent check — and recorded in `07_REPORTS_SCREEN.md`. Tests: 4 new cases in `ReportsView.test.tsx` (omitted at one month, banner still shown while omitted, rendered at two months with the banner gone, and the five zero-filling charts still mounted at one month). Because these charts are dynamically imported and mount as `ChartSkeleton` under jsdom, the tests observe skeleton counts within each section rather than chart titles. Suite 1194 → 1198. Gates: tsc/lint/1198 tests across 82 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `07_REPORTS_SCREEN.md`, this file. |
| 2026-08-21 | **Reports: "Expected vs actual" and "Income trend" merged into one card with a view toggle (UI consolidation — income data, source categorization and calculations untouched):** the two cards compared the SAME two numbers (planned income vs what landed) on different axes — one per-source for the month, one as a six-month trend — and were structurally near-identical (same legend markup, same colour roles, same axis/tooltip styling, same 240px height). New `components/reports/IncomeComparisonChart.tsx` renders both from one card titled "Income: expected vs received", with a two-option toggle. **Toggle copy/pattern (req 1):** checked for an existing convention first — the app has no established view-toggle *copy*, but it does have a consistent *control*: pill buttons with `aria-pressed` and a brand tint on the active one (Settings section nav, TodoView/UpcomingView filters, the Compare-month button on this very page). The merged card reuses that control with "By source" / "Over time". **Default is By source (req 4):** the page opens on the current month, so a six-month trend built from one month of data is a flat line for five of them — the same failure mode Budget utilization has. **Legend (req 5)** is rendered ONCE above the chart and is identical either side of the toggle (Expected on the border token, Received on the income colour); each view keeps its own empty state, and the toggle stays reachable in both so the other view is always one click away. **Position (req 6):** the merged card takes the grid slot "Expected vs actual" held, beside Income sources; the full-width "Income trend" row below is simply gone and Budget utilization moves up — no gap worth filling, so nothing was moved to compensate. **Req 7:** "Income sources" (composition, not comparison) is untouched. `ChartCard` gained an additive optional `action?: ReactNode` header slot (matching `Card`'s existing `action` prop) to host the toggle; with `action` undefined its DOM/behaviour is unchanged for the other eight charts. `ExpectedVsActualChart.tsx` and `IncomeTrendChart.tsx` deleted — ReportsView and one `ReportsVisual.test.tsx` spec entry were their only consumers. **Tests (req 8):** coverage was moved, not dropped — new `IncomeComparisonChart.test.tsx` (9 cases: single card + two-option toggle, By-source default and its subtitle, per-source aria description with real figures, six-month switch with every month label present, both empty states plus switching back out of one, and a legend-parity check asserting the swatch treatment is byte-identical across the toggle); `ReportsVisual.test.tsx`'s two old specs collapse into one for the merged card. **Verified in the running app:** "Expected vs actual" and "Income trend" now appear 0 times, the merged card once, "Income sources" once, and both toggle states screenshotted. Suite 1188 → 1194. Gates: tsc/lint/1194 tests across 82 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `07_REPORTS_SCREEN.md`, `UI_UX_SPEC.md`, this file. |
| 2026-08-21 | **Reports de-duplicated: five category breakdowns collapsed to two (which sections exist only — no calculation, colour assignment or donut/list rendering changed):** the page stated "which category spent how much" five times. Survivors are **Category analysis** (donut + ranked numeric list) and **Recommendations** (untouched — its cards are actionable). **Removed:** (1) the "Spending by category" `ChartCard` and `ExpenseBreakdown` component — checked for sharing first (req 2) and found ReportsView was its ONLY consumer since the Planner stopped rendering it in an earlier pass, so the component and its 8-describe test file were deleted outright; (2) the "Top categories" chart and `TopCategoriesChart` component — used only by ReportsView's dynamic import plus one entry in `ReportsVisual.test.tsx`, so likewise deleted and that spec entry removed; (3) the Financial Insights "biggest cost" chip, which restated the headline card immediately above it verbatim — the headline card and the two over-budget chips are untouched, since the headline summarises those only as a count. **Placement (req 5):** Category analysis moved up out of its own mid-page section into the Spending breakdown section, taking the slot the removed card vacated beside Income vs expenses; the standalone "Category analysis" `SectionHeading` is gone (the card keeps its own title). It is FULL WIDTH there, not half — the first attempt put it in the `lg:grid-cols-2` grid and the screenshot showed its donut centre amount overlapping the ring at half width, which would have meant changing the donut rendering the brief forbids. **Correction to the brief's premise (req 1):** Category analysis is NOT uncapped — it slices to `TOP_COUNT = 6`, just as the removed card capped at 5. It still survives, but because it is the only one carrying both a chart AND a numeric breakdown, not because it shows everything; lifting the cap would be a rendering change and is left for a separate call. **Verified in the running app:** "Spending by category" and "Top categories" now appear 0 times, "Category analysis" once (inside Spending breakdown), Recommendations once, and "is your biggest cost this month" exactly once (the headline). Tests: `ReportsView.test.tsx`'s section-order list drops "Category analysis" and its "shows the Spending by category card" case is rewritten as "keeps exactly one category breakdown, inside Spending breakdown"; `ReportsVisual.test.tsx` loses the Top categories spec; `ExpenseBreakdown.test.tsx` deleted with its component. Suite 1200 → 1188. Gates: tsc/lint/1188 tests across 81 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `07_REPORTS_SCREEN.md`, `UI_UX_SPEC.md`, `PROJECT_SPEC.md`, this file. |
| 2026-08-21 | **Planner hero condensed and matched to the redesign reference, verified in DARK mode (layout/spacing/icon treatment only — no copy, actions, greeting logic or data touched):** **Spacing.** Padding is now `px-6 py-5 sm:px-7 lg:px-8` (vertical 20px at every width, horizontal 24→28→32px — the reference's squarer 22/26px ratio, replacing `lg:px-10 lg:py-5`). The single `gap-3` between blocks is replaced by the reference's own per-block rhythm — date → greeting 6px → sentence 10px → stat row 14px → progress 14px (label→bar 6px) → actions 16px. The progress block and the buttons, previously side-by-side in a `flex-wrap gap-x-8` row, now stack as the reference does, and the bar spans the content width instead of being capped at 420px. Measured result: **254px card height vs the reference's 294px** in the same viewport — tighter even though this card carries one extra line. **Stat-row icons (req 2).** `CalendarIcon` before "N days remaining" and `TrendingUpIcon` before the daily-available line, both `h-3.5 w-3.5` (14px) in `text-brand-500`; they replace the old teal dot + "•" separator, which also removed a hardcoded `bg-[#0EA5A4]` so the accent now follows the theme token in both modes (req 6). **Illustration (req 3).** The existing artwork is KEPT, not replaced — only its box: an `inset-y-0 right-0 w-[38%] max-w-[340px] overflow-hidden` layer at `opacity-70`, with the concentric discs and radial glow dropped to 0.02–0.04 alpha so the two thin line strokes are what read, matching the reference's restraint rather than the previous 460×276 spread. **Typography (req 4).** Mapped to app tokens, not the reference's px: date `text-caption font-semibold` (12/600), greeting `text-kpi-tertiary font-bold` (20px — an exact token match for the reference's 20px), sentence and stat row `text-description font-medium` (14/500, dropping the previous semibold on days-remaining so the row reads as one weight), bar labels `text-caption font-medium`. **Progress row (req 5).** The label row is now `justify-between` — percentage left, received / expected right (restoring the "received X of Y" figure the UI_UX_SPEC already documented) — sitting 6px above a `thin` (6px) fully-rounded `ProgressBar`; the reference is 7px, and the shared bar was left alone rather than special-casing it. **Verification (req 6).** Screenshotted the card in Electron in dark mode and side-by-side against a standalone render of the reference's hero at the same width; also re-shot in light mode to confirm nothing regressed — every value added goes through theme tokens. **Known remaining gaps, both deliberate:** the reference folds the remaining amount INTO the greeting headline and has no separate projected-balance sentence, and it renders the illustration inside a distinct 40%-wide gradient panel; changing either would alter greeting copy/structure, which req 7 forbids. Gates: tsc/lint/1200 tests across 82 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `06_PLANNER_SCREEN.md`, `04_LAYOUT_AND_SPACING.md`, this file. |
| 2026-08-21 | **Category icon audit #2 + icon library expanded to 114 options (icon selection only — no storage field, colours, tags or budget math touched):** **Part 2 first,** since Part 1 picks from it. The app already had a grouped, labelled, keyword-searchable registry (`components/settings/iconLibrary.ts`, 83 emoji across 9 groups + 16 vector line icons) and `CategoryModal` already wired `IconPicker` with `vectors={false}`, a selected state, search, favourites and recents — so reqs 7 and 8 were already satisfied and needed no change. Measured the brief's minimum list against it and found FIVE genuine gaps: internet/connectivity, insurance, charity/donations, banking transfers, and a miscellaneous catch-all — plus 📶 and 🧺, the two icons the v4→v5 audit had ASSIGNED but never added, leaving Edi and Essentials carrying values the picker could not show as selected. Added 14 options: Finance gains 💸 Repayment, 🧾 Bill, 🔁 Transfer, 🛡️ Insurance, 🤝 Charity; Shopping & food gains 🧺 Basket, 🍽️ Dining out; Tech & office gains 🌐 Internet, 📶 Signal, 📡 Satellite; Entertainment gains 📺 TV; Lifestyle gains 🎉 Celebration, 🎂 Birthday; and a new **Other** group (📦 Miscellaneous, 🗂️ Folder, 🔖 Label, ⭐ Star). Existing icons were reused rather than duplicated wherever they already fit (groceries, rent, utilities, transport, savings, health, education, gym, shopping, travel, gifts, pets, kids, personal care all already had coverage). **Req 5 vs req 6 conflict resolved in favour of 6:** all 13 category icons are emoji, so new ones are emoji too. The library DOES mix emoji and SVG — flagged, not silently converted — but that mix is deliberate and load-bearing: a category's icon is printed as raw text in Recharts axis labels and native `<option>` elements, where a vector key would render as the literal string "wallet", which is exactly why category pickers pass `vectors={false}`. **Part 1 audit** of all 13 categories found 3 genuine mismatches, fixed via a v5→v6 migration (`migrateV5`): "Loan" 💰→💸 (a piggy bank reads as savings, and was the same glyph as Salary), "Misc" 🛒→📦 (a cart reads as groceries), "internet" 💡→🌐 (a bulb reads as electricity). The other 10 were judged correct and deliberately left alone. Both icon migrations now share a new `applyIconFixes` helper and the same defensive matching (lowercase name AND the specific wrong icon), so a user who has since re-iconed keeps their choice. Schema bumped everywhere (`types.ts`, `storage.ts`, `seed.ts`, `validateAppState` accepts 1–6, returns 6). **Req 9:** no test asserted a fixed icon count, so nothing broke; added `iconLibrary.test.ts` (33 cases) locking uniqueness, label+keyword presence, vector resolvability, the emoji-only rule for category options, searchable coverage of all 27 required category types, and — the regression that bit last time — that every icon a migration assigns exists in the library. Plus 2 migration cases (three icons corrected; Salary's piggy bank, a correct groceries cart and an already-re-iconed Loan all left alone). Gates: tsc/lint/1200 tests across 82 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `AGENTS.md`, `ARCHITECTURE.md`, `05_COMPONENT_LIBRARY.md`, `UI_UX_SPEC.md`, this file. |
| 2026-08-21 | **Typography audit: the app was ALREADY on Inter — the real gaps were the splash screen and numeric alignment (no typeface swap needed):** req 1's audit found `app/layout.tsx` already loads Inter via `next/font/google` (`variable: "--font-inter"`, self-hosted woff2 — no runtime Google request), `app/globals.css` `@theme` already sets `--font-sans: var(--font-inter), system-ui, "Segoe UI", Roboto, Arial, sans-serif`, and `body` already consumes it. Verified in real Chromium rather than by reading CSS: computed `font-family` is `Inter` on body, sidebar labels, sidebar nav, page headings, card titles, buttons and the hero greeting; `document.fonts.check("16px Inter")` true. So reqs 2 and 3 needed no code change. **Req 8 already satisfied too:** the emitted `@font-face` carries `font-display: swap`, the latin subset is `<link rel="preload" as="font">`, and Next emits a metric-matched `Inter Fallback` (`size-adjust: 107.12%` + ascent/descent/line-gap overrides) so the swap causes no layout shift — nothing to add. **Req 5 deliberately not "fixed":** Inter is loaded as a VARIABLE font (`font-weight: 100 900`, one ~48KB file per unicode-range) covering the 400/500/600/700 the UI actually uses (measured: 156 `font-semibold`, 111 `font-medium`, 66 `font-bold`, rest default 400); splitting into four static instances would mean more files and more bytes, so restricting weights would make it worse, not leaner. **Gap 1 — the Electron splash** (`electron/splash.cjs`) rendered in `"Segoe UI", system-ui`, the only surface in the app not on Inter. It is a `data:` URL with an opaque origin so it cannot reference the bundled font by path; new `interFontFace()` inlines the same next/font latin woff2 as a base64 `data:` URI (`font-display: block`, ~63KB, read at splash time from `out/_next/static/media/*-s.p.*.woff2` so nothing is committed), returning `""` and falling back to the system stack if the file is missing — the splash must never fail to open over a font. **Gap 2 — tabular figures (req 4).** A live audit of 35 rendered currency elements found **11 with `font-variant-numeric: normal`**, including the entire "spent · left" column across every budget row. `tabular-nums` is now baked into the number primitives (`AnimatedNumber`, `AnimatedMoney`, and `MetricCard`'s value + support slots) rather than left to call sites, plus the BudgetRow spent/left column and the hero's "₦X/day available" line. Re-audited: 11 → 1, and the one remaining is the status-band prose sentence ("2 budgets over their limit — Edi +₦800.00, …"), left proportional on purpose since running text has no column to align to. **Req 6 honoured:** no size, line-height or letter-spacing token touched; nothing looked visually off after the change (the typeface did not change, only figure spacing). Docs: `00_DESIGN_TOKENS.md` gained the missing FONT FAMILY token section (family, `--font-sans`, loading mechanism, weights rationale, numerals); `UI_UX_SPEC.md` had stale `--font-geist-sans`/`--font-geist-mono` names corrected; the ROADMAP known-gaps line claiming "Geist fonts fetched at build time" corrected; `03_TYPOGRAPHY.md` already said Inter and `01_UI_DESIGN_SYSTEM.md` names no typeface, so both were left alone. New `AnimatedNumber.test.tsx` (4 cases: tabular with and without a caller className, no stray leading space). Gates: tsc/lint/1165 tests across 81 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `AGENTS.md`, `00_DESIGN_TOKENS.md`, `UI_UX_SPEC.md`, this file. |
| 2026-08-21 | **Budgets donut restyled to the reference: flush conic-gradient wedges, no gaps, no rounded caps (rendering only — proportions, colors, order and all math untouched):** `DonutChart` was hand-rolled SVG (no chart library, so req 6's "check the library's own options first" had nothing to check) drawing one stroked arc per category with `strokeLinecap="round"` and a `segmentGapDegrees` prop that deliberately shortened each sweep to leave a ~5° background gap. It now paints ONE `conic-gradient` where each category contributes a stop pair whose start is the exact percentage the previous stop ended at — hard edges, zero gap, nothing stroked so there are no caps to round. The obsolete `segmentGapDegrees` prop is removed (its only caller passed 5). **Ring proportions:** hole is now 70% of the outer diameter (`innerRatio`, was effectively 80%), a 27px-thick ring at the Planner's 180px donut vs the 18px stroke it replaced; punched with a hard-stop radial `mask` rather than an opaque disc, so it is transparent on any card colour in either theme. **Hover preserved:** one invisible annulus-sector path per category overlays the ring as a hit area (`fill: transparent`, colour exposed as `data-color`), and the active wedge takes a faint white overlay, so hovering a budget row or legend row still highlights its segment. **Bug found while doing this:** a single category covering the whole ring produced a one-stop gradient, which is invalid CSS — browsers drop it and the ring painted blank; it now emits the colour twice. Also rounded the mask radius so float noise (0.7 × 180 ÷ 2 = 62.99999…) never reaches the CSS. **Verified in the running app** by screenshotting the donut card: continuous flush ring, centre text clear of the ring, and the top-5 legend swatches matching the wedge colours exactly. **NOTE — deviation from the brief:** req 3 asked for a 40–45% inner cutout, but the reference HTML itself renders 76% (`.donut` 170px with `::before{inset:20px}`), and at 44% the "of ₦X · N%" line overflows onto the ring, breaking req 4's "comfortably fit the centered text". 70% is the compromise — clearly thicker than before and than the reference, with the text still clear. It is one constant (`DEFAULT_INNER_RATIO`). `DonutChart.test.tsx` rewritten (11 cases: single gradient not stroked arcs, one stop per category sized to its share, zero gap at every boundary, hard-edged mask + custom `innerRatio`, hit area per category with repeated colours, active-only highlight, zero-value categories dropped, all-zero track, single-category full ring, aria-label); `BudgetList.donut.test.tsx` re-pointed at the gradient/`data-color` while keeping its distinct-reserve-hue intent. Gates: tsc/lint/1161 tests across 80 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `06_PLANNER_SCREEN.md`, this file. |
| 2026-08-21 | **Bugfix — the three budget-row action icons were invisible; TWO independent defects, both found by rendering the app in real Chromium rather than reading the code:** (1) **Zero-width glyphs.** The buttons used the shared `Button` at `size="sm"`, whose preset hard-codes `px-3`; the `px-0` passed via `className` does NOT override it (equal specificity — compiled CSS order decides), so a `w-6` (24px) button had 24px of padding and a **0px content box**, and the 12px SVG, being a flex item without `shrink-0`, collapsed to zero width. Measured in Chromium as `btn=24x32 svg=0x12`. `min-h-8` from the same preset also beat `h-6`, which is why the buttons were 32px tall rather than the intended 24. (2) **Hidden at rest.** The group carried `opacity-0 group-hover:opacity-100 max-sm:opacity-100` — note the UNPREFIXED `opacity-0`, unlike every other row-action group in the app which uses `sm:opacity-0 sm:group-hover:…`. At desktop widths the icons were transparent until hovered, which is what made "the faintest trace of one icon on a single row" (the row under the pointer, mid-150ms transition). **Fix:** the three actions are now plain `<button>`s (the pattern `RecentActivity`/`TransactionCard` already use) with an explicit `h-8 w-8` square, `h-4 w-4 shrink-0` glyphs, `text-muted` at rest and hover/focus emphasis instead of hover reveal — always visible at every width. **Item 1 of the brief (icon color) was NOT the cause and needed no change:** `text-muted` measures 3.90:1 on the over-limit red tint and 4.29–6.22:1 on every other row tint in light theme (4.96–6.22:1 in dark), all clearing the 3:1 WCAG 1.4.11 threshold. **Item 2 (clipping) was also not the cause:** no `overflow-hidden` ancestor clips the row, and the group is `shrink-0` beside a `min-w-0 flex-1` column, so the over-badge and tag pills truncate the name rather than squeezing the icons out. **Verification (item 4):** an Electron harness seeded browser-mode state, scrolled the Budgets section into view and screenshotted it — all three icons render clearly at rest on on-track rows (Transport, Internet, Misc, Loan, Palmpay), a long-name row ("Subscriptions and streaming services") and both over-limit red-tinted rows (Edi, Essentials); a 6× crop confirms the pencil, exchange and trash glyphs are distinct. Click handlers, aria-labels and the AllocationDrawer/edit-form/delete-confirm logic are untouched. 3 regression cases added (no `opacity-0` at rest, explicit square + `shrink-0` glyph + no `px-*` preset, still visible on an over-limit row); the two stale `h-6 w-6` assertions updated. Gates: tsc/lint/1155 tests across 80 files/build green; `npm run dist` re-run. | `CHANGELOG.md`, `AGENTS.md`, `UI_UX_SPEC.md`, `06_PLANNER_SCREEN.md`, this file. |
| 2026-08-21 | **Four independent Planner polish fixes.** (1) **Category name capitalization (display only):** a category stored lowercase ("internet") rendered lowercase everywhere. New `lib/categoryDisplay.ts` (`categoryLabel` / `categoryLabelOr`) capitalizes ONLY the first character and is applied at every site that prints a category name — BudgetRow, BudgetList donut legend + drawer target, BudgetStatusBand flags, BudgetSuggestions, AllocationDrawer source rows, RecentActivity, DeferredSection, ExpenseBreakdown, TransactionCard, ExpenseDetailsView, HistoryView, ReportsView, TopCategoriesChart, FinancialInsights, Recommendations, and the BudgetForm/QuickAddExpense selects. The stored value is deliberately NOT rewritten: it is the lookup key for `categoryAccent`/`budgetRowTreatment` (both lowercase internally) and is still used raw for lookups and sorts. Multi-word and intentionally-cased names ("PalmPay") are unaffected. (2) **Two mismatched category icons corrected via a v4→v5 migration:** "Edi" (data/airtime top-ups) carried a plant (🌲) → 📶, "Essentials" carried a store glyph (🏪) → 🧺 — the basket rather than the cart, because "Misc" already owns 🛒 and was explicitly out of scope. `migrateV4` in `lib/validate.ts` matches on lowercase name AND the specific wrong icon, so a user who already re-iconed either keeps their choice, and both stay freely editable after the one-time fix — unlike a display override, which would have trapped them. Schema bumped everywhere (`lib/types.ts`, `lib/storage.ts` `CURRENT_STORAGE_VERSION`, `lib/seed.ts`, `validateAppState` now accepts 1–5 and returns 5). Transport, Loan, Misc, Internet and Palmpay untouched. (3) **Hero card height:** the decorative SVG is absolutely positioned, so the card's height came entirely from the left column's padding — `py-6 lg:py-8` → `py-4 lg:py-5` (horizontal padding, all content and the illustration unchanged), lifting the metric cards up the page. (4) **Redundant CTAs verified, no change:** the hero's "Review Budget" and the status band's "Review budgets" already resolve to the identical destination — both go through `REVIEW_BUDGETS_HREF` + `scrollToBudgetAllocation` in `components/planner/reviewBudgets.ts` — and a note recording the check was added there. Tests: new `lib/__tests__/categoryDisplay.test.ts` (7 cases incl. intentional casing and emoji-leading names) plus 2 migration cases (icons corrected; a user-changed icon and an unrelated gift-iconed category both left alone); the 6 existing `version).toBe(4)` assertions and the unsupported-version case moved to 5/6. Gates: tsc/lint/1152 tests across 80 files/build green; `npm run dist` re-run so the packaged app carries all of it. | `CHANGELOG.md`, `AGENTS.md`, `ARCHITECTURE.md`, this file. |
| 2026-08-21 | **Regression fix — the budget row's pencil no longer hijacks the edit form; edit and allocate are now two distinct actions:** repurposing the pencil to open `AllocationDrawer` left NO way to edit an on-track category's limit or priority from the list (only over-limit categories could reach the edit form, via `BudgetSuggestions`' "Adjust"). `BudgetRow` now takes BOTH `onEdit` and `onAllocate`: the pencil is restored to the original budget edit form (limit + priority) on EVERY row regardless of over/at/under limit, and a new `ArrowsExchangeIcon` (two opposing arrows, added to `components/ui/icons.tsx` — deliberately not a pencil glyph) opens the allocation drawer beside it. Order at the end of the row is pencil → exchange → trash, all `h-6 w-6` ghost buttons at the same touch target (nothing shrunk to fit), revealed together on hover AND keyboard focus (`group-focus-within`). Accessible names are distinct: "Edit {Category} budget" vs "Allocate funds to {Category}" (was the single ambiguous "Allocate budget for {Category}"). `AllocationDrawer` itself, its triggering contract (`AllocationTarget`) and `AllocationDrawer.test.tsx` are untouched; `BudgetSuggestions`' over-limit shortcut is unchanged and remains additive. Only one existing selector broke — `BudgetList.merged.test.tsx`'s drawer case queried the old pencil label — and it was updated rather than deleted, then extended: 5 cases now cover both actions existing and being visually/semantically distinct, keyboard reachability (tab from edit lands on allocate), the pencil opening the limit/priority form for an ON-TRACK category (the regression this fixes) and for an over-limit one, and the drawer mounting only from the exchange action. Gates: tsc/lint/1142 tests across 78 files/build green | `CHANGELOG.md`, `PROJECT_SPEC.md`, `ARCHITECTURE.md`, `UI_UX_SPEC.md`, `06_PLANNER_SCREEN.md`, this file. |
| 2026-08-21 | **"Allocate remaining" retired as a permanent section — allocation is now an on-demand right-edge drawer (trigger + UI only; the persistence path is unchanged):** `AllocationPanel` rendered one slider per budget on every Planner load, including for categories with nothing left to allocate, and it took permanent vertical space. It is deleted (component + test); `BudgetList` no longer mounts it, and the dead `allocation-slider-{id}` focus lookup in the `planner:focus-budget` handler is gone (the handler still scrolls + highlights the row). The Planner now renders ZERO sliders at rest. The rebuilt **`AllocationDrawer`** (~400px, full height, dimmed scrim, slides from the right) is per-category and reusable: header "Add funds to {Category}" + close X; body in order — the over-budget hint ("{Category} is {overage} over its {limit} limit this month. Move money from a category with room to spare, or raise the limit.", values from `budgetProgress`, never hardcoded) or the simpler "Choose how much to allocate to {Category} from this month's unallocated funds."; a "Move funds from" section with one row per OTHER month budget whose `available = limit − spent > 0` (chip, name, "$X available", slider `0 … available` starting at 0), omitted ENTIRELY — no placeholder — when nothing qualifies; and a dashed-border "Or raise the limit" row sliding from the current limit to `limit + overage` (or `limit + max(unallocated, 1 unit)` when not over). Sliders are independent; when the target is over budget the SUM of moves is clamped to the overage via `clampAllocation(next, overage, otherTotal)` so they can bring it exactly to its limit and no further, and each move is additionally clamped to its own source's available, so no source can go negative. Footer "Cancel"/"Apply" (Apply disabled until dirty); Escape and scrim click behave as Cancel — the body is keyed on the target budget id, so every open remounts from live store data and discarding is automatic. Apply dispatches one `updateBudget(source, { limit: limit − moved })` per contributing source plus `updateBudget(target, { limit: newLimit + totalMoved })` — the same single persistence path — then closes; rows re-render from the store with no reload. **Second entry point:** the "Remaining" summary card's existing "Allocate remaining" drawer no longer embeds the panel — it now lists the month's budgets (name + "$X over" in danger or "$X left" in muted, most-over first) and hands the chosen one to the SAME shared drawer. New `AllocationDrawer.test.tsx` (19 cases: shell/title/close, both hints with real values, source filtering incl. the target and zero-available categories, omitted section, zero defaults, overage clamping across two sources, per-source available clamp, limit-slider range over/not-over, apply via moves and via limit, Apply disabled until dirty, Cancel/Escape/scrim all discarding, and reuse across two different targets) plus 2 SummaryCards cases for the picker→drawer flow. NOTE: the row pencil no longer opens the budget edit form, so editing an on-track budget's PRIORITY is currently only reachable via BudgetSuggestions' over-limit rows — worth a follow-up. (RESOLVED same day — see the row-actions entry above: the pencil is back on the edit form and a separate exchange icon opens the drawer.) Gates: tsc/lint/1138 tests across 78 files/build green | `CHANGELOG.md`, `PROJECT_SPEC.md` (FR-11), `ARCHITECTURE.md`, `UI_UX_SPEC.md`, `06_PLANNER_SCREEN.md`, this file. |
| 2026-08-21 | **Planner category lists de-duplicated into ONE "Budgets" section (layout only — no data model, no spent/left/percent math, no donut color assignment changed):** the Planner listed the same categories twice — "Budget Allocation" (donut + rows) high on the page and "Expense breakdown" (a second bar list) down near Recent Activity. The standalone Expense breakdown section is removed from `PlannerView`; `ExpenseBreakdown.tsx` itself is KEPT because ReportsView renders it as the "Spending by category" panel (`bare limit={5}`) — deleting it would have taken a Reports feature with it. The surviving section is retitled **"Budgets"** with a live muted count ("· N categories", singular at 1, "· 0 categories" at zero) and keeps its `#budget-allocation` id so every focus/scroll flow (`focus=over`, `focus=create`, hero + status-band "Review budgets") is untouched. Layout is now flex rather than a 5-col grid: donut card fixed at `lg:w-[300px]`, list card `flex-1`. Donut centre reads `Budgeted / {committed} / of {allocatable} · {pct}%` (one line instead of the separate "N% allocated" pill); a **top-5 legend** sits under it — swatch (the color the donut actually assigned, reserve hues included), name, share of total budgeted — deliberately capped at 5 since the full list is the right-hand column, and hovering a legend row highlights its segment. **Rows** gained: an over-limit badge ("{pct}% · {overage} over", right-aligned on the name line, rendered ONLY above 100%); an **overflow tick** on the bar at `limit/spent` of its width showing where the limit was crossed inside the full red bar (new additive `markerPercent`/`markerLabel` props on `ProgressBar`); and the shared danger red-tint token (`--color-expense-surface`) as the row background when over, at identical height/spacing. The row's pencil now opens a new **`AllocationDrawer`** (per-category: spent/left, limit slider + amount input, priority, Save) instead of the budget modal — passed the category's id and name; `BudgetRow`'s `onEdit` prop became `onAllocate`. The month-wide `AllocationPanel` sliders and the Past-months list are unchanged. **Zero budgets:** the donut card shows "No budgets set up yet" + "Create a budget" instead of an empty ring and the list card is not rendered at all. New `BudgetList.merged.test.tsx` (11 cases: header count singular/plural/zero, legend top-5 + cap, over-limit badge/tick/tint, no badge or tick at 100%, empty state, drawer open + per-category save, and a PlannerView case asserting no Expense breakdown section). `BudgetList.funding`/`focus` tests updated for the new centre copy and heading. Gates: tsc/lint/1129 tests across 78 files/build green | `CHANGELOG.md`, `PROJECT_SPEC.md`, `ARCHITECTURE.md`, `06_PLANNER_SCREEN.md`, this file. |
| 2026-08-21 | **Planner budget status unified into ONE band (presentation only — no scoring/funding logic changed):** the three stacked widgets that all said the same thing — the red "Budget needs attention" alert banner (`BudgetAttentionBar`), the "Needs Funding" card (`NeedsFundingSection`) and the "Budget Health" card (`BudgetHealthCard`) — are replaced by a single card, `components/planner/BudgetStatusBand.tsx`, rendered directly below the four metric cards (which are untouched). Layout is one horizontal row at ≥980px — conic-gradient health ring (red score arc, `--color-border` remainder, numeric score centred) → "Budget health" + info tooltip and a derived pill ("At risk" red below 60, "Good" teal at 60+) → a vertical divider → the status-flag rows → a right-anchored "Review budgets" button; under 980px the regions stack and the divider is hidden. Flags: an over-limit row listing EVERY over-limit category with its overage (`{n} budgets over their limit — Name +₦X, …`, wraps rather than truncating; absent entirely at zero), plus a funding row that is green "Every category funded · Income covers expenses" only when both hold, red "{n} categories need funding" when any are unfunded, and red "Every category funded · Income does not cover expenses" on a shortfall; if no flag applies the region collapses to a single green "All budgets on track this month." (never empty). The band does not render at all when no categories are configured. **Destination consistency:** the hero's "Review Budget" previously only scrolled while the old banner also navigated to `/?focus=over` — both now go through the shared `components/planner/reviewBudgets.ts` (`REVIEW_BUDGETS_HREF` + `scrollToBudgetAllocation`), so the two buttons on the page are identical. `Tooltip` gained an additive `wrap` prop (multi-line help text) and `group-focus-within` so a focusable trigger opens it. The three old components and their test files were deleted; `BudgetStatusBand.test.tsx` (11 cases) and one Hero destination-parity case added. The health-score formula (`budgetHealth`) and `fundingNeeds` are unchanged. NOTE: the tooltip copy is placeholder ("Reflects how many budgets are within limit, funded, and covered by income") and carries a TODO — the codebase has no written definition of the score. Gates: tsc/lint/1118 tests across 77 files/build green | `CHANGELOG.md`, `ARCHITECTURE.md`, `06_PLANNER_SCREEN.md`, this file. |
| 2026-08-18 | **OCR engine startup fixed for the packaged desktop app (real Kuda scanned-statement import); packaged app rebuilt to ship it.** (1) **ROOT CAUSE** — `TESSERACT_WORKER_OPTIONS.workerBlobURL` was `true`, so the vendored tesseract.js wrapped its worker code in a `blob:` URL whose body is `importScripts("/vendor/tesseract/worker.min.js")`; inside a blob worker that leading-slash path cannot resolve against the blob base URL and Chromium rejects it ("The URL ... is invalid") — on EVERY origin (verified empirically on both `http://` and `app://`), so the scanned-Kuda import always ended on "The built-in scanner couldn't start on this device" in the portable/dist EXE (the mock-based tests never started the real engine, which is why the suite was green). (2) **FIX — smallest production-runtime change** — `workerBlobURL: false`: tesseract.js creates the worker from its real URL, which resolves against the page origin (`app://bundle` in the EXE, the dev server in dev) — exactly how the pdfjs worker already loads in the packaged app; the worker's own `importScripts`/`fetch` calls then resolve against the worker script's URL. (3) **VERIFIED IN THE PACKAGED RUNTIME** — a scratch Electron harness registered the app's own `app://` protocol handler over the packaged asar's extracted `out/` (same scheme privileges, same webPreferences, same sandbox) and ran the REAL flow: pdfjs opened `KUDA-real.pdf` (2 pages), rendered page 1 at scale 3 (1785×2526), Tesseract started over `app://` (worker + core + traineddata) and recognized the real statement text ("2003640955 01/07/2026 - 14/08/2026 … Money In Money Out … 01/08/26 2,925.00 …"). (4) **DIAGNOSTIC PATH** — `OcrService.lastStartError` (engine-level reason, asset/worker errors only — never statement content) is threaded into `OcrPdfOutcome.engineError` and the modal appends a short "Diagnostic:" line under the generic OCR-unavailable message, so a real engine failure is visible instead of only the generic text. (5) **GATES** — Kuda/OCR suites (96 passed incl. encrypted + real-engine), full suite 1129 passed/1 env-gated skip, tsc, lint, `npm run build` + `npm run dist` (new asar verified to contain the fix + diagnostic). | `CHANGELOG.md`, this file. |
| 2026-08-18 | **Packaged desktop app rebuilt to ship the prompt-8L fix (real GTCO protected-PDF import).** The 8L header-anchored alignment seam landed 8/17 19:36–20:10 PM, but the shipped binaries (`dist/win-unpacked`, Setup, Portable — built 8/17 10:09 AM) predate it: their `app.asar` bundle lacks the alignment markers (no `^value date$`/`^posting date$`/`^details$`/`^transaction reference$`/`^ref$`/`^amount$` columnar header tokens) and contain the pre-8L chunk (`32fzkpkr_hrv2.js`), so the REAL password-protected GTCO file unlocked (Phase D was already in that build) but every left-packed row was rejected as misaligned → "No transactions could be detected in that file." The current source was re-verified end-to-end (real file + real password: 49 transactions via the env-gated real-engine test; the modern pdfjs build and a REAL Electron/Chromium worker both extract the identical 839-item text layer as the snapshot) and the app was rebuilt + re-packaged (`npm run build` + `npm run dist`, 8/18): the new `app.asar` contains the fixed chunk (`3hsf3thyhf80o.js` with the seam markers) and the packaged EXE passes the full `--smoke` suite. No source changes were needed. | `CHANGELOG.md`, this file. |
| 2026-08-17 | **Password-protected GTCO export PDF — real-fixture regression coverage (prompt 8L): the REAL protected statement now parses end-to-end — 49 transactions, 0 skipped, 0 errors — through the REAL runtime path (classify with the user's password → aligned text-layer cells → GTCO parser).** (1) **ROOT CAUSE** — `tests/fixtures/statements/GTCO-real-protected.pdf` IS protected (Standard security handler, /V 2 /R 3 /Length 128 = RC4-128 — pdfjs supports it fully; unlock was never the problem) and it HAS a text layer, but pdfjs emits NO text for empty cells: every sparse row came out left-packed and the columnar engine's misalignment guards correctly rejected the shifted numbers (0 transactions, 118 skipped). Detection was always correct (gtco, supported). (2) **FIX — HEADER-ANCHORED ALIGNMENT SEAM, NOT A PARSER SWAP** — new `rowsFromPdfItems` (`lib/statementImport.ts`) re-slots every text-layer line into the columns anchored by the page's columnar table header (nearest x; missing cells become explicit empties) when the anchor line carries ≥5 columnar vocabulary tokens; PalmPay/Kuda/mock headers stay below the trigger and keep raw reading-order cells. `lib/statementColumnar.ts` gained: silent repeated-table-header skip; a boundary stop when a second account's block starts ("CUSTOMER STATEMENT" token, or a "Statement Period" + date-range label row) — multi-account statements never mix accounts, and the 6 cosmetic invalid-date rows of the account-2 label block are no longer read; and narration continuation-line merging — wrapped-narration lines (no date/amounts/balance) form groups attached to whichever of previous transaction / next row is NEAREST by page line (`rowYs`; tie → next), falling back to the previous transaction without geometry, and trailing fragments after a boundary stop are dropped (they belong to the next account's label block). `gtcoParser.ts` composes the description from the Originating Branch cell only when a Remarks column exists (`hasColumn` gate) — the real PDF prints long narrations inside the branch column; remarks-less exports unchanged. (3) **VERIFIED END-TO-END** — classify (with the user's password) → text → cells → `detectStatementFormat` gtco → `processStatement`: supported, 49 transactions, 0 skipped, 0 errors, all rows < 105 (the page-4/5 account blocks are never read). Exact spot rows: 12 (SMS alert 0.20 with merged "…Recover Partial Charges" wrap), 17 (80,000.00 credit, "TRANSFER BETWEEN CUSTOMERS … BESTAF TECHNOLOGIES NIG LTD" — fragment above + below), 20 (79,500.00 debit "NIP TRANSFER TO PALMPAY - DAVID OSAHON OGBEIDE"), 27 (50,000.00 credit), 92 (50.00 credit), 101 (57.35), 104 (4.30, closing balance 21.77). (4) **PASSWORD HYGIENE** — the user's password is NEVER committed: the real-unlock test reads `GTCO_STATEMENT_PASSWORD` and skips cleanly when unset (CI); temp scripts that used it were deleted; the committed snapshot is decrypted text-layer items only (no password/encryption material). (5) **FIXTURES + TESTS (all REAL, additive)** — `GTCO-real-protected.pdf` + `GTCO-real.extracted.json` (real pdfjs snapshot, 5 pages, 839 items) registered as `"gtco-real"` in the manifest with layout facts; fixture describe asserts realignment, silent repeated headers, the exact 49 transactions, and the full pipeline; env-gated real-engine tests prove needs-password / incorrect-password / unlock→parse; unit tests cover alignment trigger/fallback and continuation/boundary engine behavior. Full suite 1111 → 1128 (+1 skipped env-gated), 0 failed; tsc/lint/build clean. | `CHANGELOG.md`, this file. |
| 2026-08-17 | **Rasterized Kuda export PDF — real-fixture regression coverage (prompt 8F); the parser itself needed ZERO changes.** (1) **ROOT CAUSE of the reported "zero transactions"** — `tests/fixtures/statements/KUDA-real.pdf` has NO text layer: its page content streams draw ONLY image XObjects (raw-stream evidence: `Do` paint calls, no text operators; the only real text run per page is the "Page 1 of 2" marker — a Type0 CIDFontType2 with a ToUnicode mapping just the marker glyphs). pdfjs extraction (legacy build — same engine as the modern build) surfaces exactly the two page markers, so the text-layer path correctly yields zero rows. The app's `needsOcr`/classify routing sends the file to OCR — correct, not a workaround; there is no transaction text to misroute. (2) **REAL OCR ground truth** — regenerated with the same `scripts/ocr-check.mjs` tooling (mupdf render at scale 3 + tesseract.js eng, vendored assets): `KUDA-real.ocr.txt` is the same statement as the scanned fixture (account 2003640955, period 01/07/2026 - 14/08/2026). (3) **VERIFIED END-TO-END** — the REAL app path (`extractStatementRowsWithOcr` → classify "scanned" → REAL tesseract OCR → `ocrTextToCells` → `detectStatementFormat` → `processStatement`) yields supported Kuda, 5 transactions, 0 skipped, 0 errors: 01/08/26 13:05:08 in ₦2,925.00 (local funds transfer, Sporty Internet Ltd - Disbursment/3000127755/Kuda), 02/08/26 21:39:58 out ₦2,800.00 (Wema outward, wrapped to/from), 04/08/26 16:53:03 out ₦100.00 (Opay outward), 04/08/26 16:53:03 out ₦10.00 (spend and save), plus the Spend + Save pocket's own ₦10.00 row (balance 840.00) — exact dates/times/amounts/directions/balances/rows; no summary/totals/header/footer becomes a transaction. (4) **SPEND + SAVE DECISION** — the pocket section is parsed, not skipped: each pocket's own table produces its transaction, matching the existing scanned-fixture behavior; the internal "spend and save" movements remain money movements (direction out) handled by the existing transfer/duplicate semantics. (5) **FIXTURES (all real, additive)** — `KUDA-real.pdf` + `KUDA-real.ocr.txt` + `KUDA-real.extracted.json` (real pdfjs markers) registered as manifest id `"kuda-real"`; `scripts/ocr-check.mjs` gained optional `<pdfPath> <outTxtPath>` args so any OCR ground truth is regenerable. Tests +8 (real-engine: real pdfjs → zero rows + needsOcr on the REAL file; fixture describe: OCR cells shape, Kuda high detection, 5 exact transactions, no-summary/header/footer, full pipeline supported). Parser logic, OCR scope, encryption support and import UI untouched. Full suite 1101 → 1109 green; tsc/lint clean. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 note), this file. |
| 2026-08-17 | **Password-protected bank statement PDFs (Phase D — password entry, local unlock, text/OCR routing; no new parsers, no schema change).** Previously a protected PDF landed on the generic "couldn't read that file" error; the import modal now shows a dedicated password stage. (1) **CLASSIFICATION SEAM** — new `lib/statementPdf.ts` carries the shared vocabulary: `PdfDocumentKind` ("text"/"scanned"/"encrypted"/"unsupported"), `PdfPasswordStatus` (needs-password/incorrect-password/unsupported-encryption), the typed `PdfPasswordError`, and `passwordStatusOf` (pdfjs `PasswordException` code 1/2 → status). New `classifyPdfDocument(data, { password? })` in `lib/statementOcr.ts` probes a PDF WITHOUT rendering pages (opens with pdfjs, reads the text layer, reports kind + password status). (2) **ROUTING** — `extractStatementRowsWithOcr` classifies FIRST: an "encrypted" probe throws `PdfPasswordError` (modal keeps the file and shows its password screen); on retry with a password the probe unlocks and routes by text-layer presence — "text" → the existing cells → `processStatement` → registry bank detection → the existing parsers (GTCO/OPay/Kuda/PalmPay — nothing duplicated), "scanned" → the existing OCR pipeline (`ocrPdfToCells`, per-page failure isolation, abort, phase callbacks). `pdfRowsFromPdf` gained the same `password` param + PasswordException→`PdfPasswordError` mapping. (3) **UI** — modal "password" stage: the file is kept (`pendingFile`) so Unlock retries the SAME bytes; the password lives in component state ONLY (never persisted, logged or sent; cleared on any close); wrong password stays on the stage ("That password is incorrect — try again."); unsupported encryption returns to upload with a specific message; cancellation aborts any in-flight pass and clears the password. (4) **REAL-ENGINE ENCRYPTED TESTS** — new `tests/fixtures/statements/buildPdf.ts` (mupdf, AES-256, a REAL embedded TrueType font — base-14 Helvetica is referenced-not-embedded and jsdom lacks `standardFontDataUrl`) + `statementEncrypted.test.ts` (6 tests): encrypted text Kuda → needs-password → wrong password → unlock → full pipeline (5 transactions); encrypted OPay → columnar debit-only grid → 3 transactions; encrypted GTCO → credit-only grid → 2 transactions (PDF text layers have NO empty cells — a blank column emits no text run, cells left-pack and the columnar engine's misalignment guards reject the row, so synthesized columnar grids are single-sided with trailing money columns — the app's real limitation, documented by the tests); encrypted SCANNED Kuda (the REAL fixture, encrypted in-memory only) → wrong password → unlock → OCR path with the REAL `kuda.ocr.txt` → 5 transactions; unencrypted regression of the same statements; `PdfPasswordError` typing. (5) **FIX SURFACED** — `extractStatementRowsWithOcr` reused the ArrayBuffer for OCR after `classifyPdfDocument` detached it (pdfjs transfers the buffer to its worker) → encrypted scanned PDFs crashed with "Cannot perform Construct on a detached ArrayBuffer"; the OCR pass now re-reads `file.arrayBuffer()` (matching the plain scanned path). PRIVACY: passwords are memory-only end to end (tests assert they reach pdfjs but never storage or any console output); PDFs are unlocked locally by the in-app pdfjs engine and never leave the device. Tests: +5 statementPdf, +9 statementOcr, password mapping in statementImport, +7 modal, +6 real-engine encrypted; full suite 1046 → 1101 green; tsc/lint/build green; EXE deliberately not built. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1), `PROJECT_SPEC.md`, this file. |
| 2026-08-16 | **Redesign the import review transaction list (prompt 8I.1 — UI/UX correction ONLY; no parser/OCR/categorization/duplicate/database/model changes).** The review screen no longer feels like a form: each transaction is now a COMPACT ONE-ROW LIST ITEM — [checkbox] [date] [direction icon + truncated description] [category chip or compact "Assign" button when uncategorized] [subtle confidence (High/Medium/Uncertain, hidden below sm)] [actionable status chips only: Already imported, Possible duplicate (+Skip/Keep), Duplicate, Excluded, Needs review] [right-aligned amount with explicit "+" prefix for credits, text-income/text-expense styling] [chevron]. The per-row "New" chip is GONE (an import is new by definition; the "Already imported" chip still marks ledger matches). Editing controls (Transaction type, Category, Confidence readout, Exclude from import) moved into an EXPANDED DETAILS section reached by clicking the row or its chevron (aria-expanded row + dedicated chevron button; inner controls stopPropagation): full description, merchant/provider/channel/branch line, date/time/value date/reference/transaction-type metadata, kind + category selects, exclude toggle. Duplicate-group and bulk flows unchanged. Tests updated to the new structure (expand rows before editing; chip presence/absence assertions adjusted — 66 modal tests green, full suite 1046; tsc/lint/build/dist green). | `CHANGELOG.md`, this file. |
| 2026-08-16 | **Final bank statement import audit (prompt 8M — full feature audit 8A→8L + 2 concrete cleanup fixes + documentation; no redesign, no schema change).** AUDITED all 19 checklist areas: file selection/import (modal drop zone, supported types, size/type guards), format detection (registry scoring, never filenames/bank names), PDF text extraction (pdfjs lazy-import + worker, page-by-page cleanup + destroy, y-geometry), OCR fallback (text-layer PDFs never OCR'd; page-at-a-time rendering; per-page failure isolation; abort), GTCO/OPay/PalmPay/Kuda parsers (signed-amount direction, month-first PalmPay dates, Kuda balance-delta chain vs direction tags, running-balance never read as amount — Kuda flow = first amount token, balance = last), normalization (sign = direction fact, never type), classification (11 kinds, word-boundary rules), categorization (existing categories only, confidence + reason), confidence handling (kind vs category confidence), duplicate detection (reference-first identity, same-amount-never-duplicate), review UI (filters, bulk actions, badges, possible-duplicate Skip/Keep), SQLite persistence (one atomic kv UPSERT per confirm; migration backup+marker in one transaction), error handling (generic messages, session survives, retry), privacy/security (nothing leaves the device: pdfjs worker + Tesseract assets served by the app; no temp files in the import path; only capped reference/narration persist as provenance), EXE packaging (out/** + electron/** + better-sqlite3 unpacked; OCR assets ship inside out/vendor/tesseract — an EXE user needs NOTHING installed), tests (1046 suite green), performance (one-pass extraction, OCR only when the whole doc is scanned, one page rendered at a time ~35 MB transient, UI yields before parse; note: relationship linking is O(n²) in rows — fine for real statements, a limitation for very large files). FINANCIAL CORRECTNESS VERIFIED: statement amount ≠ running balance in every parser; debit/credit direction preserved (CR/DR tags, signs, parens, balance-delta epsilon chains); fees (bank-fee/tax), refunds (income), interest (income), internal transfers (never ledger items) and expenses are distinct kinds — transfers/fees/taxes/savings/loan-payments never create ledger entries, so an internal transfer can never become an accidental expense. FIXES (the only concrete defects found): (1) **duplicated logic** — `pipelineToken` in `lib/statementPipeline.ts` was an exact copy of `headerToken` in `lib/statementColumnar.ts` (drift risk in detection scoring); the pipeline now imports the shared helper. (2) **obsolete comment** — `lib/statementNormalize.ts` header still described the pre-7A `buildCandidates → candidatesToNormalized` flow as the live path; corrected to document the legacy seam accurately. Everything else in the checklist (duplicate imports, misleading messages, broken cancellation, incomplete transactions, loading states, temp files, sensitive logging, OCR dependency problems) verified clean with zero changes. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1), this file. |
| 2026-08-16 | **Comprehensive bank statement import testing (prompt 8L — full-pipeline test pass; no feature changes, no functionality added except test coverage).** Audited the whole flow (FILE → format detection → text extraction → OCR fallback → parser → normalization → classification → categorization → duplicate detection → review → SQLite commit) against the 8L checklist and closed the gaps. (1) **REAL-ENGINE INTEGRATION TESTS** — new `tests/fixtures/statements/statementRealEngine.test.ts` (10 tests) runs the REAL pdfjs and SheetJS engines against REAL bytes (no mocks, no snapshots): the real PalmPay PDF's text layer extracts into statement cells and flows through `extractStatementRows` → `needsOcr(false)` → `processStatement` as a supported PalmPay preview with all 75 transactions; the real scanned Kuda PDF yields zero cells and `needsOcr(true)`; corrupt bytes and zero-byte PDFs reject through the real engine (the modal's generic catch path); multi-page extraction visits every page of the 3-page PalmPay statement; a real SheetJS-built `.xlsx` workbook reads back through `rowsFromExcel` and `extractStatementRows` into the pipeline (first sheet only, cells trimmed, debit-only row → 50,000 minor expense); a real CSV File flows end-to-end and is detected as GTCO (canonical Trans. Date/Value Date/Reference/Debits/Credits/Balance/Branch/Remarks header). **PLATFORM-DEPENDENT BY DESIGN (8L)**: pdfjs-dist's modern build requires browser globals jsdom lacks (`DOMMatrix` at module load, `Uint8Array.prototype.toHex` in Node), so the file (a) installs a minimal test-side DOMMatrix shim (2D affine — constructor from 6-element array/`matrix(...)` string, multiply/preMultiplySelf/multiplySelf, translate/scale, invertSelf, transformPoint; a, b, c, d, e, f) and (b) routes the app's `pdfjs-dist` import to the official LEGACY build (same parsing engine, Node-compatible shims — the pdfjs team's documented Node choice). The app itself keeps the modern build (browser/Electron renderer); the ordinary unit suite never touches these shims; real OCR engine availability is NOT required anywhere in the suite (the Kuda OCR ground truth `kuda.ocr.txt` stays the OCR-path fixture, per 8L's "do not make real OCR mandatory"). (2) **PARSER-FAILURE EDGE** — new modal test locks a registered parser throwing mid-`processStatement` (previously only extraction-level failures were tested): generic user-facing error ("We couldn't read that file…"), no parser internals or statement contents in the UI, clean upload-stage session, nothing in the store, nothing in the persisted database. (3) **OCR-FAILURE EDGE** — new modal test locks the "OCR ran but read no page" branch (`ocrUsed: true`, zero cells): shows "We couldn't read the scanned pages of that statement…", never the "OCR unavailable" copy and never the empty-file message, session clean. (4) **COVERAGE MATRIX (verified already-covered, regression-locked)**: formats GTCO/OPay/PalmPay/Kuda ✓ (text-based + OCR/scanned via the real Kuda ground truth); edge cases empty/scanned/malformed/multi-page PDF, wrapped descriptions, missing references, missing balance (PalmPay), debit-only/credit-only rows, bank charges/VAT/stamp duty/interest/refunds/internal transfers, duplicate statements (re-import), overlapping statements (8H), unknown merchants, ambiguous categories, OCR unavailable, database failure (atomicity) ✓ all locked by the existing 1,036 tests. Suite 1034 → **1046** (0 failed, 0 skipped); tsc/lint/build green. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1), this file. |
| 2026-08-15 | **Harden multi-bank statement support (prompt 8J — shared columnar engine, real PalmPay parser, registry capabilities, wrapped-line geometry; GTCO/OPay/Kuda behavior unchanged and test-locked).** (1) **SHARED COLUMNAR ENGINE** — new `lib/statementColumnar.ts` (`headerToken`, `ColumnarSpec`, `columnarHeaderScore`, `parseColumnarStatement`): the duplicated GTCO/OPay engine logic (header vocabulary scoring, positional fallback, per-row skip/error reporting, description/reference caps, value-date fallback) is now ONE engine driven by a thin per-bank spec; `lib/gtcoParser.ts`/`lib/opayParser.ts` are specs over it (all exports preserved; ids now `gt-r`/`op-r`). (2) **REAL PALMPAY PARSER** — new `lib/palmpayParser.ts` reads the REAL text-layer statement (75 certified rows, printed totals ₦183,800.71 in / ₦340,270.00 out): month-first MM/DD/YYYY dates (08/09/2026 = 9 Aug, never 8 Sep), SIGNED amounts (sign decides direction — never the "unknown" default), wrapped Detail/Transaction-ID lines merged in reading order, lone page-number footers ignored, no balance/value-date/category ever fabricated, per-row "missing debit and credit" skips (never silent), reference cap 80, ids `pp-r`. (3) **WRAPPED-LINE GEOMETRY** — y geometry is threaded end-to-end: `groupPdfRows`→`ExtractedStatement.rowYs`→`StatementOcrOutcome.rowYs`→`StatementPipelineInput.rowYs`→`parse(cells, context, rowYs?)`; PalmPay attaches each continuation to the nearest date line within 8 y units **on the same page** (pdfjs y resets per page — the real page-2 fragment at y=568.5 would otherwise glue onto the page-1 date line at y=570.5); index-walk fallback for geometry-less CSV/Excel. The real "Send to FRIDAY PATIENCE NISMA" row merges verbatim (index adjacency alone is ambiguous — the before-fragment of the 08/12 date line is also the after-adjacent row of 08/13). (4) **REGISTRY CAPABILITIES** — every `BANK_PARSERS` entry carries `capabilities { ocrAware, wrappedLines }` (GTCO/OPay false/false, Kuda true/true, PalmPay false/true); `ImportProvenance.bank` + `IMPORT_BANKS` include "palmpay"; `supportedBankList()` → "GTCO, OPay, Kuda, PalmPay". **OCR matrix is accurate and non-overstated: OCR is implemented for KUDA ONLY (`ocrAware: true`); GTCO/OPay/PalmPay are text-layer/columnar (`ocrAware: false`) — a scanned/image-only PDF for them is UNSUPPORTED and lands honestly on "unsupported" (zero transactions, never misdetected).** (5) **DETECTION** — PalmPay entry min 6 (real header 11; generic "Transaction Date + Detail + Amount" = 5 never qualifies); cross-bank matrix test proves each bank's header detects as ITSELF (PalmPay header: GTCO 3 / OPay 3 / Kuda 4, none viable; GTCO header 14 vs OPay 13 → distinctive tie-break keeps GTCO). **Bank-specific DATE handling is preserved per parser, never normalized to one convention: PalmPay stays month-first MM/DD/YYYY ("08/09/2026" = 9 Aug, never 8 Sep), GTCO/OPay day-first NGN, Kuda day-first with 2-digit years.** (6) **FIXES SURFACED** — `headerToken`/`pipelineToken` trailing trim ("(NGN)" strip left trailing spaces); OPay value-date fallback preserved in the engine; `groupPdfLines` now top-down (y descending) so the PalmPay table header is within the first-12-row detection scan. (7) **TESTS** — new `palmpayParser.test.ts` (17) incl. real-fixture totals/lens rows/reading order/skip-never-silent; cross-bank describe in statementPipeline; fixture integration passes `rowYs` through `processStatement`; manifest gains `fixtureRowYs`. Suite 1009 → 1030; tsc/lint/full suite/build green. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1), this file. |
| 2026-08-15 | **Security & privacy hardening for bank statement import (prompt 8K — audit + targeted hardening + tests; no feature redesign, no schema change).** AUDITED every surface: imported files, temporary files, OCR images, OCR text, parsed transactions, logs, error messages, SQLite persistence, cached data, debug output, analytics/telemetry. FINDINGS + FIXES: (1) **RAW DATA** — files/OCR images/OCR text live only in renderer memory (`file.text()`/`arrayBuffer()`, canvas `getImageData`); NO temporary files are created by OCR or parsing (nothing to clean up on success or failure — the only temp-file path in the app is the desktop atomic-write helper, already fixed in 7B); nothing is uploaded; no analytics/telemetry (no fetch/beacon/XHR in lib/). (2) **PARSED TRANSACTIONS / SQLITE** — only CONFIRMED rows persist via the single storage seam: capped ledger fields + `importSource` provenance (reference ≤80, raw narration ≤200, statement date, bank — required for re-import detection); balances, full raw statements, account numbers and OCR text never persist. Fixed a stale comment contract in `lib/statementTypes.ts` that claimed raw narration is "never persisted" (it IS persisted capped, on confirmed rows only, never logged) — docs now state the truth. (3) **ACCOUNT NUMBERS** — never extracted into structured/persisted fields (Kuda parser refuses bare 10+ digit runs as amounts); header account numbers masked at display ("Account •••• 6789") as a convenience, NOT the security boundary (the boundary = never structured/logged). (4) **ERRORS** — every user-facing failure is generic (no stack traces, filesystem paths, OCR command lines, parser internals); the import catch blocks don't even `console.error` (7B regression-tested). (5) **LOGGING** — no renderer path logs statement content; main-process logs are startup/smoke/paths only. (6) **DEPENDENCIES / NETWORK** — pdfjs + SheetJS fully local; Tesseract.js fully vendored, but the shipped bundles carry **jsdelivr CDN DEFAULTS** for worker/core/lang that are only safe because every path is overridden to `/vendor/tesseract/` with gzip off — HARDENED: `lib/ocrService.ts` now exposes the config as a named `TESSERACT_WORKER_OPTIONS` const and new `lib/__tests__/ocrService.test.ts` (4 tests) asserts local-only paths, no network origin, gzip false, workerBlobURL true — a stray CDN path fails the suite. (7) **EXE DISTRIBUTION** — the four OCR assets ship inside the EXE via the normal static-export path (`public/vendor/tesseract/` → `out/` → asar, `app://bundle/vendor/...`): no native OCR module, no separate installation. **DOCUMENTED PACKAGING LIMITATION (8K): `public/` is currently UNTRACKED in git** — `scripts/fetch-ocr-assets.mjs` (pinned URLs, dev-time network only) must run before a fresh build/dist or the packaged EXE ships without OCR (graceful "scanned + OCR unavailable" fallback; CSV/Excel still fully supported). Suite +4 → 1034; tsc/lint/build green; out/vendor/tesseract verified present after build. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 privacy + OCR), this file. |
| 2026-08-15 | **Bank statement review & confirmation experience (prompt 8I — targeted improvements to the import modal; no redesign, manual expense entry untouched).** (1) **REVIEW SUMMARY** — the preview header now also counts duplicates (rows flagged within the statement), uncategorized (ledger-able rows without a category) and low-confidence (low/uncertain) transactions, on top of the existing bank chip, statement period, transactions/expenses/income/transfers/needs-review/excluded — every number the review screen should show. (2) **NO RAW PARSER INFO** — the technical `detectionReason` ("GTCO header vocabulary (score 14, distinctive 13)") is no longer rendered in the review banner (stays a data-only field) and the unsupported-bank explanation no longer prints per-bank scores for ambiguous headers. (3) **NEW FILTERS** — Uncategorized + Low confidence pills with live counts. (4) **CONFIRMATION** — footer summary "You're about to import N transactions." immediately before the final Import action ("Assign a category to the highlighted rows to import." while a category is missing; "Nothing new will be imported." when nothing is importable). (5) **SUCCESS** — "N imported · N skipped as duplicates · N requiring review" (requiring review = imported rows still flagged needs review / low or uncertain confidence) + the full Skipped breakdown (now incl. excluded). (6) **DUPLICATE COPY** — the banner explains each group is imported once and the repeats are skipped automatically — no manual removal needed. (7) **TESTS +8 modal** (review-summary counts, within-statement duplicate count, raw-scores-never-shown, uncategorized/low-confidence filters, confirmation summary, blocked-import message, success line incl. duplicate-only); 5 done-summary assertions updated to the new friendly line. Suite 1001 → 1009; tsc/lint/build green. | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1), this file. |
| 2026-08-15 | **Safe, idempotent bank statement import (prompt 8H — full audit + test-locking pass; no feature changes).** Verified the whole import feature against the safety contract and locked every guarantee with 13 new tests. (1) **EXACT DUPLICATE STRATEGY (locked):** identity = bank + date + amount + direction + reference + normalized description; references PREFERRED but never required — a ledger row WITH a stored reference matches only by that reference (same-day same-amount payments with DIFFERENT references are distinct transactions), only reference-less ledger rows fall back to full-details (date + amount + direction + normalized description); amount alone NEVER matches (two ₦1,000 same-day rows are never duplicates); reference-less rows stay CONSERVATIVE — partial overlap → "possible duplicate" (default KEEP + user Skip/Keep), never silently merged/deleted. Duplicate fees/refunds can never double-book: fees & money movements never enter the ledger at all; refunds import once by reference or full details. (2) **IDEMPOTENCY:** same statement twice = 0 new / N already existing; OVERLAPPING statements (January, then January–March) import ONLY the genuinely new rows — existing rows counted "already existing", ledger untouched; identical reference-less rows merge only on full-details equality. (3) **PREVIEW BEFORE COMMIT:** the plan (imported/excluded/movements/duplicates/already-existing/possible/failed/missing-category) is computed before any write; nothing reaches the ledger until confirm. (4) **ATOMICITY:** one confirmed import = one `addTransactions` write = one atomic kv UPSERT; a mid-batch failure writes NOTHING — new modal test proves a throwing write leaves the persisted database untouched and returns to the upload stage with the session intact for retry. (5) **TESTS (+13):** statementIdentity +5 (same-day same-amount different descriptions → possible-duplicate never already-imported; identical reference-less merge only on full details; duplicate refunds by reference AND by details; distinct refunds never merged; duplicate fees never silently confirmed), statementRelations +2 (distinct-reference fee rows stay separate; same-reference fee duplicates group), statementPipeline +5 (overlapping statements import only genuinely new rows; duplicate refunds; reference-less refund fallback; duplicate fees = movements never double-booked; empty statement), modal +1 (atomicity on a failed write). Suite 988 → 1001; tsc/lint/build green. | `CHANGELOG.md`, this file. |
| 2026-08-15 | **Automatic expense categorization for imported statements (prompt 8G — deterministic, LOCAL, no external AI API; manual expense-entry behavior untouched).** A category matching layer that assigns the app's EXISTING categories to imported rows, with a confidence level on every assignment. (1) **MERCHANT NORMALIZATION** — new `lib/merchant.ts`: `normalizeMerchantKey` (lowercase, punctuation stripped, whitespace collapsed), word-boundary `merchantKeyMatches`, curated `KNOWN_MERCHANTS` (telcos MTN/Airtel/9mobile/Glo/Spectranet/Smile incl. aliases Etisalat→9mobile, Glo/Globacom, D.S.T.V→DSTV; streamers Netflix/Spotify/Showmax/Disney+/Apple TV/Amazon Prime/YouTube Premium; rides Uber/Bolt/inDrive; retail Shoprite/SPAR/Checkers/Justu/Jumia/Konga/Game/Park N Shop; food Chowdeck/Glovo/Uber Eats/Pizza Hut/Domino's/KFC/McDonald's/Chicken Republic/Kilimanjaro/Tantalizers/Mr Bigg's; fuel Total/Oando/NNPC/Conoil/11 PLC; health MedPlus/HealthPlus), and `extractMerchantFromNarration(description, provider?)` — a reusable mechanism that exposes "FRIDAY PATIENCE NISMA" from "Transfer to FRIDAY PATIENCE NISMA \| Sterling Bank \| 0059375307" WITHOUT modifying the original description (known merchant → HIGH, transfer recipient → MEDIUM; banks/wallets like Sterling/Wema/PalmPay/OPay/Paystack/Kora are deliberately NOT merchants). Replaces the local `extractRecipient` in classify. (2) **CATEGORY MATCHING** — new `lib/categoryMatching.ts`: `MERCHANT_CATEGORY_HINTS` maps merchants → category-NAME hints resolved adaptively against whatever expense categories the user has (exact name then containment, never hard-coded ids — "MTN" → hints ['utilities','mobile data','data',…], so it lands on Utilities OR a user's "Mobile Data"/"Bills & Utilities"; food merchants fall back to Groceries when no Food category exists) + `matchExpenseCategory(description, provider, categories)` → `{categoryId, confidence: high|medium|low, reason}`. Confidence model: HIGH = known merchant/provider whole-word match or the category's own name; MEDIUM = strong whole-word keyword/alias (≥5 chars, reusing the shared `KEYWORD_ALIASES` vocabulary now exported from `statementImport.ts`); LOW = short/weak alias (<5 chars) — LOW assignments are never silently certain. Normalization is punctuation/case/number-insensitive ("MTN 3.2GB, 2 Days Plan!" matches). (3) **CLASSIFY INTEGRATION** (`lib/statementClassify.ts`): for expense rows the matcher refines the category + records `categoryConfidence`/`categoryReason` (new transient fields on `NormalizedBankTransaction`; HIGH merchant overrides the rule hint, MEDIUM/LOW keeps the rule's category; learned-rule decisions from 6A are never refined — user corrections always win); an UNKNOWN debit upgrades to expense ONLY on a HIGH known-merchant match ("SHOPRITE PURCHASE" → expense + Groceries high); fees/taxes/refunds/interest/savings/transfers/internal-transfers are NEVER reclassified (8F eligibility layer respected); LOW keyword matches never flip a rule-decided row to review. FUTURE-FRIENDLY OVERRIDES: the 6A `learnedRules` mechanism is unchanged and test-proven to beat the matcher (correcting a provider-based category becomes a reusable rule for later imports — no rule-management UI built). TESTS: 44 new (`merchant.test.ts` + `categoryMatching.test.ts`): keyword matches, merchant aliases (Etisalat/Globacom/D.S.T.V), case/punctuation/number insensitivity, Nigerian providers, mobile-data packaging, transfer/bank/wallet non-merchants, fees/refunds/interest/savings never matched, internal transfers, unknown merchants, confidence tiers, adaptive resolution to different category names (including no-hint null), whole-word guards ("FOODSTUFF GLOBAL" stays review), unknown→expense upgrade + non-upgrade, and the learned-rule-wins flow. Suite 944 → 988 (73 files); tsc/lint/build green; manual expense entry (`suggestCategory`, `lib/categorize.ts`) untouched. | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 module layout, §4.1 classify), this file |
| 2026-08-15 | **Transaction normalization & classification hardening (prompt 8F — parsers NOT rebuilt; audited the normalized model, fixed the inconsistencies, added a deterministic transaction-type layer).** (1) **NEW DETERMINISTIC TRANSACTION-TYPE LAYER** (`lib/transactionTypes.ts`): `TransactionType` (transfer, card-payment, bank-charge, transfer-fee, vat, stamp-duty, sms-charge, airtime, mobile-data, interest, refund, savings, withdrawal, deposit, internal-transfer, loan-payment, salary, unknown) + `classifyTransactionType(description)` — ordered word-boundary table, first-match-wins, **narration-only** (direction is never consulted: an incoming transfer is still "transfer", never income; income/expense stays the classification layer's job) and kept **SEPARATE from expense categories** ("Transfer to John" → txType transfer while its category may later become Personal/Family). Every classified row now carries `txType` (new transient field on `NormalizedBankTransaction`; never persisted, privacy-safe). Specificity locked: "VAT on Transfer Fee"→vat, "Commission on NIP Transfer CHARGES"→transfer-fee, "Card maintenance fee"→bank-charge (never card-payment), "POS Withdrawal"→card-payment, "ATM Withdrawal"→withdrawal, "OWealth Deposit (Transaction Refund)"→refund before internal-transfer, "Transfer to OWealth"→internal-transfer. Fees/charges are split so a charge is **never automatically an ordinary expense** (kind stays bank-fee/tax, categoryId null, never importable). (2) **KIND CLASSIFICATION STRENGTHENED** (`lib/statementClassify.ts`): Kuda's structural labels now classify deterministically instead of staying unknown — "local funds transfer"/"outward transfer"/"incoming transfer" → **transfer** (never income/expense), "spend and save"/"spend + save" → **savings** (own-account movement, never spending); the matcher regex-escapes patterns so the literal "spend + save" phrase matches as written. (3) **CROSS-PARSER NORMALIZATION FIXED** (`lib/gtcoParser.ts`): GTCO no longer uses the transaction REFERENCE (or branch) as the `description` fallback — remarks-only, else "(no description)" (reference stays in its own field); same description semantics as OPay/Kuda. (4) **CONSISTENCY LOCKED BY TESTS** (30 new: `transactionTypes.test.ts` + `statementNormalization.test.ts` — same conceptual transaction → same representation across GTCO/OPay/Kuda: date, debit/credit split, direction, balance, kind AND txType; amount always the transaction amount, never the running balance; single-amount Kuda rows leave balance absent; charges never expenses; incoming transfer not income; ambiguities honestly preserved — Kuda's "local funds transfer" could be a salary (stays transfer), Kuda "bank charges" can't separate VAT from SMS (stays generic bank-charge), unreadable narrations stay unknown→needs review on every bank). Existing tests updated where behavior intentionally improved (GTCO no-remarks fallback; Kuda end-to-end now asserts transfer/savings instead of unknown). Suite 914→944; tsc/lint/build green. | CHANGELOG, ARCHITECTURE (§4.1 diagram + lib listing) |
| 2026-08-15 | **Kuda statement parser (prompt 8E — the scanned Kuda statement from 8C/8D is now read; GTCO/OPay/PalmPay untouched).** ONE parser for BOTH Kuda forms: text-based PDFs (column-split cells) and scanned PDFs (8D OCR cells) land in the same `lib/kudaParser.ts` → registry entry (`BANK_PARSERS` + `supportedBankList` → "GTCO, OPay, Kuda"), detected through the existing pipeline, no OCR-architecture rework (the prompt's constraint — except ONE genuine defect found and fixed at the DETECTION layer: OCR'd lines arrive as ONE CELL per line, single spaces, so cell-position vocabulary could never match; `kudaHeaderScore` matches multi-word PHRASES inside cells over a normalized token stream, unit tokens like "(NGN)" stripped). DETECTION: `KUDA_HEADER_PHRASES` weights (date/time 3, money in/out 2, opening/closing balance 3, to/from 2, spend account / spend + save 2, category/description/summary/account number 1); the registry entry carries its own `minHeaderScore` 8 (vs the shared 5) because "opening balance"/"closing balance" alone sum to 6 — a generic statement must never qualify on those labels; real Kuda tables always score 10–11 (totals row 10, full table header 11). Cross-bank verified: GTCO 2–3, OPay 1, PalmPay 4 (all below 5), transfer-heavy random 0; detection is high-confidence on both real forms. PARSING: whitespace-token stream — a transaction starts at a date row (dd/mm/yy; `parseStatementDate` gained a calendar-validated 2-digit-year branch, day-first for NGN; `parseAmountCell` gained the OCR-read naira sign "#" — superset changes, existing parsers/84 tests unaffected), optional next token = time (HH:mm:ss); section rows ("Spend Account", "Spend + Save", "Summary", "Type…", totals rows, table headers, "Page N of M") close the open block and reset the balance chain; continuation lines merge wrapped descriptions — time-led rows always, text-only rows only until the block's time row (OCR footer junk guard: the real dump's "cea/LN/_ Sn/…" never pollute descriptions); amounts = first token = flow, last = balance (single-amount rows: flow only, never mislabeled); bare 10+ digit runs never amounts (references like 3000127755 stay narration). DIRECTION IS STRUCTURAL, never guessed: exact balance-delta chain (epsilon 1 kobo; a mismatch poisons the chain) → then Kuda's own printed category tags (outward/spend/merchant payment/bill payment/withdraw/stamp duty/bank charges/debit → out; local funds/incoming/top up/salary/deposit/refund/interest/credit → in); "Transfer" alone never decides (a bare "Transfer to JOHN DOE" is skipped with "unresolved direction"); per-row errors ("missing debit and credit" | "unresolved direction") skip + report, never crash. Optional-field honesty (8C): NO reference/channel/valueDate columns (Kuda prints none; the "/"-strings are narration preserved in description); balance/category/counterparty/account/time present. REAL FIXTURE GROUND TRUTH: the 8D OCR dump became `tests/fixtures/statements/Kuda/kuda.ocr.txt` (regenerated WITHOUT the dump script's "=== Page N ===" banners so the file is a faithful OCR transcript; manifest `ocrTextFile` + `loadFixtureOcrText`); the fixture test runs it through the exact runtime path — `ocrTextToCells` → `detectStatementFormat` (kuda, high) → `parseKudaStatement` → the 5 REAL transactions (01/08/26 ₦2,925.00 in 13:05:08 bal 2,939.33; 02/08/26 ₦2,800.00 out 21:39:58 bal 139.33; 04/08/26 #100.00 out 16:53:03 bal 839.33; 04/08/26 #10.00 out bal 29.33; Spend + Save 04/08/26 #10.00 out 16:53:03 bal 840.00) — exact kobo amounts, times, running balances, source rows; summary/totals/footers/page-2 noise never become transactions; the pipeline reports supported. TESTS: 35 unit (`kudaParser.test.ts` — detection incl. one-cell OCR rows + false positives, parse of BOTH forms to identical transactions, wrapped descriptions 2- and 3-line, fees/tag-fallback, references-not-amounts, multi-section chain reset, malformed/empty/junk) + 5 fixture (real OCR ground truth) = 40 new (874 → ~914). KNOWN LIMITS, honestly stated: the real page-2 scan reads nothing (zero rows fabricated); a mangled OCR balance breaks the delta chain (tag fallback keeps the row, chain resets); a very long description's 3rd wrap line is dropped once the time row merged (keeps footer junk out); the last two wrapped description tokens ("Bank", "Digital Services Limited") of the real dump are dropped by the same guard. Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 lib listing, §4.1 diagram, parsers/fixtures/OCR bullets), this file. |
| 2026-08-15 | **Scanned-statement OCR fallback (prompt 8D — LOCAL OCR ONLY, privacy-first; no Kuda parser, no cloud, no AI/LLM, GTCO/OPay/PalmPay parsing untouched).** WHY: 8C proved the real Kuda PDF is scanned (image pages, no text layer) — it needed OCR before any Kuda parser work. WHAT WAS BUILT: (1) `lib/ocrService.ts` — `OcrService` engine abstraction (`isAvailable()` never throws, `recognize(image, page)`; nothing logs statement content) + the Tesseract.js 5.1.1 implementation. **Everything is served by the app itself** from `public/vendor/tesseract/` (tesseract.min.js UMD + worker.min.js + tesseract-core-simd.wasm.js + eng.traineddata, all pinned, regenerated by `scripts/fetch-ocr-assets.mjs`): the API bundle is injected as a classic script (never bundled — Turbopack would transform the worker/core), `workerPath`/`corePath`/`langPath` point at `/vendor/tesseract/` (`app://bundle/vendor/tesseract/...` in the EXE), `gzip: false` (vendored data is the plain traineddata), `workerBlobURL: true`. NO network anywhere: a failure of any asset → `isAvailable()` false → graceful user message. (2) `lib/statementOcr.ts` — orchestration: `needsOcr(cells)` decides when a PDF's text layer is NOT a usable statement — empty, or < `OCR_MIN_TEXT_LENGTH` (40) chars, or no row carries BOTH a parseable date AND an amount (so "Page 1 of 2", logo headers, cover pages and account-number-only pages all go to OCR, while PalmPay/GTCO/OPay text never does — test-proven with the real fixtures). `ocrPdfToCells(data, { service, onPhase, signal, renderPage? })`: lazy pdfjs import (same pattern as `rowsFromPdf`), pages rendered at `OCR_RENDER_SCALE` 3 (216 dpi) via an INJECTABLE renderer (default = pdfjs canvas render; tests inject a stub — jsdom has no 2d canvas), **per-page failure isolation** (`failedPages` — one bad page never kills the import; the UI warns instead), abort via `AbortSignal` → `OcrAbortError` (closing the modal cancels), `onPhase("reading"|"scanning"|"extracting")`. OCR text → the SAME cell grid the text pipeline produces (`ocrTextToCells`: lines → rows; tabs / 2+ space runs → column splits — single spaces stay inside descriptions), pages merged in order, then the existing detection/parsers/pipeline run UNCHANGED. (3) Routing: `extractStatementRowsWithOcr(file, options)` — CSV/Excel never OCR'd; text-layer PDFs never OCR'd; only scanned PDFs go through OCR; the modal now calls this. (4) UI (`ImportStatementModal.tsx`): per-import `AbortController` (aborted on close), phase-aware processing label ("Parsing your statement…" → "Reading your statement…" → "Scanning pages…" → "Extracting transactions…"), scanned+OCR-unavailable message ("This statement is scanned and doesn't contain selectable text. OCR is required to read it. The built-in scanner couldn't start on this device — if your bank offers a CSV or Excel export, use that instead." — no internals), scanned-but-unreadable message, partial-failure warning in the preview ("N scanned page(s) couldn't be read — the rest of the statement was imported."), empty-file message preserved for CSV/Excel. (5) PACKAGING INTO THE EXE (the 8D report question): `public/` → `out/` via Next's static export (`next.config.ts` `output: "export"`), Electron serves `out/` over the existing `app://bundle` privileged protocol (`electron/main.cjs` — `supportFetchAPI` already true; worker fetches + WASM instantiation work over it). NO electron-builder changes, NO native module, NO `extraResources` — the OCR assets are plain static files shipped inside the asar/EXE like the rest of the UI. The worker loads via Blob URL (tesseract default) so `importScripts` on the custom scheme is never needed. (6) TESTS (33 new; suite 841→874): `lib/__tests__/statementOcr.test.ts` (25) — `needsOcr` (real-fixture-shaped cases: empty, markers, logo-only, short, PalmPay-like false, GTCO/OPay false, page-number footers true, date-without-amount true), `ocrTextToCells` (columns, single-space descriptions, tabs, blank lines), `ocrPagesToCells` (order + failed pages), `ocrPdfToCells` (happy path + phase sequence, recognition-failure isolation, render-failure isolation, OCR-unavailable → nothing runs, abort mid-way stops after the current page, abort before start), routing (CSV never OCR'd, text-layer PDF never OCR'd, scanned PDF → OCR cells become the statement cells, unavailable → empty cells, extraction errors propagate). `tests/fixtures/statements/statementOcr.fixture.test.ts` (4) — the REAL Kuda PDF end-to-end: `fixtureCells("kuda")` = `[]` → `needsOcr` true; real PalmPay cells → `needsOcr` false; real Kuda file through `extractStatementRowsWithOcr` with a fake engine → OCR engaged, pages visited by extraction AND OCR (4 `getPage` calls), OCR cells flow through `detectStatementFormat`/`processStatement` exactly like any extraction (here the simulated OCR text is even parseable as OPay → 3 transactions; nothing is guessed about Kuda's layout); one failing page keeps the rest usable. `ImportStatementModal.test.tsx` (+5): scanned+unavailable message, OCR→preview, "Scanning pages…" phase label, partial-failure warning, close-mid-OCR cancellation (nothing persisted). (7) MANUAL REAL-OCR VERIFICATION (dev-only, never in vitest): `scripts/ocr-check.mjs` renders the real Kuda pages with **MuPDF** (pure WASM — pdfjs canvas rendering cannot run in plain Node: pdfjs renders with its OWN `Path2D` class which @napi-rs/canvas rejects on `clip`/`fill`/`stroke`, and patching the native context crashes the process with an access violation) and OCRs them with the VENDORED language data (`devDependencies` `tesseract.js@5.1.1` + `mupdf` only — nothing new in the app bundle; `@napi-rs/canvas` REMOVED from devDeps as unused). REAL RESULT on the actual Kuda PDF: page 1 reads well — account number 2003640955, statement period 01/07/2026–14/08/2026, opening/closing balances 44.33/69.33, "Summary" section (36 OCR lines); page 2 is dark/noisy and reads poorly (7 lines of near-noise) — an honest, visible limitation of OCR quality on bad scans, handled by per-page isolation. LIMITS, STATED: OCR output is DETECTION-only, feeds the unchanged pipeline, and no Kuda parser exists yet (8E+); OCR text never leaves the device and is never logged or stored. Gates: tsc/lint/full suite 874/build green | `CHANGELOG.md`, `ARCHITECTURE.md`, this file. |
| 2026-08-15 | **Real Kuda + PalmPay statement fixtures (prompt 8C — the actual bank PDFs become the ground truth; NO parsers built, NO engine changes).** The real statements now live in `tests/fixtures/statements/{Kuda,Palmpay}/` with pdfjs text-layer snapshots captured from them (`*.extracted.json` — generated from the PDFs, never hand-edited; the PDFs are the source of truth, nothing synthesized). New fixture registry `tests/fixtures/statements/manifest.ts`: per-fixture layout facts, pages, text-layer presence, and an OPTIONAL-FIELDS MAP — which optional normalized fields the real statement carries (a future parser may only produce those; missing values are never forced into fake ones, per the 8C brief: balance, reference/Transaction ID, counterparty, category, account/sub-account, time, value date). WHAT THE REAL PDFS SHOWED: (1) the **Kuda PDF is SCANNED** — 2 pages, 6 image XObjects / 3 JPEG streams, no extractable transaction text; pdfjs yields only "Page 1 of 2"/"Page 2 of 2" markers, so through the runtime path the cells are EMPTY (`groupPdfLines` drops single-item lines) and a future Kuda parser needs OCR of the printed layout (statement metadata, transaction tables, spend+save section). (2) the **PalmPay PDF has a text layer** (3 pages): account header block (Name / Phone Number / Account Number 808 238 9369 / Total Money In ₦183,800.71 / Statement Period 07/15/2026–08/16/2026 / Total Money Out ₦340,270.00 / Print Time / Address) + a 5-column table (Transaction Date | Transaction Detail | Money In (NGN) | Money Out (NGN) | Transaction ID) — transaction rows are 'MM/DD/YYYY HH:MM:SS AM/PM' dated, Transaction Detail and Transaction ID WRAP across lines, amounts are signed ('+' only in Money In, '−' only in Money Out, thousands commas, 2 decimals), and there is **NO running balance column** (optional-field honesty: balance absent, reference present, counterparty present, category absent, account present, time present, value date absent). TESTS (14 new, `tests/fixtures/statements/statementFixtures.test.ts`): manifest integrity (PDFs exist, non-empty, snapshot page counts match); Kuda — real cells empty through the runtime path, never detected as GTCO/OPay (`unknown`, "no bank header vocabulary found"), pipeline → `unsupported` with zero transactions ("matches none of the supported banks", "Nothing was read"); PalmPay — real cells contain the header block + exact 5-column table header, never falsely detected as GTCO/OPay, `unsupported` until a parser exists; PLUS a TEST-ONLY integrity lens (explicitly documented as NOT the product parser — parsers come in a later prompt) that reads the real extraction BY COLUMN (anchors from the statement's own table header, y-proximity attachment of wrapped cells within a page) and certifies the fixture: exactly **75 transaction rows** and **no false date matches** (header block / summary rows / page footers never mistaken for transactions), **sums match the statement's own printed totals** (₦183,800.71 in = 18,380,071 minor / ₦340,270.00 out = 34,027,000 minor — proves the extraction is complete), every row carries exactly one signed amount (never both/neither) + date + detail + transaction id, wrapped cells join in reading order (real rows: "Send to FRIDAY PATIENCE NISMA" −400.00 id 03392cdb1501; "Received from DAVID OSAHON OGBEIDE" +1200.00 id 6038xn04ab06; "Disbursement-Installment loan" +35000.00 id CAL37zbz4cd01; "Stamp Duty" −50.00 with the wrapped id "20260809114820399392 0649447"), and the 5 table columns contain no balance column. NO registry/parser/pipeline/modal changes — GTCO/OPay behavior untouched; Kuda/PalmPay stay honestly unsupported until their parser prompts. Suite 827→841 (14 new); gates: tsc/lint/full suite 841/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (fixture note), this file. |
| 2026-08-15 | **Final bank statement import production-readiness audit (prompt 8B — audit + 2 targeted fixes, no new functionality).** AUDITED EVERY STAGE of the E2E flow (upload → select → detect → parse → normalize → classify → relationships → duplicates → preview → review → confirm → SQLite import → result). FINANCIAL CORRECTNESS: classification NEVER alters amounts (`classifyTransaction` spreads the row, rewrites only metadata — test-locked); `planImport` writes `amount > 0` minor units only (debitAmount → expense, creditAmount → income); a row whose kind/direction mismatch would produce `undefined` amount is counted "failed", never written with a wrong amount; `parseAmountCell`/`mergeThousands` never invent values (unparseable → skipped + reported); dates validated against real calendar days; balance-delta direction inference epsilon-guarded; transfers/fees/taxes/savings/loan payments never enter the ledger (no ledger transfer type); refund/interest import as income via creditAmount. DUPLICATES: repeated imports cannot double-book — same bank+amount+direction+normalized reference → "already imported" (never written); in-statement groups keep the first row only; possible duplicates default to KEEP with user Skip/Keep. DATABASE SAFETY: one confirmed import = one `addTransactions` set = one `budget-planner:state` kv row = one atomic SQLite UPSERT (WAL + synchronous NORMAL); a failed write touches nothing and the session stays for retry. EXISTING APP: full suite 827/827 green (forms, categories, dashboards, reports, history, navigation); `db:check` verifies embedded SQLite init. PERFORMANCE measured at 10/100/500/1,000+ rows (buildCandidates 2.9/2.0/5.1/13.8 ms; detectRelationships 1,000 rows ≈ 86 ms; ledger matching 1,000 rows × 10,000-row ledger ≈ 33 ms) — fixed the two obvious hot spots: `planImport` recomputed on EVERY modal render → `useMemo([transactions, preview, existingTransactions])`; `PreviewStage` re-derived the per-row ledger match map + duplicate set per render and the whole review table re-rendered on any single-row edit → `statusById`/`duplicateIds` memoized + review row extracted into memoized `ReviewRowItem` (stable `updateRow` via `useCallback`) so a correction re-renders one row, not 1,000. WINDOWS/EXE: SQLite embedded (better-sqlite3 + asarUnpack, no separate install); data in `<userData>`; statement picker is the standard HTML input (cancel = no-op); statements never touch disk; atomic-write temp cleanup verified (7B); no dev-machine paths (only `DEV_URL` localhost behind `--dev`); NEW guard — `openDatabase` failure now shows a native error dialog + clean exit (previously an unhandled rejection leaving a broken app whose storage IPC crashed per call). ERROR HANDLING: corrupt/unsupported/cancelled/parser-failure/empty/duplicate/unexpected-structure all covered; parser failures reset the session with generic copy (no internals); SQLite failure → guarded open + graceful persistence degradation (writes-off flag + hydrate-error UI). CODE QUALITY: no TODOs; no dead code introduced (generic `buildCandidates`/`candidatesToNormalized` retained by design as the documented generic engine for future registry banks); no swallowed exceptions in feature paths; logging stays minimal. REMAINING KNOWN LIMITATION (fail-safe by design): a DEBIT-side refund/interest row (bank wrote the reversal as a debit) stays classified income and is reported "failed" at import — never written with a wrong amount. VERDICT: ready for real-world testing; no critical issues remain. Gates: tsc/lint/full suite (827)/build green; `out/` + `dist/` rebuilt. | `CHANGELOG.md` |
| 2026-08-14 | **Complete bank statement import test suite (prompt 8A — comprehensive test pass; no feature changes, no refactors).** CHECKLIST AUDIT: (1) PARSERS — GTCO (21) + OPay (25) suites: normal debit/credit, NIP/transfer narrations, charges/VAT/interest, merchant+provider lifting, truncation, headerless positional fallback, title rows/extra columns, missing columns, invalid dates, unparseable amounts, missing debit+credit, footer rows, decimals (leading-dot, small naira), comma amounts, missing description fallback, multi-row independence. GAPS CLOSED (+3): empty statements — both parsers return zero transactions/skipped/errors on `[]` and all-blank rows (no crash; positional fallback + empty data rows); very large amounts — GTCO ₦2,500,000,000.00 debit → 250,000,000,000 and ₦9,999,999,999.99 credit → 999,999,999,999 minor units exact. (2) CLASSIFICATION — all 11 kinds explicitly covered (49 cases): expense (rent/mobile data/MTN/Airtel/Utilities/Paystack), income (Salary), transfer (bare/NIP/to person/structured merchant), internal-transfer (OWealth Withdrawal), bank-fee (commission/USSD/SMS/charges), tax (stamp duty/VAT/VATrecover), refund (OWealth deposit refund), interest (OWealth/INTEREST CAPITALISED), savings (Auto-save), loan-payment (EaseMoni/bare), unknown (empty/odd narrations + overclassification guards BUSINESS/FOODSTUFF). (3) DUPLICATES — same statement twice (pipeline re-import + modal e2e "0 imported · 3 already existing"), duplicate groups (reference/description/timestamps, same-amount-never-duplicate, distinct-timestamp repeats legitimate), same amount different transactions, same description different dates, partial previous import (statementIdentity 19 + statementRelations 17). (4) USER REVIEW — category editing, type changes (transfer→expense re-pools + resets mismatched category), exclude/include, bulk assign/exclude/include, review counter, duplicate Skip/Keep, cancel discards, confirm imports, Done resets (48 modal cases). (5) DATABASE — expenses/income with exact amount/date/note/category + importSource provenance; transfers/fees/taxes/savings/loan-payments NEVER booked; excluded rows never imported; only the first duplicate imported; re-imports double-book nothing. +2 NEW SEAM TESTS assert the PERSISTED payload (`budget-planner:state` via the one persistence seam — localStorage in tests, SQLite kv row on desktop): canceling a review leaves the persisted DB untouched (no statement descriptions, no account number in the raw payload) and a confirmed import is written EXACTLY ONCE (3 rows: expense 50,000,000 @2026-08-01 note "RENT PAYMENT" + provenance bank opay/statementDate 2026-08-01, income 90,000,000 → Salary, Utilities expense; every row expense-or-income, no transfers/movements). (6) REGRESSION — entire suite run clean: **65 files / 827 tests, 0 failed, 0 skipped**; no test disabled or todo'd (only `.skip` matches are in node_modules); all pre-existing failures were already investigated and fixed in 7A/7B (nothing left open). (7) REPORT — totals above; remaining issues: none known in the import feature; known limitations (by design): account header masking is heuristic (only 10-digit NUBAN-style runs in the first 12 rows), format detection requires header vocabulary (scan-only PDFs without text layer are unreadable), reference-less duplicate detection is conservative (medium confidence, user decides), auto-save/refund pairs are not linked to expenses. Gates: tsc/lint/full suite 827/build green | `CHANGELOG.md`, this file. |
| 2026-08-14 | **Bank statement privacy and local file security (prompt 7B — audit + fixes, no feature changes):** AUDIT — (1) RAW STATEMENTS: never retained — files are read in memory only (`file.text()`/`file.arrayBuffer()`), never written to disk (browser or Electron), never uploaded; the ONLY statement-derived persistence is the confirmed row's capped `importSource` provenance (reference ≤80, originalDescription ≤200, statementDate, bank — required by 5A for re-import detection) + the capped ledger note; no raw statements/PDF bytes/account numbers/balances ever stored (5A was already clean; now regression-locked by tests). (2) LOGGING: no renderer path logs statement content — the modal's catch blocks don't even console.error; main/preload logs are paths/status only (reviewed: `[desktop] sqlite: <userData file>`, smoke/updater/migration messages — no account numbers, references, names or statement content; `dbcheck.cjs` logs kv KEYS only and is a dev script, not shipped). (3) ERRORS: all user-facing failures are generic ("We couldn't read that file…", "Unsupported bank statement format…") — no stack traces, no parser internals, no statement contents (asserted by a new console-spy test). (4) DATABASE: single kv row holds AppState only; no raw statements; header account numbers shown masked ("Account •••• 6789") and never leave the renderer. (5) TEMP FILES: statements create NO temp files; FOUND + FIXED ONE REAL LEAK — the desktop atomic-write helpers (`desktop:fs:writeText`, `desktop:export` in `electron/main.cjs`, `writeBackup` in `electron/backups.cjs`) left `<target>.tmp` behind when writeFileSync/renameSync threw (partial copy of sensitive content — on export that's the full state JSON). New shared `electron/atomicWrite.cjs` `atomicWriteText(target, content, {mkdir?})`: temp file + atomic rename, best-effort temp unlink on ANY failure, target never half-written; all three sites now use it. (6) WINDOWS: renderer sandbox + contextIsolation + no nodeIntegration, dialogs/fs only through validated IPC — identical on a user's EXE; the temp-cleanup fix matters most on Windows (rename can throw EPERM). TESTS: +5 `electron/atomicWrite.test.ts` (success + no .tmp left; write failure → temp removed, target untouched; rename failure (target is a directory) → temp removed, existing content intact; mkdir option; invalid inputs never touch fs) +3 modal 7B cases (failed parse → upload stage restored, no file name/preview, close + reopen starts clean; console spies prove statement contents never logged on failure and the error UI exposes no internals; serialized app state never contains the statement's account number while the UI shows only "Account •••• 6789"). Suite 814 → 822. Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 privacy block, §6 security notes), this file. |
| 2026-08-14 | **Extensible bank statement format architecture (prompt 7A — new banks are pluggable, unknown formats are never guessed):** the importer no longer hardcodes GTCO/OPay. PARSER CONTRACT (`lib/statementTypes.ts`): `BankStatementParser` (id, label, headerScore, distinctiveTokens, minHeaderScore, parse), `BankParseResult`, `StatementRowError` — GTCO (`GtcoRowError`/`GtcoParseResult`) and OPay (`OpayRowError`/`OpayParseResult`) now implement it. REGISTRY (`lib/statementRegistry.ts`, NEW — the ONLY place the engine learns about banks): `BANK_PARSERS` (gtco + opay entries; distinctive tokens moved here from the pipeline) + `supportedBankList()` → "GTCO, OPay" in UI copy. DETECTION (`lib/statementPipeline.ts`, registry-driven): `detectStatementFormat`/`processStatement` take `parsers = BANK_PARSERS`; the two-way GTCO-vs-OPay winner logic is generalized to N parsers — score gap ≥ 3 → high confidence; close race decided by the contender with the uniquely-highest distinctive-column count → medium; distinctive tie → ambiguous. Never filenames, never bank names (unchanged from 3F). UNSUPPORTED OUTCOME (replaces the generic path): no registered bank matches → `status: "unsupported"` + `unsupportedReason` ("Unsupported bank statement format. We couldn't recognize this statement's layout — it matches none of the supported banks (GTCO, OPay). Nothing was read from the file…", or the ambiguous variant listing each bank's score) — zero transactions invented, `buildCandidates` fallback removed from the pipeline (helpers stay exported for their own tests). MODAL (`ImportStatementModal.tsx`): unsupported → explanatory upload-stage error (back to the drop zone); preview banner shows the registry `detectedLabel`; the old "General statement — review carefully" note is gone. EXTENSIBILITY GUARANTEE (test-proven): adding a bank = one parser + parser tests + one registry entry; a mock parser added to the registry detects its own header, runs through detection → parse → classification untouched, while GTCO/OPay behave exactly as before and unregistered headers stay unsupported. Tests: pipeline +8 (2 unsupported + 6 registry/extensibility), generic-fallback case removed (suite 807 → 814). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (lib list, §3.6 pipeline diagram, §4.1 preview), this file. |
| 2026-08-14 | **Classification accuracy improved against the real statement patterns (prompt 6B — analysis layer only; no import UI changes, no SQLite/schema changes, 6A learned rules untouched):** audited every pattern from the GTCO ("NIBSS Instant Payment Outward", "NIP TRANSFER", "Commission on NIP Transfer CHARGES", "VAT CHARGES", "SMS ALERT CHARGE", "VATrecover Partial Charges", "INTEREST CAPITALISED") and OPay ("Mobile Data", "MTN", "Airtel", "OWealth Withdrawal", "OWealth Interest Earned", "Auto-save to OWealth Balance", "OWealth Deposit (Transaction Refund)", "Stamp Duty", "VAT on Transfer Fee", "USSD Charge", "loan repayment", "Paystack Checkout", "merchant transfers") statements. TWO FIXES. (1) "VATrecover Partial Charges" was classified **bank-fee** via the "charges" keyword — added `vatrecover` to the tax rule (word-boundary safe, exactly like the existing patterns) so VAT-recovery charges are **tax**. (2) OVERCLASSIFICATION (§2): the expense fallback (`suggestExpenseCategory`) trusted the keyword system's substring matches — "BUSINESS" matched the transport keyword "bus" (startsWith → high) and was confidently classified **Transport**, needsReview FALSE; "FOODSTUFF GLOBAL" similarly became Food. Per §2 a confident-but-wrong category is worse than Needs Review, so the fallback now re-verifies the matched keyword against the narration with WORD BOUNDARIES (`matchesWord`) — "BUSINESS"/"FOODSTUFF" now stay **unknown → needs review**, while genuine word matches ("GYM MEMBERSHIP"→Health, "ONLINE DELIVERY"→Groceries) keep working. VERIFIED CORRECT ALREADY (regression-locked): every other pattern classifies as intended — generic "Transfer to JOHN DOE" stays a low-confidence **transfer** (never expense/category); structured "Transfer to DAVID OSAHON OGBEIDE | PalmPay" stays medium with provider detection; "Paystack Checkout" is an expense that NEEDS REVIEW (no category guess); type and category remain separate concepts (transfer/bank-fee/tax rows carry `categoryId: null` — they are never importable as spending or income). REGRESSION (§4): 27 new tests in `lib/__tests__/statementClassify.test.ts` (7 GTCO patterns, 12 OPay patterns, 3 type-vs-category separation, 5 overclassification guards) — full suite **780 → 807 tests / 64 files, all green**; tsc/lint/build green; no previously correct classification changed | `CHANGELOG.md`, `docs/ARCHITECTURE.md` |
| 2026-08-14 | **Learning from user classification corrections (prompt 6A — repeated corrections improve statement classification, local-only, no AI API):** new pure `lib/learnedRules.ts` + a `LearnedRule` model in AppState (schema **v4**; v3→v4 migration backfills `learnedRules: []`; validator round-trips + rejects bad kind/unknown category/strength < 1 — rules are exported, imported, validated and backed up with the state; the SQLite architecture needs no new table: one state row). HOW LEARNING WORKS (§7 answer): a "correction" is the user changing a review row's CATEGORY away from the suggested classification and then ACTUALLY importing that row (captured in-session in the modal; flipping back to the suggestion drops it; cancelled sessions, excluded rows, deduplicated and already-existing rows never teach anything — `planImport` now reports `importedIds` so learning is tied to the ledger write). Each correction keys on the row's most specific reliable signal (§2): provider ("MTN") > merchant ("DAVID") > full normalized description. One correction creates an inactive CANDIDATE (strength 1 — NEVER applied, §6 safety: no rule from one ambiguous transaction, no "John → Food" from a single occurrence, no keyword auto-extraction); a second matching correction activates it (`RULE_MIN_STRENGTH = 2`). A conflicting correction (same key, different category) re-baselines the rule (mapping replaced, strength reset, deactivated — the new direction needs two more consistent corrections; flip-flopping can never activate anything). PRIORITY (§3): `classifyTransaction(tx, categories, learnedRules?)` checks learned rules FIRST — user-created rule → built-in keyword rules → `suggestCategory` fallback → needs review; a learned match is high confidence, never needs review, reason `learned:<kind>:<key>`; direction guards mirror the built-ins (expense rule never fires on credits, income rule never on debits); a rule whose category was deleted never fires. USER CONTROL (§5): compact Settings → "Learned rules" panel (see, strength + Active/Candidate status, enable/disable toggle, re-target the category, delete with confirm). Tests: new `learnedRules.test.ts` (26 — repeated correction, provider rule, merchant rule, description rule, conflicting rules incl. flip-flop, disabled rule, deleted rule, deleted category, direction guards, signal priority, validation/migration) + classify integration, pipeline +3 (`processStatement` with learnedRules; inactive rules ignored; `importedIds`), modal +2 e2e (three sequential statements: two MTN corrections activate a provider rule that pre-classifies the third — and a cancelled/excluded correction never learns), store +3 (`learnFromCorrections` / `updateLearnedRule` / `deleteLearnedRule`), settings panel +6, validator v3→v4 migration + version-gate updates (suite 739→780, 64 files). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (classification priority + state/schema), this file. |
| 2026-08-14 | **Safe re-import and duplicate protection (prompt 5B — repeated imports can't double-book):** new pure `lib/statementIdentity.ts` (`matchExistingTransaction`) scores every preview row against the existing ledger with deterministic identity (prompt §2): bank, transaction date, amount, debit/credit direction, reference, normalized description — amount alone NEVER matches (two ₦1,000 transactions are not duplicates). VERDICTS (prompt §3): "Already imported" (confirmed — same bank+direction+amount+normalized reference; or, only for REFERENCE-LESS ledger rows, same bank+date+amount+normalized description), "Possible duplicate" (same bank+amount+direction with date OR description overlapping while the other detail is absent/conflicting — the canonical ambiguous case), "New", "Excluded" (existing flag). REFERENCE RULES (3E/5A-consistent, authoritative): a ledger transaction WITH a stored reference is matched only by that reference; a row whose reference DIFFERS from a ledger row's is a different transaction (banks distinguish same-day same-amount payments by reference) → "same date and amount, different reference" = NEW; only reference-less ledger rows fall back to date/amount/description signals, so the fallback stays unambiguous. USER CONTROL (prompt §4): possible duplicates are never silently discarded — default KEEP (importable), "Possible duplicate" badge + per-row Skip/Keep toggle (session-only `skipAsDuplicate` → counted as "possibleSkipped" in the done breakdown), warn banner ("N transactions may already be in your budget…"), new "Already imported" + "Possible duplicates" filter pills. RE-IMPORT (prompt §5): second import of the same statement = 0 new / N already existing; identity is checked BEFORE the category gate, so a plain re-import needs no category reassignment (button enabled with "Nothing to import"). FIXES: `addTransactions` was DROPPING `importSource` on write — provenance (the reference signal) is now persisted (store test added); the 5A date+amount+note dedupe is replaced by the shared identity module. Tests: new `statementIdentity.test.ts` (19 tests covering the prompt's 7 scenarios — exact duplicate, same amount different transaction, same description different date, same date and amount different reference, repeated statement import, partial previous import, duplicate internal transfer — plus bank/direction/ref-normalization/conflict-precedence edges); pipeline re-import suite +3 (possible default-keep, user skip → possibleSkipped, already-imported never blocks on category) (25→28); modal suite 38→43 (status chips + counts, skip/keep toggle + done breakdown "1 possible duplicate", default-keep imports everything, new filters, full re-import without reassignment; stored rows carry importSource); store +1 (provenance preserved) (suite 711→739). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1), this file. |
| 2026-08-14 | **SQLite statement import engine (prompt 5A — the import pipeline now writes to real persistence):** the confirm step writes through the EXISTING persistence path — `addTransactions` (one store update → one atomic `kv` UPSERT via `lib/storageAdapter.ts`; Electron: preload bridge → main → `electron/db.cjs`). No second transaction database is created (or needed): the SQLite DB holds a single `kv` table and the whole AppState JSON under `budget-planner:state` — transactions live inside that state, so a batch import is ONE atomic key-value write; a failure writes nothing (error + session kept, retry possible). SCHEMA: confirmed rows carry optional `importSource` provenance (`ImportProvenance` in lib/types.ts: `source: "statement-import"`, `bank` (gtco/opay/owealth/other/unknown), `reference?`, `originalDescription?`, `statementDate?` — reference/originalDescription capped at MAX_NOTE_LENGTH 200, sourceBank from the parser; `validateImportSource` + round-trip in `validateTransaction` so provenance survives validation/hydration; optional field → no schema version bump, v3 stays). RE-IMPORT DETECTION: `planImport` now takes the current ledger (`existing`); transactions WITH a stored reference match by reference only; reference-less transactions fall back to amount + date + note (the reference is authoritative — a referenced row never pollutes the fallback index, test-proven); matches are counted "already existing" and never written twice. DONE STAGE: five-way result line "N imported · N skipped · N excluded · N already existing · N failed" + "Skipped:" breakdown (money movement(s) / duplicate(s) / without a date / already existing / failed) — the plan is SNAPSHOTTED at click time (the store changes on write; recomputing afterwards would misreport every imported row as already existing); Import button enabled when all rows are categorized even if nothing is importable ("Nothing to import") so a re-import still shows the summary. Transfers/movements are still never booked as expenses; the description still becomes the note (capped). Tests: planImport re-import suite (reference match, amount+date+note fallback, referenced rows never falsely match the fallback, provenance preserved on inputs) (pipeline suite 21→25); validator provenance round-trip + malformed rejection (validate +2); modal five-way summary + re-import click-through (modal suite 36→38; suite 703→711). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 confirm + import + storage), this file. |
| 2026-08-14 | **Import confirm step (prompt 4D — the modal now writes to the budget):** `planImport` (lib/statementPipeline.ts, pure, recomputed at click time) turns the review session into ledger `TransactionInput[]` written via the existing `addTransactions` (single write, no new store surface). Rules: excluded rows stay out; money movements (transfer/internal-transfer/bank-fee/tax/loan-payment/savings) are NEVER booked (no ledger transfer type; they also can't be miscategorized) and count in the done summary as "money movement(s) skipped"; rows without a readable date are skipped ("without a date"); the first importable row of each duplicate group is kept, the rest counted as duplicates skipped (an excluded keeper promotes the next member); every ledger row needs a category — while any is missing the Import button is disabled ("Pick a category for N transactions before importing" title; button label = honest count of what will be written, "Nothing to import" when zero). Inputs: absolute minor amount (debitAmount → expense, creditAmount → income), type = category kind (validator-required), date = transactionDate (ISO), note = description capped at MAX_NOTE_LENGTH. Done stage: "N transactions added to your budget" + skipped breakdown (excluded/duplicates/money movements/no date/unreadable, only non-zero parts) + "Add another file" (reset to upload) + "Done" (close); ANY close (X/overlay/Escape/Cancel) now resets the session. Tests: modal suite 30→36 (blocked-until-categorized + title, imports reviewed rows with exact amounts/dates/notes into the store, movements never in the store, first duplicate only, excluded honoured, Done closes + next open starts fresh); pipeline suite 13→21 (ledgerKindFor never maps movements, planImport keeper/excluded-promotion/note-cap/unreadable) (suite 689→703). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 confirm + import), this file. |
| 2026-08-14 | **Transaction review and classification editing (prompt 4C — session-only edits, no database writes, no final import, no learning):** preview rows editable via `ReviewRow` (= classified transaction + `excluded` + `selected` flags) kept ONLY in the modal's component state (cancel/close/new file discards them — store stays untouched, Import still disabled). PER-ROW: the 4B kind chip became a TYPE SELECT (all `BankTransactionKind` options, e.g. "Transfer to JOHN DOE" → Expense → Food); the category select re-pools by chosen type (expense/income/refund/interest/unknown-by-direction); changing the type resets a now-mismatched category (old pool ≠ new pool → categoryId null) and any type/category/exclude edit clears the row's needsReview flag ("settled by the user"); per-row EXCLUDE/INCLUDE toggle button — excluded rows remain VISIBLE (muted + line-through description, "Excluded" badge), leave the stats (expenses/income/transfers/needs review), the Needs review filter and the Import count ("Import N transactions" = non-excluded), and can be re-included. BULK: per-row checkboxes + bulk bar ("N selected", Assign category… — applies only where the row's ledger kind matches the category kind, "Exclude", "Include", "Clear selection"). REVIEW COUNTER: "N needs review" summary stat counts down as rows are fixed; the Needs review filter still finds them. Tests: modal suite 23→30 (reclassify transfer → Expense + Food, type change resets mismatched category + re-pools income cats, exclude keeps row visible + "1excluded" + Import label updates + include restores, bulk category assign to 2 rows, bulk exclude/include, review counter 2→1) (suite 682→689). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 preview step), this file. |
| 2026-08-14 | **Bank statement preview screen (prompt 4B — session-only, no database writes, no final import, no learning system, no parser changes):** `ImportStatementModal` preview upgraded into a full review surface operating ONLY on the transient preview state (canceling/closing never touches the budget — same guarantees as 4A). PREVIEW HEADER: bank chip (GTCO/OPay/General) + MASKED account number when the statement header carries one (`findMaskedAccount`: 10-digit NUBAN-style run within the first 12 rows, cells containing `/`, `,`, `.` or currency marks excluded so amounts/dates are never mistaken; shown as "Account •••• 6789", full number never rendered) + statement period ("Aug 1, 2026 – Aug 5, 2026" from transaction dates, "Period unknown" when none) + currency symbol & code + `Statement summary` group (transactions / expenses / income / transfers / needs review counts) + detection reason line (scores, skipped/unreadable notes). TRANSACTION LIST: date · description (+ merchant/provider/channel/branch microcopy) · kind chip · ALWAYS-VISIBLE confidence badge (High/Medium/Low/Uncertain) · duplicate badge (RepeatIcon, rows in `report.duplicates` groups) · "Needs review" badge · direction icon (money in/out, aria-labelled) · signed amount · category select for ledger-able rows only (unchanged 4A rule). REVIEW STATES: high/medium/low/uncertain, needs review, transfer, internal transfer, duplicate — nothing is hidden. FILTERS: pill row with live counts (All / Expenses / Income / Transfers / Needs review / Duplicates; filter sets: expense kind; income+refund+interest; transfer+internal-transfer+savings; needsReview flag; duplicate-group ids) + "No transactions match this filter." empty state; no reusable filter component existed, so the modal's chip language was reused. Tests: modal suite 16→23 (header stats, period, masked account + full number absent, confidence badges, direction icons, transfer row without combobox, duplicate badges on every group row, per-filter filtering + counts + empty state) (suite 675→682). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 preview step), this file. |
| 2026-08-14 | **Bank statement import entry point — preview only (prompt 4A — no database writes, no auto-insertion, no learning system, no new parsers):** `ImportStatementModal.tsx` reworked from the old generic-candidate review into the pipeline entry point: Upload (drop zone + browse, CSV/XLSX/XLS/PDF via the existing `isSupportedFile`/`extractStatementRows`; future image/OCR slots into `SUPPORTED_EXTENSIONS` + accept list) → Processing (spinner, modal stays closable) → PREVIEW (detection banner — bank chip + `detectionReason` scores from 3F, "General statement" + "review carefully" note for unknown formats; summary counts — detected/needs review/rows skipped/rows unreadable; 3E findings — possible-duplicate warning + money-movement link note; per-row: date, description + merchant/provider/channel/branch microcopy, kind chip map (expense/income/transfer/internal-transfer/bank-fee/tax/refund/interest/loan-payment/savings/unknown), confidence label when not high, signed amount, "Needs review" badge, category select ONLY for ledger-able rows — expense/income/refund/interest + unknown-by-direction; transfers/fees/taxes/savings/loan payments get NO select so they can never be miscategorized as spending). Import button PRESENT BUT DISABLED with an explanatory title — `addTransactions` is no longer reachable from this modal, no "done" stage, footer note "This is a preview — nothing has been added to your budget yet". User-friendly error states: unsupported type, empty file, no readable transactions ("may not be a bank statement"), unreadable/password-protected file — no stack traces. Parser fix surfaced by the UI: OPay parser now falls back to Value Date for `transactionDate` when "Trans. Time" is absent (was: every row "Date?" + duplicates missed). Tests: modal suite rewritten 16 tests (upload/processing/preview/errors/preview-only: store stays empty, Import disabled, cancel discards; transfer row shows "Transfer" + low confidence + NO combobox; duplicates warning; General banner) + 1 OPay parser regression (suite 668→675). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§4.1 preview step), this file. |
| 2026-08-14 | **Statement processing pipeline (statement import, prompt 3F — ends at the PREVIEW: no budget/store/storage writes, no UI changes):** new `lib/statementPipeline.ts` — `processStatement({ cells, context, categories })` = format detection → bank parser (or generic path) → classification (3D) → relationships (3E) → `StatementPreview { detectedBank, detectionReason, transactions (classified), report, skipped, errors }`. DETECTION (`detectStatementFormat`): header-vocabulary scoring over the first 12 non-blank rows (min score 5) per bank — `gtcoHeaderScore(row)` / `opayHeaderScore(row)` now exported from the parsers (3B/3C) — near-ties broken by canonical distinctive columns (GTCO: originating branch/remarks/debits/credits/posting date… OPay: trans time/channel/balance after/description/debit/credit…); bank name and file name NEVER consulted (test-proven); gap ≥3 → high confidence, else medium; nothing ≥5 → unknown → GENERIC path (`buildCandidates` → `candidatesToNormalized`, direction via balance delta/income hints). GTCO/OPay branches carry `skipped` + per-row `errors` through to the preview. Computed cross-scores: GTCO header 14 vs 13, OPay header 14 vs 4–8, branch-less GTCO tie 13/13 → distinctive decides. Tests: 13 new (`lib/__tests__/statementPipeline.test.ts`): full GTCO pipeline (9 rows — rent/salary/NIP transfer to PalmPay/SMS charge/commission/VAT/interest/USSD/unknown deposit, needsReview, skipped bad-date row + error entry), full OPay pipeline (12 rows — mobile data/MTN, salary, transfer to DAVID|PalmPay, stamp duty, VAT on transfer fee, USSD+SMS charges, OWealth interest/auto-save/refund/withdrawal, unknown TRF; funding-pair link transfer↔withdrawal 50,000 high, movementIds, no duplicates), duplicates THROUGH the pipeline (same ref rows flagged), detection units (GTCO/OPay/minimal OPay/GTCO-without-branch tie-break/generic→unknown/blank cells/title rows ignored), generic fallback (RENT PAYMENT + SALARY via unknown path), safety (input cells never mutated, nothing ever gets status "imported"). Gates: tsc/lint/full suite (655→668)/build green | `ARCHITECTURE.md` (§2, §4.1 now shows the pipeline entry + detection + preview step), `CHANGELOG.md`, this file. |
| 2026-08-14 | **Transfer relationships and duplicate detection (statement import, prompt 3E — analysis only: no import UI, no auto-expenses, no deletes, no merges):** new `lib/statementRelations.ts` — `detectRelationships(transactions)` (runs on CLASSIFIED 3D rows, one source statement per session) returns `{ duplicates, links, movementIds }` and never mutates its input. DUPLICATES (`DuplicateGroup[]`): deterministic multi-signal matching; pass 1 = same normalized reference (trim/uppercase/collapse) + same amount + same direction → high ("same-reference"+"same-amount"+"same-direction" + same-date/time/description when also equal); pass 2 = same normalized description + same amount + same direction + same date → high when timestamps equal, medium when both timestamps missing; a DISTINCT timestamp on otherwise identical rows = legitimate same-day repeated payment (NOT a duplicate); different references disprove; empty descriptions only match via reference; groups union-merged, ids in statement row order; same amount alone NEVER a duplicate. LINKS (`TransactionLink[]`): movement row (savings/internal-transfer) + transfer row → `funding-pair` (e.g. "Transfer to DAVID | PalmPay" ↓ "OWealth Withdrawal (Transaction Payment)" same amount); two movement rows → `savings-movement` (auto-save in + withdrawal out); requires same effective amount + same direction + non-unknown direction; same date → high else medium; provider/date listed in signals; partial withdrawals (different amounts) deliberately NOT linked; movement+expense pairs NOT linked (conservative). MONEY MOVEMENT (`movementIds` + `isMoneyMovement`): savings/internal-transfer rows flagged as money movement, never spending. Signals exported as `RELATIONSHIP_SIGNALS` (same-reference/date/time/amount/direction/description/provider). Tests: 20 new (`lib/__tests__/statementRelations.test.ts`: identical amounts never duplicate, true duplicates by ref, reference-less duplicates high/medium, same-day repeated payments legit, matching amounts+times with different descriptions not duplicates, different refs disprove, 3-row group merge, empty descriptions, row-order ids, funding pair high/medium, partial withdrawal not linked, movement+expense not linked, credit-side pair, savings-movement link, movementIds, purity, nothing deleted). Suite 635→655. Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 lib listing, §4.1 pipeline) |
| 2026-08-14 | **Bank transaction classification layer (statement import, prompt 3D — analysis only, no budget/SQLite writes, no UI, no merges, no deletes):** new `lib/statementClassify.ts` — `classifyTransaction(tx, categories)` / `classifyTransactions(txs, categories)` attach a suggested kind + confidence + existing Category id + `needsReview` + `classificationReason` to each normalized transaction (new transient fields on `NormalizedBankTransaction`). Kind vocabulary = existing kebab-case `BankTransactionKind` (prompt mapping: internal_transfer→"internal-transfer", bank_fee→"bank-fee", tax_or_charge→"tax", loan_payment→"loan-payment"). ONE category system: resolution against the app's existing `Category[]` by name hint (exact then containment — "Utilities" matches "Bills & Utilities", expense hints only match expense categories, income hints only income); no hint match → `categoryId: null` + needsReview (never a guessed id). Conservative transfers: "Transfer to John Doe" → transfer (low when bare, medium when structured "X | Provider"), NEVER Food/Shopping/Transport; expense only with explicit merchant evidence — payment gateways (paystack/kora/moniepoint/flutterwave/interswitch), "merchant order", "checkout". Rule engine: `CLASSIFICATION_RULES` ordered table, first match wins (refund → loan-payment → tax → bank-fee → interest → savings → internal-transfer → merchant-gateway expense → transfer → income-salary → expense-utilities/groceries/food/transport/housing/entertainment/shopping/health), word-boundary matching ("SALARY" ≠ "JSALARYX"), expense rules skipped on credits / income rules skipped on debits; `PROVIDER_RULES` table (PalmPay, OPay, Paystack, Kora, Moniepoint, Flutterwave, Interswitch, MTN, Airtel, 9mobile, Glo, Access/Sterling/Wema/Fidelity/GTBank/Zenith/UBA/First/Union) + `detectProvider(description)`; parser-provided merchant/provider kept, gaps filled (recipient extraction, provider detection). Fallback: existing `suggestCategory` (categorize.ts, learned={}) for unmatched expense narrations, only confident keyword matches → expense medium. Confidence: high (explicit patterns), medium (gateways/suggestions), low (bare transfers), none (unknown → status stays draft). `needsReview` = unknown kind OR no category. No schema/store changes. Tests: 35 new (`lib/__tests__/statementClassify.test.ts`: fees/taxes/charges, interest/savings/loans/refunds, transfer conservatism, gateway expenses, expense patterns + category resolution, keyword fallback, credits-not-guessed, income, unknowns, provider detection, extensibility, batch purity). Suite 600→635. Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 lib listing, §4.1 pipeline) |
| 2026-08-14 | **OPay/OWealth bank statement parser (statement import, prompt 3C — no classification, no internal-transfer linking, no UI changes):** new `lib/opayParser.ts` — `parseOpayStatement(cells, {currency})` converts OPay rows straight into the normalized model (`NormalizedBankTransaction[]`, `sourceBank: "opay"`, type "unknown"/confidence "none"/categoryId null/status "draft"). Columns matched BY HEADER NAME ("Trans. Time", "Value Date", "Description", "Debit(₦)", "Credit(₦)", "Balance After(₦)", "Channel", "Transaction Reference"; currency markers `(₦)` stripped from header tokens; header located by vocabulary scoring over the first 12 rows, min score 4 = time/date + one more column) with the canonical export order as a positional fallback for headerless files. Full original narration preserved (capped) in both `description` and `originalDescription`; only obvious structured FACTS are lifted — `merchant`/`provider` from "|"-separated narrations ("Transfer to X | Y" → merchant X + provider Y, truncated names kept as-is; "Mobile Data | MTN | 3.2GB 2 Days Plan" → provider MTN, plan text stays for classification; "Third-Party Merchant Order | Kora Payments Network Limited" → provider only) — classification is NOT attempted. Channel → new transient `channel` field on `NormalizedBankTransaction` (statementTypes.ts); reference capped at 80 (`MAX_OPAY_REFERENCE_LENGTH`). Amounts reuse `parseAmountCell` — ₦0.70/₦0.75/₦1.88, large amounts, empty debit/credit cells → `undefined` (never forced to zero). Robustness mirrors the GTCO parser: invalid dates ("invalid date"), unparseable amounts ("unparseable amount"), dated rows with neither debit nor credit ("missing debit and credit") → skipped + per-row `OpayRowError[]`; missing descriptions fall back to "(no description)" (originalDescription undefined); undated footer rows silent; missing channel/reference columns degrade gracefully; missing amount columns skip all rows. Tests: 25 new (`lib/__tests__/opayParser.test.ts`: normal transfer + merchant/provider, truncated recipient, mobile data, OWealth withdrawal/interest/auto-save/refund, stamp duty, VAT on transfer fee, USSD charge, EaseMoni loan repayment, merchant payment, naira small decimals, large amounts, empty amount cells, invalid date, unexpected text, missing amounts, missing-description fallback, silent footer, missing columns, headerless positional fallback, missing amount columns, title-row + extra-column tolerance, multi-row independence). Suite 575→600. Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 lib listing, §4.1 pipeline diagram) |
| 2026-08-14 | **GTCO bank statement parser (statement import, prompt 3B — no OPay, no classification, no UI changes):** new `lib/gtcoParser.ts` — `parseGtcoStatement(cells, {currency})` converts GTCO rows straight into the normalized model (`NormalizedBankTransaction[]`, `sourceBank: "gtco"`, type "unknown"/confidence "none"/categoryId null/status "draft"). Columns matched BY HEADER NAME ("Trans. Date", "Value Date", "Reference", "Debits", "Credits", "Balance", "Originating Branch", "Remarks" + tolerant variants — "Transaction Date"/"Narration"/"Val Date"; header located by vocabulary scoring over the first 12 rows, min score 4 = date + one more column) with the canonical export order as a positional fallback for headerless files. Remarks is the primary description, preserved verbatim (capped at `MAX_ORIGINAL_DESCRIPTION_LENGTH`) in `originalDescription` for the classification layer; branch → new transient `originatingBranch` field on `NormalizedBankTransaction`; reference capped at 80 (`MAX_GTCO_REFERENCE_LENGTH`). Amounts reuse `parseAmountCell`, which now also accepts Excel-style leading-dot decimals (`.20` → 20, `.75` → 75; `.0` → null; `lib/statementImport.ts` regex widened). Robustness: one bad row never fails the import — invalid dates ("invalid date"), unparseable amount cells ("unparseable amount") and dated rows with neither debit nor credit ("missing debit and credit") are skipped and reported per-row in `GtcoRowError[]`; undated footer/balance rows are ignored silently; missing remarks/reference/branch columns degrade to reference/branch/"(no description)" fallback descriptions; missing debit+credit columns skip all rows gracefully. Tests: 21 new (`lib/__tests__/gtcoParser.test.ts`: normal debit/credit, NIP transfer, transfer between customers, commission/VAT/SMS charges, capitalised interest, leading-dot/comma/whole amounts, empty debit cells, transaction time, invalid date, unexpected text, missing amounts, silent footer, no-remarks fallback, headerless positional fallback, missing amount columns, title-row + extra-column tolerance, multi-row independence) + 1 new `parseAmountCell` case (suite 553→575). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 lib listing, §4.1 pipeline diagram) |
| 2026-08-14 | **Normalized bank transaction foundation (statement import groundwork — no bank parsers, no classification, no UI changes):** new `lib/statementTypes.ts` defines the canonical intermediate between a raw statement and the ledger — `NormalizedBankTransaction` (session id, transactionDate ± transactionTime, valueDate, description, capped originalDescription, transient reference, debitAmount/creditAmount split, balanceAfter, currency, sourceBank, merchant/provider, type, direction, confidence, categoryId, status, source row) + enums `BankTransactionKind` (expense/income/transfer/internal-transfer/bank-fee/tax/refund/interest/loan-payment/savings/unknown), `BankDirection` ("in"|"out"|"unknown", mirrors `ParsedAmount["direction"]`), `ClassificationConfidence` (high/medium/low/none, extends the high/low pattern from `categorize.ts`), `ImportStatus` (draft/classified/reviewed/imported/skipped), `BankSource` (gtco/opay/owealth/other/unknown). New `lib/statementNormalize.ts`: `candidatesToNormalized(candidates, {currency, sourceBank})` maps detection output onto the model WITHOUT interpreting the sign — debit/credit only set `direction`; `type` starts "unknown", confidence "none", categoryId null, status "draft" (explicitly NOT debit=expense / credit=income; classification decides meaning). `lib/statementImport.ts` gained `parseStatementDateTime` (ISO date + optional 24h time: "12/08/2026 14:32", "2026-08-12T09:05:07", "2:32 PM" → "14:32"). Layer is TRANSIENT by design (never written to AppState/localStorage/SQLite — privacy: references and raw narrations capped at `MAX_ORIGINAL_DESCRIPTION_LENGTH` and never logged; only final `Transaction` rows persist via `addTransactions`). GTCO/OPay parsers plug in ahead of `buildCandidates` and set `NormalizationContext.sourceBank`. No schema/store changes (no migration). Tests: 11 new (`statementNormalize.test.ts`) + 6 new `parseStatementDateTime` cases (suite 536→553). Gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `ARCHITECTURE.md` (§2 lib listing, §4.1 statement import pipeline) |
| 2026-08-14 | **Bank statement import is now a working 4-stage flow (Upload → Processing → Review → Import), replacing the scaffold modal:** drop or browse a CSV/Excel/PDF statement into `ImportStatementModal.tsx`; parsing is fully client-side in `lib/statementImport.ts` — `parseCsv` (RFC-4180-ish: quoted fields, escaped quotes, embedded commas/newlines, plus an unquoted comma-thousands merge pass for values like `500,000.00`), `parseAmountCell` (CR/DR/DB markers, parens, leading minus, `₦$€£¥` prefixes, minor units), `parseStatementDate` (ISO, dd/mm ambiguous → day-first for NGN, named months, time suffixes), `rowsFromPdf` (lazy `import("pdfjs-dist")`, worker bundled via `new URL(..., import.meta.url)` — Turbopack emits it as a hashed static asset — x-gap column split with a `min(max(16, smallestGap*2.5), 40)` threshold), `rowsFromExcel` (lazy `import("xlsx")`, SheetJS 0.20.3 from the CDN tarball; `sheet_to_json` header:1 raw:false), `extractStatementRows` (routing by extension), `detectColumns` (header-vocabulary scoring for date/debit/credit/amount/balance/description + balance-delta direction inference), `buildCandidates` (skips rows without date/amount, reports `skipped`), `detectCategory` (kind-filtered pool, longest-keyword + direct-name matching, `KEYWORD_ALIASES`). Review stage: per-row date/description/signed amount/type chip/compact category select, warn text "N transaction(s) needs a category" / "missing a date", muted "N rows skipped", Import disabled until every transaction has a category and a date, nothing written until confirm — then a single bulk write via the new `addTransactions` store action (prepend in order, `createId()` each, one `set()`), success toast + "N transactions imported" done screen, "Choose another file" resets. `PlannerView` remounts the modal with a `key={importOpen ? "import-open" : "import-closed"}` so every open starts clean. Tests: 35 lib (`statementImport.test.ts`: CSV/amounts/dates/CR-DR/balance inference/headerless/skips/categorization/PDF-line grouping + mocked xlsx/pdf routing + new comma-thousands merge), 10 component (real CSV parse end-to-end: copy, unsupported-file via drop, drag-drop, processing state with mocked pending promise, detection, uncategorized warning + disabled Import, category change, save-nothing-before-confirm, full import into store with amounts/type/date/note/categoryId, done screen, cancel discards), 2 store (`addTransactions` single-write prepend + empty-list no-op). Tests 337→385; gates: tsc/lint/full suite/build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md`, `PROJECT_SPEC.md` (non-goal rewritten: client-side import convenience, not account aggregation) |
| 2026-08-14 | **"Import Statement" action added to the Planner header (scaffold only — no parsing, superseded by the working flow above):** a subtle outlined secondary button (upload icon, `UploadIcon`) sits alongside Add Expense/Add Income (both untouched) and opens a dedicated **Import Bank Statement** modal (`ImportStatementModal.tsx`): dashed drop-zone panel (upload icon tile, "Choose a statement file", "Drag & drop or browse a CSV export from your bank", CSV chip), a muted note that parsing isn't available yet, footer Cancel + a disabled Import button, standard `Modal` (`closeButton`, border+`shadow-card`, Escape/focus-trap/scroll-lock unchanged). No file reading, no schema/store changes, no transaction writes. New `ImportStatementModal.test.tsx` (5 tests: closed renders nothing, scaffold copy, disabled Import, Cancel closes, close icon closes). Gates: tsc/lint/tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md`, `PROJECT_SPEC.md` (non-goal: CSV ingestion now noted as scaffold-only until parsing lands) |
| 2026-08-13 | **Fixed the Next.js runtime crash in `app/layout.tsx` ("Encountered a script tag while rendering React component") and a resulting hydration failure ("server rendered HTML didn't match the client", seen around the ToastHost container).** Two attempts with `next/script` `<Script strategy="beforeInteractive">` (child of `<html>` and inside `<body>`) both failed in the Electron dev window: React 19 rejects sync/defer scripts outside the root `<head>` ("Cannot render a sync or defer <script> outside the main document", "<html> cannot contain a nested <script>") and the `self.__next_s` push script next/script serializes inside the body shifts hydration matching — the client tree then regenerates, React *creates* a script element (dev-mode script-tag warning at `createInstance`), and the offset reports a misleading "aria-live" diff. Final fix follows the official Next.js 16 guidance (`node_modules/next/dist/docs/01-app/02-guides/preventing-flash-before-hydration.md`): the theme bootstrap is a REAL inline executable `<script type="text/javascript">` in the root layout `<head>` (new `components/ui/InlineScript.tsx`), running synchronously as the browser parses the HTML — before first paint and before hydration — so `THEME_BOOTSTRAP_SCRIPT` (theme/accent/animations bootstrap in `lib/theme.ts`) still prevents theme flicker. `InlineScript` renders `type="text/plain"` on the client, so React treats the node as a data block (never creates/executes a script in the client tree — no dev warning) and `suppressHydrationWarning` accepts the type difference; the `<html>` `suppressHydrationWarning` (genuinely required — the script flips `data-theme` before hydration) and `data-theme="light"` stay. Script contents untouched (7/7 theme tests still execute it in jsdom). Also made the first-run name modal hydration-safe: the display-name store now starts `name: null, ready: false` on BOTH server and client, and AppShell seeds the saved name at mount — so the modal never renders in the server HTML and its presence can never diverge between SSR and hydration (previously a saved name would have made the modal close client-side against server HTML that rendered it open). `NameSetupModal` opens only when `ready && name === null` (+1 test for the gate). Empty `<head>` element removed. Verified: served HTML (dev + static export) carries the inline bootstrap script with no `self.__next_s` push; dev + Electron browser console fully clean (zero `[browser]` error lines across two fresh sessions); gates: tsc/lint/full suite 488/build green | `CHANGELOG.md`, `ROADMAP.md` |
| 2026-08-13 | **Budget attention bar made responsive (CSS/layout only — no redesign, wide-width appearance and both interactions untouched):** the alert now declares itself a container (`@container`; it spans the full content column so its own width tracks the window). Collapsed, the second budget message is `hidden @4xl:flex` — at container widths < 896 px (≈ < 1200 px window with the 240 px sidebar) only the most important message shows alongside "View more"; at ≥ 896 px the up-to-2-message layout is byte-identical to before. Every message span is now `min-w-0` and its text span `min-w-0 truncate`, so long budget text shrinks and ellipsizes instead of overlapping the title, "View more" or the "Review budgets" button or forcing the alert wider than the viewport (the title, "View more" and the button stay `shrink-0`; the button keeps `ml-auto`, so it always renders fully and clickably, and messages yield space to it). No JS resize listeners — pure container queries. "Review budgets" (`/?focus=over` + scroll) and "View more"/"View less" (expand/collapse all issues, wrapped layout) behave exactly as before. New test: collapsed 2nd message carries `hidden @4xl:flex`, drops it when expanded, root has `@container` (jsdom runs class-level assertions since CSS media/container queries are not evaluated). Tests 486→487; gates: tsc/lint/full suite 487/build green | `CHANGELOG.md`, `ROADMAP.md` |
| 2026-08-13 | **First-install name setup (new, presentation layer only):** on first launch with no saved display name, a small non-dismissable `NameSetupModal` (size `sm`, standard surface/border/shadow + teal primary + teal focus) overlays the shell: heading "What should we call you?", supporting copy "Tell us your name so we can make your Budget Planner feel a little more personal.", one text input (placeholder "Enter your name", `aria-label`, auto-focused, 60-char cap) and a **Continue** button disabled until the trimmed value is non-empty (Enter submits; whitespace-only never passes; trimmed before save). Saving writes the name through the single persistence seam as an app-level preference (`settings:display-name` — `lib/displayName.ts` `loadDisplayName`/`saveDisplayName`, SQLite-backed on desktop, localStorage in the browser; **no AppState schema involvement, no migration, no user/account model**), a tiny `store/useDisplayName.ts` zustand store (seeded at mount in AppShell — hydration-safe, see the layout-fix row) flips to the saved name, and the modal is gone for good — it only opens while `ready && name === null` (AppShell renders it once, above ToastHost; closing is intentionally impossible until saved, so it can't reappear on every launch). The Planner hero greeting (the only place the app addresses the user) now renders the saved name — "Good afternoon, Daniel 👋" — instead of the hardcoded "Archer" placeholder, with a nameless fallback ("Good afternoon 👋"). Tests: `lib/__tests__/displayName.test.ts` (4 — null default, roundtrip, trim-on-save, whitespace-only = no name), `NameSetupModal.test.tsx` (7 — copy + disabled Continue, whitespace keeps disabled, typing enables, Enter submits trimmed + closes, Continue submits trimmed + closes, skipped when a name exists, SSR `ready` gate keeps it closed pre-seed), Hero suite +1 (nameless greeting) and the greeting test now seeds "Daniel". Gates: tsc/lint/full suite 486/build green | `UI_UX_SPEC.md`, `CHANGELOG.md`, `ROADMAP.md` |
| 2026-08-13 | **Reports page polished into the analytics companion to Planner (presentation only — data, calculations, routing, report functionality and page dimensions untouched):** the header (title, subtitle, Compare month, month selector, Export) and the teal/blue curved-line decoration are unchanged (already present). Monthly overview KPI cards keep the four-column layout and sizing but are refined into a coherent group — per-card `shadow-none` (hairline border only) — and where previous-month data exists each card now shows the change directly beneath the primary value: "↑/↓ ₦X from last month" for income/expenses/net savings (green when the movement is positive, restrained red when negative — expenses falling is green) and "↑/↓ N% from last month" for the savings rate (whose duplicate % chip was dropped); the "No prior month to compare yet" / "Record income to start comparing" / "Spending more than you earn" fallbacks are retained exactly (new `DeltaLine` helper; `formatMoney` + `Currency` reuse). Financial insights: the main card gets a deliberate treatment — very subtle teal-white tint (`bg-brand-500/[0.03]`), `border-brand-500/20`, no shadow, icon tile raised to the h-10 convention, strong headline with explanation below; the three supporting observations become compact bordered surface cards with their contextual icons and red/amber/green semantic tints. NEW **Spending breakdown** section sits directly below Financial insights: two columns on lg — LEFT the existing "Income vs expenses" 6-month bar + net-line chart; RIGHT a "Spending by category" card (this month's top-5 horizontal category bars via the shared `ExpenseBreakdown`, existing category colors, total + "N more categories not shown" footer, polished empty state when nothing is recorded). NEW **Savings trend** section below it: the existing quiet thin-line `SavingsChart` (previously buried in Detailed breakdowns). The old standalone "Income vs expenses" section and the duplicated SavingsChart + "Spending this month" cards were removed from Detailed breakdowns (charts moved, not deleted). All remaining sections (Spending trend, Category analysis, Cash flow, Forecast, Recommendations, Detailed breakdowns) and their charts/spacing (`gap-6` section rhythm, `lg:grid-cols-2` chart grids) are untouched. New tests: `MonthlyOverview.test.tsx` (5 — green income rise + shadow-none chrome, green expenses fall, red expenses rise, no-prior-month fallback, % delta on the rate card), `FinancialInsights.test.tsx` (2 — tinted card classes + savings headline, three bordered supporting rows), `ReportsView.test.tsx` (3 — section order with the new sections, Spending by category card for the current month, Spending this month gone). Gates: tsc/lint/full suite 475/build green | `UI_UX_SPEC.md`, `CHANGELOG.md`, `ROADMAP.md` |
| 2026-08-13 | **To-Do page reworked into a focused task/action screen (presentation only — functionality, routing, data model, task behavior and sidebar untouched):** the header row (title, description, month selector) keeps its exact sizing/positioning; a compact muted summary line now sits beneath it — "N item(s) needs attention · 0 completed · N high priority" (attention = danger/warn items, high priority = danger items; no completed state exists so that count is 0). The oversized single `Card` is replaced by an intentional task section titled "To-do · <month>" with a compact dot-separated filter control (All • Needs attention • Completed; All active; purely client-side — the app has no task completion state, so Completed always shows its own subtle "Nothing completed yet." empty note). Tasks render as compact horizontal rows (white/dark-navy surface, subtle border, `rounded-xl`, consistent `p-3.5` padding): tinted icon tile (`h-9`, `rounded-md`), title + small priority chip, muted description beneath, action far right — "Resolve" is now a subtle teal ghost button (`bg-brand-500/10`, brand text, same focus ring) still linking to the same resolving href. Tone treatments stay subtle: danger = soft red/pink tint + red icon + "High priority" chip, warn = soft amber tint + "Medium priority", success = soft green tint + "On track", neutral = neutral surface + "Normal". Below the list a subtle dashed-border section reads "You're all caught up." / "Nothing else needs your attention this month." with a small teal "View Planner" action (shown whenever no additional unresolved tasks exist — replaces the old single-card empty state). The existing teal/blue curved-line decoration and radial tints already present on the page are kept as the identity band. Dark mode uses the existing tokens (deep navy page, lighter navy surfaces, blue-gray borders, teal primary, near-white text, restrained red/amber). Rows wrap instead of overflowing at narrow widths. New `TodoView.test.tsx` (4 tests: header + summary + over-limit row with chip and Resolve href, all-caught-up section below the list, All/Needs attention/Completed switching with aria-pressed, empty-month zero counts + neutral no-data row). Gates: tsc/lint/full suite 465/build green | `UI_UX_SPEC.md`, `CHANGELOG.md`, `ROADMAP.md` |
| 2026-08-13 | **Upcoming page reworked into a forward-looking planning screen (presentation only — no functionality, data model, routing or header changes):** below the unchanged header sits a compact 4-tile summary row (2 cols mobile → 4 desktop, mirroring the Planner card language — soft white surface, subtle border, rounded-xl, muted blue-gray labels): "Upcoming expenses" (total + N planned), "Next due" (amount + relative countdown + date; brand-tinted icon; "—"/"Nothing scheduled" when empty), "This month" (total + count) and "Recurring" (count) — from new pure helpers `upcomingSummary()` + `filterUpcoming()` in `lib/upcoming.ts`. The Disclosure preview list is replaced by an always-open timeline section: "Upcoming" heading with right-aligned controls and a subtle left rail (`border-l-2 border-border/60`) with small muted dots on each date-group header (Overdue / Today / Tomorrow / This week / Next week / Later — the existing grouping, counts and group totals kept); rows keep their exact compact layout (icon, name, category + recurring chip, countdown chip, date, amount, actions menu, edit-on-click) and the soonest expense gets a very subtle teal tint + border (`accent` prop). New controls above the list (client-side only, existing data): **All / This month / Later** filter pills (active = subtle brand tint, `aria-pressed`) and a **Soonest ↔ Latest** sort toggle (Latest renders a flat descending list with the same rows). The empty area below the list is now intentional: "You're clear for the rest of the month." / "Nothing else is scheduled." + small "+ Add upcoming expense" secondary action (replaces both old empty cards; the Paid section stays below). The upper-right teal/blue curved-line decoration was kept and raised to sit behind the header/summary. No horizontal overflow at any breakpoint (wrapping controls, truncation, 2-col grid). The long-standing date-flaky UpcomingView test ("counts the items in each date group") was rewritten deterministically (all items at the same relative offset → always one group) as part of the test rework: UpcomingView tests 5→9, upcoming lib tests +6; full suite 451→461 — now 100% green with no known flakes. Gates: tsc/lint/full suite 461/build green | `CHANGELOG.md`, `ROADMAP.md` |
| 2026-08-13 | **Add Expense modal refined to the reference (presentation only — fields, positions, dimensions, data model, submission and validation untouched; no Payment method / no Receipt upload):** the modal body gets the reference container treatment — the panel swaps the heavy `shadow-pop` for a subtle full-opacity 1px `border-border` + `shadow-card` (scoped via a new optional `panelClassName` on the shared `Modal`; every other modal keeps its current look), the heading is now the per-kind action ("Add Expense" / "Add Transfer" / "Add Income", "Edit transaction" when editing — was "New transaction"), a close icon renders top-right (new optional `closeButton` on `Modal`, same `onClose`; header gains `pr-20` so long titles never collide), and the header gets a little more breathing room from the first form row (`mt-2`). Tabs are restyled from a filled segmented pill to the reference underline style — Expense/Transfer/Income keep their order and `aria-pressed`; the active tab is a 2px `border-brand-500` teal underline with ink text, inactive tabs are muted blue-gray with a transparent underline (subtle `border-border/70` divider underneath, `-mb-px` overlap). Form rows get slightly more vertical rhythm (`gap-y-5`, `gap-x-4` unchanged). The Amount input now shows the configured currency symbol as a subtle left-edge prefix (new optional `prefix` prop on the shared `Input`, `pl-11` keeps the value separated; `currencySymbol()` exported from `lib/money.ts`). The note becomes a 3-row `textarea` — same `maxLength` 200 cap, placeholder now "Add a note...", slightly more internal padding (`pt-3 pb-7 px-4`), and a subtle `0/200` tabular counter aligned bottom-right (aria-hidden; `note.length/MAX_NOTE_LENGTH`). Select chevron, date field, labels, borders, teal focus rings, footer buttons (already bottom-right, matching heights) all already matched and are untouched. 4 new form tests (heading "Add Expense", close icon calls onClose, "$" prefix with default USD, 0/200→9/200 counter while typing). Gates: tsc/lint/TransactionForm 17 tests/build green; full suite 450/451 (pre-existing date-flaky UpcomingView failure unchanged) | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation rows refined to the reference palette + layout (presentation only — data, ordering, calculations, functionality, row/card dimensions, typography, bar thickness untouched):** every `BudgetRow` now has an extremely subtle pastel category-tinted background (Transport cream/yellow, Loan pink/lilac, Edi red/pink, Misc cool gray/blue, Essentials pink/red, Internet peach/orange, PalmPay light blue — 8–10% alpha, never saturated; unknown categories fall back to their stored-color tint). The thin progress bar and percentage carry the stronger per-category accent (Edi red, Internet orange, Essentials red, Transport teal, PalmPay blue, Loan teal, Misc muted gray) via a new optional `fillColor` prop on `ProgressBar`; over-budget rows keep the existing warn/far-over danger treatment. Row layout inside the unchanged 40px height now follows the reference two-line structure: icon far left → name + subtle neutral Medium badge close together; below, the bar starts at the same left alignment as the name, percentage immediately right of it, "spent · left" after, budget amount pinned far right — consistent alignment across all seven rows, no overlaps. Unintended floating "Priority: Medium" tooltip removed (`PriorityBadge` no longer sets the `title` attribute; the Medium badge itself remains). Palette in `lib/accents.ts` (`budgetRowTreatment`, name-keyed with stored-color/brand fallback) + 3 new accents tests. Gates: tsc/lint/planner suite 95 + accents 8/build green; full suite 446/447 (pre-existing date-flaky UpcomingView failure unchanged) | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **"Review budgets" in the Budget attention bar now actually opens Budget Allocation (bugfix — button appearance/size/position/text and banner untouched):** the button was a plain `next/link` to `/?focus=over`; its client-side navigation rides the App Router, which is a no-op in this static-export Electron build (same reason `router.push` is documented unsupported), so the click did nothing. The same proven handler the working hero "Review Budget" button uses — a direct DOM `scrollIntoView` to `#budget-allocation` (smooth, block-start) — is now attached directly on the rendered link element, so it works with mouse and keyboard (Enter). `href="/?focus=over"` and Link semantics are preserved for router-capable contexts (middle-click / open-in-new-tab); if both fire they converge on the same section. The `next/link` mock in `BudgetAttentionBar.test.tsx` was corrected to compose the component's real `onClick` instead of clobbering it (previously the tests passed while the real handler could never run); one test added (clicking Review budgets calls `scrollIntoView({ behavior: "smooth", block: "start" })` on the Budget Allocation section). Gates: tsc/lint/BudgetAttentionBar 9 tests/planner suite 95/build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **"Split red donut" root-caused: duplicate category colors, not rendering — fixed with presentation-only donut color de-duplication (data/business logic untouched):** data inspection of the user's actual state showed the four categories internet, Essentials, Misc, and Loan ALL stored `#ef4444` (red), so the (already correct) one-arc-per-category rendering legitimately drew four red arcs — reading as "the red category split into pieces". `BudgetList` now gives each repeat donut color a distinct reserve hue (purple, orange, teal, pink, …) when building `donutSegments`; stored categories, the budget list, recommendations, and all values are untouched, and every category stays ONE continuous arc. Also added `DonutChart.test.tsx` (5 tests: one path per segment, no internal M/A duplication, same-color categories stay separate arcs, real angular gaps 5–30° between all adjacent pairs, all-zero renders nothing, accessible label). Gates: tsc/lint/planner+charts tests (97)/build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Donut segments rendered as single continuous arcs — split-segment bug fixed (presentation only — proportions/colors/data/functionality untouched):** the previous dash-based rendering (`strokeDasharray` on `<circle>` elements) could emit one category as several pieces when its dash crossed the SVG path seam at 12 o'clock — the red (first) segment was visibly split into separate red arcs. `DonutChart` now computes a startAngle/endAngle per category from its proportion and renders EXACTLY ONE `<path>` arc per category (no dash patterns, no subdivision, no rotation change); the seam now falls inside an inter-category gap, so no arc can ever straddle it. The angular sweep is reduced only at each arc's own start/end boundaries (still visibleGap + strokeWidth total, i.e. ~5° visible ≈ 7px at mid-radius), keeping the real gaps between adjacent categories (red→yellow→blue→purple→green→teal→red) and rounded ends (`strokeLinecap="round"`). Hover/active highlight (stroke-width +4) preserved; the old dasharray mount animation is gone. Small-segment guard retained (`gapPerSide` capped at length/3). Diameter (180px), ring thickness, colors, proportions, centre text unchanged. Gates: tsc/lint/planner tests (92)/build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Donut segment separation actually fixed (presentation only — proportions/colors/data/functionality untouched):** the previous 4° gap was invisible because the rounded caps (strokeLinecap round, 18px stroke) extend 9px past each dash end — the ~6px gap was completely covered and adjacent segments kept overlapping. `DonutChart` now computes the sweep reduction as `visibleGap + strokeWidth` so the caps' combined reach is inside the removed arc and the background genuinely shows through the requested visible gap (5° ≈ 7px at mid-radius, ~6–8px across the ring, consistent around the whole circle: red→yellow→blue→purple→green→teal→red). Segments stay independently rounded at both ends; tiny segments are guarded (`gapPerSide` capped at `length/3`) so no category ever vanishes. Diameter (180px), ring thickness, colors, proportions, centre text, and rotation unchanged. Gates: tsc/lint/funding tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Donut segment gaps widened to the reference (presentation only — proportions/colors/data/functionality untouched):** the Budget Allocation donut's inter-segment gap grew from 3° to 4° (~4.7px→~6.3px visible at the outer edge, inside the 5–7px target) so every category is clearly separated by a small, consistent radial gap around the whole ring (red→yellow→blue→purple→green→teal→red). Segments stay independently rounded at both ends; diameter (180px), ring thickness, colors, proportions, centre text, and card layout unchanged. Implemented via a new optional `segmentGapDegrees` prop on `DonutChart` (default 3°, so other consumers are unaffected) set to 4 in the Budget Allocation card. Gates: tsc/lint/funding tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation donut enlarged and statistics moved lower (presentation only — data, calculations, colors, functionality untouched):** 150px→180px donut (175–185 range), still centered with comfortable side whitespace; ring thickness/rounded caps/3° gaps kept, centre text unchanged (13/18/12/12px, 4px line gaps, clear breathing room to the ring). Donut→statistics gap increased 16px→28px (statistics visibly lower, no visual competition); statistics rhythm flattened to a uniform 12px between every element (label row → bar → label row → bar) with labels/amounts/percentages aligned as before and 6px bars; card height grows naturally (no compression, no excessive bottom space). Right list, recommendations panel, and everything else untouched. Gates: tsc/lint/funding tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation donut card rescaled to a compact in-card chart (presentation only — data, calculations, colors, functionality untouched):** 160px→150px donut (145–155 range), sitting in the upper-center of the card with generous side whitespace (upper ~55% of the card; statistics lower ~35%; card ~300–330px tall with ~24px top / ~20px bottom padding). Centre content significantly reduced and tightly grouped (4px line gaps, visible empty space between text and ring on all sides): "Budgeted" 13px, committed amount 18px bold KPI, "of ₦X" 12px, "N% allocated" pill 12px. Ring thickness unchanged (proportional, max(18, size/10)), rounded caps and 3° gaps kept. Donut→statistics gap 16px; Allocated/Remaining rows stay 14px with bold percentages pinned far right; ~10px label→bar, ~14px between the two bars. Right list, recommendations panel, and everything else untouched. Gates: tsc/lint/funding tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation donut card rescaled to the reference proportions (presentation only — data, calculations, colors, functionality untouched):** 185px→160px donut (150–165 range), no longer dominating the card; card keeps ~24px horizontal padding with ~24px top padding and small bottom padding. Centre content tightened to consistent 4px line gaps with breathing room: "Budgeted" 14px, committed amount 20px bold KPI, "of ₦X" 13px, "N% allocated" pill 13px (no crowding of the segments). Donut→statistics gap 20px; Allocated/Remaining rows at ~14px with bold percentages pinned far right; ~10px between each label row and its 6px progress bar; ~12px between the Allocated bar and the Remaining row. Right list, recommendations panel, and everything else untouched. Gates: tsc/lint/funding tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation chart card scaled up to match the reference's visual weight (presentation only — data, calculations, colors, functionality untouched):** the left donut card is now a proper dashboard analytics card instead of a miniature widget. Donut enlarged 140px→185px (dominant visual element; stroke scales with it), centre text upgraded — "Budgeted" caption, committed amount as the main KPI (24px bold), "of ₦X" beneath, "N% allocated" pill (all clearly readable). Card padding 16px→24px; gap between donut and the Allocated/Remaining statistics increased (12px→24px); row typography raised from 12px caption text to comfortable 13px with the percentages as bold values pinned far right; 6px progress bars kept full card width. Right list, recommendations panel, and everything else untouched. Gates: tsc/lint/funding tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation content densified to the reference (presentation only — calculations/data/actions untouched):** the two-column allocation area is now a dense dashboard module. LEFT card: padding p-5→p-4, donut stays 140px at the top with the Budgeted/of/pill centre, Allocated/Remaining rows moved up (card gap 4→3, row gaps 2.5→2 / 1.5→1). RIGHT card (`BudgetRow` rebuilt): rows are ~40px (was min-h-16) with padding p-4 card and gap-1 list; each row is now a single two-line block — small 28px icon, category name + inline small priority badge on the left with the budget amount far right; below, a thin subordinate progress bar with the percentage beside it, and tiny "spent · left" secondary text; subtle tinted background per row via the category color at low alpha (over-budget rows keep the existing warn/danger tint), hover reveals small h-6 Edit/Delete actions. Rows are now ordered by **category list order** (Edi, Transport, Internet, Essentials, PalmPay, Misc, Loan) instead of priority — the donut segments follow the same order. No new information, no calculation changes. Gates: tsc/lint/planner tests (92) green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation donut card refined to the reference (presentation only — calculations/data untouched):** the left card of the allocation grid is now a white card (subtle border, small radius) with a substantially smaller 140px donut (was 220px default) centered near the top, category colors preserved. The donut centre shows the reference hierarchy: "Budgeted" label → committed amount → "of ₦X" (allocatable income) → small "N% allocated" pill (hidden when there is no income). Below the donut sit two compact progress rows replacing the old funding caption block: "Allocated ₦X N% + progress bar" and "Remaining ₦X (100−N)% + progress bar" (thin bars); the old "Allocated X of Y allocatable · Remaining to allocate Z" caption, "Over allocated" header and single warn-toned bar are gone. Over-allocated edge case preserved: true ratio shown ("130% allocated"), allocated bar clamps to 100%, remaining shows ₦0.00 / 0%, plus a short warn caption. `AnimatedMoney` centre value replaced by static formatted text; funding tests rewritten for the new structure (centre pill "74% allocated", "of $3,500.00", allocated/remaining bars 74/26, 97/3, 130→clamped-100/0). Right-side category list untouched. Gates: tsc/lint/planner tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget Allocation reworked to the reference (presentation only — data, calculations and actions preserved):** the section no longer collapses — it is an always-open compact card. Header row: "Budget Allocation" title + "N budgets" pill badge beside it + outlined "New budget" button far right (was a Disclosure header with a collapsed preview + "View all"). Directly below the header sits the compact warm/cream recommendations panel (`BudgetSuggestions` rewritten): "A few budgets are over their limits", one row per over-limit budget (category-colored chip indicator + "Name → increase limit by X or reduce spending" when covered by remaining income, "Name → reduce spending by X to stay within limit" otherwise; clicking a row still opens the budget edit form — the old Adjust action), and a "View recommendations →" link (bottom-left) to `/reports`. Below it the main allocation area is a compact two-column grid: LEFT — donut chart card (committed in the centre) with the existing funding caption (Allocated X of Y allocatable · % / Remaining to allocate Z or over-allocated warning + progress bar); RIGHT — card with all budget rows (BudgetRow unchanged). The old large "N budgets over their limits" adjustment panel (per-budget coverage notes + Adjust buttons) is gone. Focus flows keep their behavior minus expansion (rows are always visible): `focus=over` scrolls + highlights over rows, `focus=create` scrolls + opens the New budget form, `planner:focus-budget` highlights + scrolls to the row and its allocation slider. Past-months list and the Allocate-remaining sliders (AllocationPanel) remain below, unchanged. New `BudgetSuggestions.test.tsx` (6 tests); BudgetList focus/funding tests and NeedsFundingSection test updated for the always-open layout. Gates: tsc/lint/435 tests green (1 pre-existing date-flaky UpcomingView failure unchanged); build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Planner summary cards updated to the reference (presentation only — underlying calculations/data untouched):** the four Summary KPI cards (Expected Income / Remaining / Budgeted / Savings Rate) adopt the reference's compact vertical hierarchy — icon → label → value → supporting text → comparison row pinned to the card bottom (`mt-auto`), top-aligned content instead of `justify-between` (removes the stretched look), value scale lowered from kpi-hero to kpi-secondary (slightly smaller). New optional `MetricCard` props (`compact`, `labelExtra`, `comparison`); Reports `MonthlyOverview` unchanged (defaults preserve the old layout). Card copy: Expected Income keeps Edit chip + green arrow icon, support "% received", green "↑ ₦25,000 from last month"; Remaining keeps target icon + progress bar, support now "% of allocatable income", red "↓ ₦17,500 from last week"; Budgeted gains a small info icon beside the label, support now "% of allocatable income" ("N budgets" text dropped), green "↓ ₦12,300 from last week"; Savings Rate value now shows a "%" suffix ("12%"), support "₦X remaining", red "↓ 2% from last month". Comparison rows are reference copy (no new calculations). New `ArrowDownRightIcon` in icons.tsx. 1 test copy assertion updated ("You keep $X after expenses" → "$X remaining"). Gates: tsc/lint/SummaryCards tests green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget attention bar collapses to 2 readable issues (presentation only):** the collapsed `BudgetAttentionBar` now shows exactly the top 2 attention items instead of 3 — the third chip was being truncated by the single-row layout. The two visible issue texts render in full (no ellipsis/`truncate`), keep their order (over-limit first, then at-limit), and the hidden items no longer influence the collapsed width; "View more" reveals everything, "View less" returns to exactly 2, "Review budgets →" stays pinned right. Limit is `COLLAPSED_LIMIT = 2`. Tests updated (collapse shows top 2 + hides the rest, expand/collapse round trip). Gates: tsc/lint/430 tests green (1 pre-existing date-flaky UpcomingView failure unchanged) | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Budget attention bar collapse/expand (presentation only):** the `BudgetAttentionBar` now collapses to its compact ~76px single row regardless of how many budgets need attention: the collapsed state renders ONLY the top 3 issues (existing order — over-limit first, then at-limit), hides the rest, and replaces the "N recommendations" count with a subtle text "View more" + downward chevron control. Clicking it expands the SAME bar to reveal all remaining issues (same red/orange dot styling) and switches to "View less" + upward chevron; clicking again collapses back to 3. No duplication on expand; no hardcoded budgets (limit is `COLLAPSED_LIMIT = 3`); "Review budgets →" stays pinned right and the underlying `overBudgetCategories`/spent-at-limit logic, ordering and `/?focus=over` action are untouched. New `ChevronUpIcon` added to `components/ui/icons.tsx`. Tests: BudgetAttentionBar 5→7 (collapses to top 3 + hides rest, expand/collapse round trip, no View more when ≤3 issues). Gates: tsc/lint/429 tests green (1 pre-existing date-flaky UpcomingView failure unchanged) | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-13 | **Planner budget alert area reworked to the reference (presentation only — underlying data/actions preserved):** the three separate blocks (OverBudgetAlert "Over budget this month" card, TodayRecommendations' main "Budget far over limit" card, and its "N more recommendations" disclosure) are replaced by ONE compact horizontal alert bar (`BudgetAttentionBar.tsx`, new): ~76px tall, 18px radius, very light red/pink fill + subtle 1px light-red border, 20px horizontal padding; single flex row, vertically centered, wraps gracefully at narrow widths (no horizontal overflow). Contents: warning triangle in a 40x40px pale-red circle → "Budget needs attention" heading (16px semibold) → one chip per category — red dot + "Name is ₦X over its ₦Y limit" (from `overBudgetCategories`) or orange dot + "Name is at 100% of its limit" (spent == limit) → "N recommendations" + downward chevron (count from `insightsFor`, the same recommendation data as the Reports insights) → teal filled "Review budgets →" button (48px, 12px radius, `ml-auto` far right) navigating to `/?focus=over` (existing focus mechanism). `OverBudgetAlert.tsx` and `TodayRecommendations.tsx` deleted; PlannerView renders `<BudgetAttentionBar month={month} />` in their place; nothing else on the Planner touched (hero, summary cards, needs funding, health, budget list, activity, breakdown unchanged). Bar hides when no category is over/at its limit. New `BudgetAttentionBar.test.tsx` (5 tests: heading+issue rows, red/orange dot classes, recommendation count, Review budgets → `/?focus=over`, renders nothing when no issues). Gates: tsc/lint/427 tests green (1 pre-existing date-flaky UpcomingView failure unchanged) | `CHANGELOG.md`, `06_PLANNER_SCREEN.md`, `ARCHITECTURE.md` |
| 2026-08-13 | **Planner hero rework (presentation only — no logic/data changes):** the top of the Planner is reworked to the approved hero reference. Header row unchanged (Planner title + subtitle left; Add Expense / Add Income + month selector right). The "Month at a glance" card is replaced by a large hero card: left column — current date ("Thursday, August 13"; today for the current month, else the 1st of the viewed month), time-aware greeting "Good afternoon, Archer 👋", one projected-remaining sentence from `monthFinance()` (empty state / on track / short by), and Review Budget (scrolls to `#budget-allocation`) + View Reports (`/reports`) buttons; right column — two stat tiles (days left + "$X / day available" or shortfall/complete captions; expected income received % over a thin 3 px `ProgressBar` with "received of expected" caption). Soft teal/blue radial glows, translucent concentric circles and curved lines sit on the RIGHT of the hero inside an `overflow-hidden` clip layer — no horizontal page overflow. `MonthlyStats.tsx` + its test deleted, replaced by `Hero.tsx` (greeting/date/decoration/math all derived from existing `monthFinance`, `lib/date` helpers; no new business logic) with 8 new tests (date+greeting, projected sentence, days left + daily amount, received % + thin bar, actions, empty state, shortfall, past month) replacing the 5 MonthlyStats tests. Gates: tsc/lint/tests/build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md`, `ARCHITECTURE.md` |
| 2026-08-12 | **Planner horizontal-overflow fix (presentation only):** the Planner rendered a horizontal scrollbar at desktop sizes because several decorative layers (soft teal/blue radial blobs + curve SVGs positioned with negative right offsets) were absolutely positioned inside wrappers that did not clip — the page-level blob (`-right-24`) alone extended 54–64 px past the viewport, and the Month-at-a-Glance / Recent Activity blobs sat 3 px from the edge. Verified by a real Electron render of the running app at 1280/1440/1536 px (`docOverflow` 64 → 0; every decoration now reports a clipping `overflow-hidden` ancestor). Fix: rule — every decorative layer (`pointer-events-none absolute inset-0`) must carry `overflow-hidden` so decorations can never widen the layout. Applied to the five Planner layers: `PlannerView`, `MonthlyStats`, `RecentActivity`, `BudgetList`, `AllocationPanel` (BudgetHealthCard already clipped; modals/disclosure bodies clip internally). No content, spacing or logic changes; no global `overflow-x` hack. Gates: tsc/lint/419 tests green | `CHANGELOG.md` |
| 2026-08-12 | **Planner layout to the approved mockup (presentation only — no logic/data changes):** the standalone Hero is no longer rendered — **Month at a Glance is now the page hero** (directly below the page header; the header row itself unchanged: Planner title, subtitle, Add Expense / Add Income, month selector). The previously dormant **OverBudgetAlert is now the Budget Alert Banner**, wired between Month at a Glance and Today's Recommendations: one compact full-width row — warning icon chip + "Over budget this month" title + one-line count description + right-aligned **Review budgets** button (`Button` secondary sm navigating to `/?focus=over`, the existing Budget Allocation focus mechanism); the per-category overage chips and the inline text link are gone; rendered only while `overBudgetCategories` is non-empty. Everything else preserved: Recommendations, Summary KPI grid, Needs Funding + Budget Health, Budget Allocation; **Recent Activity now precedes Expense Breakdown** (Breakdown moved below, nothing dropped). `Hero.tsx` stays in the repo unused (untouched — reversible purely by re-adding the render). Gates: tsc/lint/419 tests/build green | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-12 | **Final visual-consistency pass (presentation only — no logic/feature changes):** one deliberate product feel across every screen. **(1) Identity restoration in the remaining large/empty/hero areas:** the shared `EmptyState` icon/illustration tiles now carry the teal/blue identity (radial tints `rgba(14,165,164,…)`/`rgba(59,130,246,…)` + concentric circle + curved line inside the tile) — all 22 call sites lift at once, including the timeline, todo, reports and planner empty states; page-level decorative bands (blobs + curve SVG) added behind the Timeline (HistoryView), Reports and To-Do page headers, matching the existing Planner/Upcoming bands; the History empty state and Upcoming's "Nothing upcoming" sub-empty are now carded decorated surfaces (circle + curve like Upcoming's main empty); Budget Health card gained the in-card motif (blobs + circle beside the donut; still nothing over dense content). **(2) Consistency fixes:** Card family radius normalized — TransactionCard and MetricCard to `rounded-xl`, Modal `rounded-[24px]`→`rounded-2xl`, hex-pinned radii replaced with tokens (`rounded-md` for row icon tiles app-wide, `rounded-sm` chart legend tile); expense amounts in the Timeline now `text-expense` (matches Recent Activity); small chips standardized to `px-2.5 py-0.5 text-xs font-semibold` (timeline category chip, "Moved", "% of pool", allocation count, upcoming days, settings chips); row ghost/reveal icon buttons standardized to `h-9 w-9` (TransactionCard edit/move/delete + chevron, BudgetRow, Recent Activity); Reports toolbar buttons aligned to Button sizing (`min-h-10 px-4 rounded-md`), History "Export" now uses the `Button` primitive; reports chart empty states unified to the brand tint (was income-green in 3 charts); Reports metric chips `h-9 w-9` (was h-10); KPI-insight rows in Budget Health no longer fake hover; row hovers added where missing (Budget Suggestions, To-Do, backups list); income-source stat cells aligned to the settings stat-tile style (`rounded-xl bg-canvas/50 px-4 py-3.5`) and the emerald hex replaced by the `success-text` token; `BarChart` track now uses the `--color-track` token; light-theme `brand-950` ramp fixed (was a duplicate of 900 — dark usages unaffected); `PageHeader` description line-height now from the token. Gates: tsc/lint/tests/build green | `UI_UX_SPEC.md` |
| 2026-08-12 | **Add Expense form redesign (Transfer tab + two-column layout; one additive store change):** the transaction modal's Income/Expense toggle becomes segmented **Expense / Transfer / Income** tabs, and the fields move to a two-column layout — Amount + Category (left column pair), Date + amount preview (right pair) — with the Note field full-width below; the modal body gains the planner's subtle teal/blue radial-gradient + curved-line decoration. **Transfer** records a deferred expense: `TransactionInput` gained an optional `deferred` flag and `addTransaction` stores it (`deferred: true`), so the record appears under "Transfers" on the Timeline for its chosen month and rolls into next month when moved — the exact semantics of the existing deferred-flag system (`moveTransactionToNextMonth`, timeline Transfers chip, deferred todo item), no new transaction type, no new fields. Submit label is now action-per-tab — **Add Expense / Add Transfer / Add Income** ("Save changes" unchanged when editing); switching tabs still resets the category and the error. Editing a deferred expense opens on the Transfer tab; saving it as Expense clears the flag. Validation, category filtering, date defaults and note rules are byte-for-byte unchanged; Payment method / Receipt from the UI reference were explicitly **not** implemented (already documented as "Not recorded"/"Not attached" — out of product scope). Also 2 store tests (deferred flag round-trip, unflagged default) + 5 form tests (transfer records deferred, transfer shows expense categories, edit opens Transfer tab, transfer→expense clears the flag, income tab creates income); tests 412→419; gates: tsc/lint/tests/build green | `UI_UX_SPEC.md` |
| 2026-08-12 | **Expense Details screen (new, no logic changes):** a dedicated `/history/expense?id=` screen (static export — query param, not a dynamic segment) for a single expense, opened by clicking an expense row in the Timeline (the chevron still expands inline; income rows unchanged). Header: Back (browser back when history exists, else `/history?month=` of the expense on direct entry) + Edit action. Hero card (the Planner's teal/blue radial tint + curved-line/concentric-circle motif): large category icon (96 px accent chip), expense title, category pill, prominent amount, Recurring/Deferred badges, full date + time chips. Note card when a note exists. Details card with labeled rows: Category, Budget (`limit · spent` of the expense's month or "No budget set"), Payment method ("Not recorded" — not in the stored model), Receipt ("Not attached" — not in the stored model), Created (`formatDateTime` of `createdAt`), Updated ("Edited" when flagged, else "—"). Actions: full-width Edit Expense (opens the standard `TransactionForm` edit flow — all existing edit functionality intact) and Delete Expense (standard `ConfirmDialog`, recurring-aware copy, back-navigation after delete). Unknown/income ids → "Expense not found" empty state. No schema changes, no store changes, no persisted data touched. New `formatDateLong`/`formatTime`/`formatDateTime` helpers in `lib/date.ts` (invalid/empty → ""). Tests 394→412; gates: tsc/lint/tests/build green | `UI_UX_SPEC.md`, `08_TIMELINE_SCREEN.md` |
| 2026-08-12 | **Planner dashboard redesign (presentation only — no behaviour/logic changes):** order is now Hero → Month at a Glance → Recommendation card → Summary KPI cards (Expected Income / Remaining / Budgeted / Savings Rate) → Needs Funding + Budget Health → Budget Allocation → Expense Breakdown → Recent Activity. **Budget Allocation** gained a compact collapsed preview: mini donut (allocated % in the centre) + top-3 budgets (icon, name, amount) + "+N more" + footer showing allocated-of-allocatable and remaining (or the over-allocated warning) + a "View all" button that expands the in-place disclosure; every focus flow (`focus=over`, `focus=create`, `planner:focus-budget` events, hero "Review Budget") still force-expands unchanged. **Recent Activity** is now a compact preview: latest 3 transactions with a "View all" timeline button (was 5 + "View Full Timeline"); row-click edit + hover-reveal Edit/Delete unchanged. **Visual language:** the soft teal/blue radial gradients and curved-line/concentric-circle motifs are now a recurring subtle backdrop (page-level layer behind hero/summary sections, Month at a Glance whitespace, allocation + empty-state areas; never behind dense content). Gates: tsc/lint/tests green (394 tests) | `CHANGELOG.md`, `06_PLANNER_SCREEN.md` |
| 2026-08-12 | **Expense Breakdown + Recent Activity interactivity (no logic changes):** (1) **Expense breakdown rows are now clickable buttons** — `BarChart` accepts a per-item `onClick` and renders the row as a real `<button>` (same visuals + tooltip, added `cursor-pointer` + `hover:bg-sidebar-hover` + focus ring; container drops `role="img"` for an interactive chart, rows get `aria-label` "Name $amount"); `ExpenseBreakdown` wires each row to the History page's existing category filter via `router.push("/history?month=MM&category=ID")` — chart, bar cap, tooltips, totals footer and the collapse-after-five behaviour untouched (2 new tests: rows render as buttons, click pushes the filtered URL; the `row()` test helper now resolves `closest("div, button")`). (2) **Recent Activity rows are interactive** — the row content (icon + title/meta + amount) is now a button that opens the already-mounted `TransactionForm` edit flow for that record (same `setEditing` handler as the pencil icon, so zero duplicated logic); Edit/Delete hover-reveal actions unchanged (2 new tests: clicking an expense row and an income row opens the pre-populated edit dialog). Tests 384→388; gates: tsc/lint/tests green | `CHANGELOG.md`, `UI_UX_SPEC.md` |
| 2026-08-12 | **Budget Allocation + Allocate remaining UI polish (no logic changes):** (1) **BudgetSuggestions compact warning** — the over-budget banner is now one header line ("N budgets over their limits" + short coverage status "Covered / Partly covered / Not covered by remaining income") and one line per budget (icon, name, overage, suggested limit, inline trim hint, Adjust button). Was: title, per-budget multi-line description, separate coverage note. Same data (`lib/recommendations.ts` untouched); tighter padding (`px-3 py-2`), hairline row dividers, `truncate` so Adjust stays reachable. (2) **Allocate remaining amount fields** — a small editable amount field sits beside each slider; slider and field stay synchronized (slider onChange rewrites the field via `minorToInput`, typing parses with `toMinorUnits` and commits through the same `clampAllocation` as the slider, so the sum can never exceed the remaining balance), invalid input is ignored and snaps back on blur, Reset/Clear/Apply reset both states. `Slider` gained an optional `trailing` slot rendered beside the track. `lib/allocation.ts` untouched; tests 379→384 (5 new AllocationPanel cases: slider→field sync, field→slider sync, typed clamp, combined typed clamp, invalid input). Gates: tsc/lint/tests green | `CHANGELOG.md`, `UI_UX_SPEC.md`, `PROJECT_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-12 | **Insight action links now actually do something (bugfix):** every budget-related action in the Today's Insight list (`lib/insights.ts` → `InsightList` in `TodayRecommendations`) pointed at `href: "/"` — a no-op when the planner is already the current page, so "Review budgets" / "Create a budget" / "Add a budget" / "Fund them" appeared dead. All six actions now reuse the planner's focus modes: the three review actions (high-over, over-120, almost-exhausted) navigate to `/?focus=over` — the mechanism the OverBudgetAlert already uses, which makes `BudgetList` expand the Budget Allocation disclosure, scroll to the section and highlight the over-limit rows — and the three creation actions (unallocated, no-budget, needs-funding) navigate to a new `/?focus=create` mode: `PlannerView` reads `focus=create` and `BudgetList` gains a `focusCreate` prop that expands the disclosure, scrolls into view and opens the New budget form (same setTimeout-before-setState pattern as the existing focus effect, so the react-hooks rules stay satisfied). Tests: the two existing `insights.test.ts` href assertions updated to the new targets and two more href assertions added (over-120 → `?focus=over`, no-budget → `?focus=create`); tests 370→372 green, tsc/lint/build green | `CHANGELOG.md` |
| 2026-08-12 | **Transaction handling fixes (delete expense + backdated income):** (1) **Delete from the Planner:** the ledger delete flow existed only on the Timeline (`TransactionList`), so the Planner — where expenses are actually added — had no way to remove a record; `RecentActivity` rows are now rows with a hover-reveal trash action that opens the app's standard `ConfirmDialog` ("Delete this transaction? This cannot be undone.") and calls the existing `deleteTransaction` store action (recurrence-rule exceptions bookkeeping untouched). All dependent figures recalculate instantly because every surface derives from the shared selectors (`monthFinance`, `totals`, `spent`, `budgetHealth`, report series) over the single `transactions` array — verified: monthly expenses, remaining income, savings rate, category/budget spending, budget health and reports all update on delete, and unrelated transactions are untouched (delete filters strictly by id). (2) **Backdated income date defaults:** the ledger math already buckets purely by the transaction's calendar date (`transactionsForMonth` via `monthKeyFromIso` — no "today" gate anywhere; Aug 5 lands in August even when today is Aug 12, and a Sep 1 future-dated income stays out of August), but both add-transaction entry points defaulted the date to **today** regardless of the planner month being viewed, so records added while viewing a past/future month silently landed in the current month and "didn't reflect" in the month the user was working in. New `defaultDateForMonth(month)` in `lib/date.ts` (today when today falls inside the viewed month, else the 1st of the viewed month — always inside the month's start/end boundaries, pure local-date string math, no timezone conversion); `TransactionForm` gained a `defaultMonth` prop (wired from `PlannerView` and the history `TransactionList`, kept when editing an existing record), and the unused `QuickAddExpense` got the same default. Month switching and refresh/reopen behave because dates persist verbatim and all totals re-derive from them. 10 new tests (4 `defaultDateForMonth`, 2 TransactionForm date-default/backdated-record, 3 RecentActivity delete confirm/cancel/unrelated-unscathed) — tests 363→372; tsc/lint green | `UI_UX_SPEC.md` |
| 2026-08-07 | **Instant simultaneous theme switching (release-blocker fix):** toggling Light↔Dark painted a lagging strip at the sidebar/content boundary. Root cause: `body` is the page canvas (`html` has no background of its own, so `body`'s background covers the whole viewport, including the `pl-60`/`px-8` gutter at the sidebar edge) and it carried `transition: background-color 250ms var(--ease-premium), color 250ms …` while every surface (`.sidebar-surface`, title bar `bg-surface`, mobile header, cards) switched instantly with no transition — so the canvas gutter blended the old theme for 250 ms after the rest of the page flipped (dark→light: white sidebar + dark strip; light→dark: mirror). Fix: removed the one-sided body transition entirely — the whole app now switches theme in the same frame (no strip, no white/black flash, no delayed repaint); verified every layout container (html, body, root wrapper, sidebar, title bar, header, page content) carries no background transition. Electron: the `BrowserWindow` `backgroundColor` was hardcoded `#0d0f14` (dark regardless of theme) — now resolved from the persisted appearance via a shared `resolvedAppearance()` helper (canvas tokens: light `#f7f8fc`, dark `#0f172a`; `titleBarOverlayColors()` refactored onto the same helper) so the frame never paints an off-theme color before first render. All surfaces already come from design tokens (`--color-canvas`, `--color-sidebar`, `--color-surface`); no hardcoded colors added. Smoke suite now asserts computed `transition-duration: 0s` / no `background-color` transition on `body` in the real renderer. Docs: `10_ANIMATION_SYSTEM.md` THEME CHANGE section rewritten (instant, simultaneous, no flash — crossfade removed). Gates: tsc/lint/tests/build + source and packaged-exe smoke green | `CHANGELOG.md`, `10_ANIMATION_SYSTEM.md` |
| 2026-08-07 | **VS Code-style auto-hiding scrollbars (app-painted, final):** the first attempt used the native thin scrollbar (`scrollbar-width: thin` + `scrollbar-color`) — pixel-measured on the packaged app it proved unreliable: on this machine it was invisible even while scrolling, and where the OS "Automatically hide scroll bars" setting is off (the Windows default; `HKCU\Control Panel\Accessibility\DynamicScrollbars` absent here) it stays permanently visible, so the native path cannot deliver the required appear-on-hover / appear-while-scrolling / fade-when-idle behaviour. Replaced with app-painted overlay scrollbars in `app/globals.css`: `::-webkit-scrollbar` 6 px, track transparent, thumb `--color-muted` at 60 % (75 % on direct thumb hover) with `border-radius: 9999px` and a 220 ms `--ease-premium` background transition; thumb transparent at rest, revealed by `*:hover` (hover) and by `html[data-scrolling="on"]` (scrolling), faded out 600 ms after the last scroll event via `lib/overlayScrollbars.ts` (passive capture-phase scroll listener; `useEffect` in `AppShell` with cleanup). Custom webkit scrollbars are overlay in this Chromium (layout probe: `reserved: 0`), so no layout shift ever; a `scrollbar-width`/`scrollbar-color` pair remains as a Firefox-only fallback. Smoke suite now asserts the real painting behaviour — `getComputedStyle(root, '::-webkit-scrollbar-thumb')` transparent at rest, non-transparent with `data-scrolling="on"`, width 6 px (previous assertion only checked computed `scrollbar-width: thin`, which never proves runtime visibility). Gates: tsc/lint/tests/build + source and packaged-exe smoke green | `CHANGELOG.md` |
| 2026-08-07 | **Custom title bar (desktop):** the main window drops the OS title bar via Electron's recommended approach — `titleBarStyle: "hidden"` + `titleBarOverlay` (44 px) — and the renderer paints a design-system title bar (`components/shell/TitleBar.tsx`): `--color-surface` background, `border-border/70` bottom border, compact logo + "Budget Planner" brand on the left, the whole strip `-webkit-app-region: drag` (no interactive children, so no no-drag elements). Native min/max/restore/close stay overlaid top-right with Windows hover states; drag, double-click maximize and Aero snap are native. Overlay colors read the `--color-surface`/`--color-ink` tokens in the renderer and sync to main over the new `desktop:window:setTitleBarOverlay` IPC (hex-validated; `windowFor`-scoped), keeping dark/light/system themes correct live; main seeds the overlay from the persisted theme at window creation (`nativeTheme` resolves "system"). Layout realigned: `Sidebar` sits below the bar (`top-11`, brand block folded into the title bar), shell content offset `pt-11`, mobile `Header` sticks at `top-11` (subtitle + theme toggle only — branding moved up), toasts start at `top-14`, transaction-list sticky headings at `top-[100px]`/`lg:top-11`, skip link below the bar. The File/Edit/View/Window/Help menu stays installed (accelerators untouched, `autoHideMenuBar` + Alt reveal unchanged). Smoke suite now asserts the `[data-titlebar]` element (top: 0, height: 44, contains "Budget Planner"), the `window.setTitleBarOverlay` bridge surface, and a design-token overlay IPC roundtrip. Gates: tsc/lint/tests/build + source and packaged-exe smoke green | `CHANGELOG.md`, `UI_UX_SPEC.md` |
| 2026-08-07 | **Native menu bar hidden by default (desktop):** the main `BrowserWindow` now sets `autoHideMenuBar: true` — the File/Edit/View/Window/Help bar is hidden at launch, pressing **Alt** temporarily reveals it (Windows default), and the application menu itself stays installed via `Menu.setApplicationMenu` so every accelerator (Ctrl+O import, Ctrl+S export, Ctrl+B backup, …) keeps working. No `Menu.setApplicationMenu(null)`, no menu removal. The smoke suite now asserts `win.isMenuBarVisible() === false` on launch (menu groups + accelerators still asserted). Gates: tsc/lint/build + packaged-exe smoke green | `CHANGELOG.md` |
| 2026-08-07 | **Financial summary consistency audit (every widget with an X-of-Y / percentage / progress / remaining figure verified against one business rule) + 9 fixes in one pass:** (1) `BudgetRow` % was clamped to 100 (dead `pct > 100` branch) while BudgetUtilizationChart/BudgetList show true % — now `round(100·spent/limit)` unclamped, bar stays clamped, over-100 in danger; (2) `CashFlowChart` tooltip + aria "Expenses" showed the bar-capped `min(expenses, income)` — now true total (`expensesTotal` = capped + shortfall); "Money in" still reconstructs income; (3) Recommendations "Allocate remaining income" card used plan-only `receivedAmount` and a title/description mismatch — now `monthFinance().received/expected`, title "Some expected income hasn't arrived", text "You've received X of Y expected income"; (4) Recommendations savings-rate card recomputed rate with plan-only received — now uses `monthFinance().savingsRate` (same number as the Savings Rate card); (5) BudgetSuggestions "Your remaining income covers these overages" compared remaining cash per-budget instead of the sum of overages — now aggregate `Σ overspent ≤ remainingIncome`; (6) insights "Unallocated funds" used `net` (received − expenses) while every planner surface defines unallocated = allocatable − committed — now `received`; (7) SummaryCards Remaining card "% of allocatable" suffix added (was bare "%" vs Budgeted card); (8) CategoryAnalysisChart center total is the top-6 sum — caption now "top categories spent" (not "spent"); (9) Hero "on track to finish with X remaining" is expected-based while ForecastCard is received-based — copy now says "a projected X remaining". Accepted as-is and documented: HistoryView Income chip = ledger-only sum (Timeline is a ledger view; Transfers chip pre-logged UX M5), CategoryAnalysis vs TopCategories share % denominators, AllocationPanel "nothing left" copy nuance. Tests 363/363; tsc/lint/build green | `UI_UX_SPEC.md` |
| 2026-08-01 | Initial roadmap created | All |
| 2026-08-01 | Phase 4 foundation implemented (steps 1–6: lib, store, hooks, shell, ui components, tokens, error boundary); pages deferred to later steps. All ACs covered by automated tests to date pass (61 tests) | `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | Application shell completed: 5 routes with shared page layout and placeholders, responsive sidebar/bottom nav, motion system (modal 120 ms, reduced-motion), title template, page metadata | `UI_UX_SPEC.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | Scope addition approved by product owner (option 2): expense prioritization (FR-10), allocation sliders (FR-11), move-to-next-month (FR-12), insights & recommendations (FR-13), budget health (FR-14), animated charts (FR-15), NGN/USD formatting-only currency (FR-16) + AC-16..AC-22. Additive v1 migration (currencySymbol → currency; missing priority defaults). Docs updated to be the new source of truth; implementation follows | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | Phase 4 scope implementation completed: lib (allocation, insights, budgetHealth, recordException, nextMonthDate, currency money helpers), store (updateBudget, moveTransactionToNextMonth, updateCategory, ToastStore), all feature components (dashboard, budgets incl. allocation panel + priority, transactions incl. list/filters/move/recurring forms, settings incl. currency/categories/data management), and all 5 pages live. Gates green: `npx tsc --noEmit`, `npm run lint`, `npm run test` (95 tests), `npm run build` | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | Planner-first UX restructure approved: the Planner (`/`) becomes the primary screen (merged dashboard + budgets: summary cards, month selector, budget allocation + priority, allocation sliders, budget health, insights, charts, remaining balance, Quick Add Expense, deferred expenses); new To-Do page (`/todo`, FR-17) aggregates actionable recommendations; Transactions renamed to History (`/history`) as a chronological ledger; nav is Planner, To-Do, History, Reports, Settings. `Transaction.deferred` flag added (set by move-to-next-month, FR-12). Additive v1; no data migration. Docs updated first; implementation follows | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | History module completed: chronological ledger (add/edit/delete via reused `TransactionForm`, income/expense, categories, date, notes, search, type/category filters, month picker), user-selectable sorting (date/amount × asc/desc via `sortTransactions`, URL-persisted `&sort=`), pagination 25/page, deferred chip, toasts; everything persists to localStorage and derived values recalc immediately. All gates green (106+ tests) | `UI_UX_SPEC.md`, `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | Planner completed as the primary workspace (before Reports, per product owner): new **Needs Funding** checklist (unfunded expense categories, "Fund" opens the budget form with category preselected, AC-25 via `needsFunding` selector) and the **Allocated** section (budget table retitled, funding bar `Σ limits` vs `max(0, net)`); PlannerView restructured to the workspace flow (summary → needs funding → allocated → sliders → quick add → insights). All other Planner features verified present: monthly income card, month selector, remaining balance, budget allocation, expense priority, allocation sliders, budget health, deferred expenses, move-to-next-month, insights, recommendations, animated charts, category % breakdown, Quick Add Expense, NGN/USD formatting. Docs updated first; gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | **Dependency decision (product owner):** Recharts approved for Reports charts, overriding FR-07's earlier "pure CSS, no chart library" stance. Single chart dependency; route-split so only `/reports` loads it. Planner keeps the custom animated `BarChart`. | `PROJECT_SPEC.md` (FR-07), `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-01 | Reports module completed (analytical-only): 6-month window ending at the selected month — snapshot (savings, remaining balance), income vs expenses (grouped bars), monthly spending trend, savings & remaining lines, budget utilization per month, top 5 categories across the window; Recharts with exact-value tooltips, compact axis ticks, reduced-motion support, empty states, responsive layout. New pure selectors `monthlySeries`, `budgetUtilizationSeries`, `spendingByCategoryInMonths`, `windowMonths`. Docs updated first; gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md`, `PHASE-04-IMPLEMENTATION.md` |
| 2026-08-04 | **Income planning rework (FR-18):** expected income is now a standalone month-scoped collection `{id, month, name, icon, expectedAmount, receivedAmount}` — editable name, icon, and both amounts per source. Fixed the overwrite bug (the modal's per-source drafts now seed from existing plans and write per entry id; store `setIncomePlan(month, id | null, patch)` merges a single source, with a regression test). Expected income modal redesigned (name input, icon picker, Expected/Received inputs, live Difference status: to collect / collected in full / exceeded by). Icon picker upgraded: favourites, "Line icons" vector set, `IconValue` renderer; reports income charts (sources, expected vs actual, trend) now use plan data. State schema v3; v1→v2→v3 migrations in `validateAppState`. 218 tests, tsc/lint/build green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Finance math centralized + Net/Remaining bugfix (FR-06/FR-18):** Net/Remaining/Projected were computed from ad-hoc `totals()` calls — Net used transaction income only, so a month funded via income plans (received stored in plans, not transactions) showed Net/Remaining 0 and Net −Expenses. New `lib/finance.ts` `monthFinance()` is the single source of truth: Received = `max(income transactions, Σ plan.receivedAmount)`, Net = Received − Expenses, Remaining = `max(0, Net)`, Projected remaining = Expected − Expenses, savings rate from Received. All consumers migrated (SummaryCards, AllocationPanel, BudgetList, BudgetHealthCard, Hero, MonthlyStats, KpiStrip, ReportsView, IncomeExpenseChart, SavingsChart, InsightList, ReportsInsights, TodayRecommendations, TodoView, BudgetSuggestions, usePlannerStatus, selectors `receivedForMonth`/`budgetHealth`, insights/todo/recommendations/predictions/reportTrends). Focus-loss bugfix: `Modal`/`Drawer` effect re-ran on every keystroke because inline `onClose` was in its deps, stealing focus — now `onCloseRef` + `[open]` deps (regression test `Modal.test.tsx` proves the old code fails, new passes); `autoFocus` workaround removed from icon picker search. Tests 218→229; gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Budget dialog form-state fix (FR-06):** "Choose a category." persisted after picking a category because `error` was only cleared at the next submit (validate-on-change was missing). Also fixed: the edit branch dropped a changed category (`updateBudget` only patched limit/priority — now accepts a validated `categoryId` with a same-month duplicate guard), and the footer submit button used a duplicated `id="budget-form"` across the three mounted `BudgetForm` instances (now a `useId`-unique form id per instance so submit can never target another dialog's form). Regression tests: 6 `BudgetForm.test.tsx` cases (error clears on selection, submission persists the id, amount error clears when valid, edit pre-selects, edit persists a category change, unique form ids) + 2 store tests. Tests 229→237 | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Nested-dialog focus isolation (Modal/Drawer) + remount audit:** the outer modal's focus trap reacted to keys pressed inside a nested dialog (IconPicker inside IncomeModal) — Escape closed both dialogs and Tab wrapping crossed dialog boundaries. Both traps now stand down when the active element is inside a nested `[role="dialog"]` (regression tests in `Modal.test.tsx` fail against the old code). Instrumented the modal chain (render/MOUNT logs + `useId`) and proved via DOM-node-identity tests (`BudgetForm.remount.test.tsx`, `IncomeModal.remount.test.tsx`) that no modal contents remount on keystrokes: BudgetForm/IncomeModal/IconPicker form, inputs, selects and scroll containers keep the same DOM nodes while typing; all temporary logs removed. Tests 237→245; gates green | `PROJECT_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Category edit modal state isolation (FR-08 settings):** replaced the shared-state edit modal in `CategoryManager` (which reused the inline add-form `newExpenseName/Icon/Color` + `newIncome*` state, rewriting parent state on every keystroke) with a standalone `CategoryEditModal` that mounts per edit session, seeds a local `Category` draft exactly once at mount (`useState(category)` — no effects, no sync), and reads/writes name/icon/color ONLY through that draft. The store is untouched until Save, which calls `updateCategory(draft)` exactly once, then closes. The IconPicker's search/query, memoized `sections`/`flat`/`offsets` and stable grid keys were audited (search input, options tree and scroll container keep DOM identity while typing) and locked in with 8 `CategoryEditModal.test.tsx` cases incl. save-once (spy), no store writes while typing, focus/node stability, and no uncontrolled/controlled warnings. Tests 245→253; gates green | `PROJECT_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Needs Funding redefined as an obligation-driven task list + planner polish:** `needsFunding` (no-budget categories) is retired from the panel in favor of `fundingGaps` (`lib/selectors.ts`): a category appears ONLY when it has unpaid upcoming expenses in the selected month whose total (Target/"Needed") exceeds its budget limit (Allocated); Missing = max(Target − Allocated, 0); rows sorted by largest Missing, showing icon, name, urgency, Allocated/Needed/Missing (Missing red when > 0), a thin colored progress bar (Allocated/Target, tone by ratio) and a Fund button. Fund: no budget → "New budget" modal preselected with the limit prefilled to the Missing amount (new `presetLimit` prop), then auto-scrolls via a `planner:focus-budget` window event; budget exists → BudgetList expands the Allocated disclosure, highlights the row (`focusedBudgetId` ring) and focuses the matching `allocation-slider-{id}` range input (id added in AllocationPanel). Empty state copy now "covers every upcoming expense this month"; `attentionOnly` preview shows the top 3 gaps. BudgetHealthCard "Every category funded" now counts `fundingGaps` (same semantics). **Budget Health hierarchy:** score enlarged to 5xl as the focus, checklist rows lightened (no pill backgrounds, smaller icons, plain status text, `gap-0.5`), progress bar thinned to `h-1` via new `ProgressBar className` prop, overall card height ~15% shorter. **Spacing polish:** PlannerView section rhythm `gap-10`→`gap-7` (−30%), BudgetList `gap-6`→`gap-5`; no card padding touched. Tests 253→267 (11 reworked panel cases, 6 `fundingGaps` selector cases, 2 BudgetList focus-budget cases); gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Month at a glance metric emphasis (MonthlyStats):** same metrics, stronger hierarchy — labels reduced (11px, medium weight, no uppercase) and numbers enlarged `text-lg`→`text-2xl`. Tiny contextual indicators added: Largest expense carries the category's colored icon badge (`monthStats.largestExpense` now exposes `categoryId`), Savings rate gets a green `ArrowUpRightIcon` when positive (red `TrendDownIcon` when negative), Projected remaining a muted `WalletIcon`. 4 new `MonthlyStats.test.tsx` cases (badge, positive/negative arrows, wallet icon). Tests 267→271; gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-04 | **Expense breakdown v2:** percentage now sits in a cleaner right-aligned amount/% column beside each bar; longest bar capped at 80% of the track (proportions preserved); hover tooltips add Category/Amount/Percentage/Budget limit/Spent (unbudgeted rows say "Not budgeted", over-budget Spent turns red); more than five categories collapse to five behind a **Show N more categories** button (replacing the plain "N more categories when expanded" text) with an animated `list-in` entrance, wired to expand the Planner's collapsed preview. `BarChart` gains `budget`/`spent`/`overBudget` fields, a capped width and an `animateFrom` prop. 9 new `ExpenseBreakdown.test.tsx` cases. Tests 271→280; gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md` |
| 2026-08-04 | **Timeline polish (no structural redesign):** row hover bumped to full `hover:bg-canvas`; date groups separated by an 8px spacer row; the **Today** group gets a subtle brand accent (tinted header band + colored label + small brand dot); running group totals made prominent (`text-sm font-bold text-ink`, brand-colored for Today); row action icon buttons reduced to `sm` size; income rows render an invisible fixed-width placeholder in the Move slot so the Edit/Delete icons align perfectly across income and expense rows. 3 `TransactionRow.test.tsx` cases + 2 `TransactionList.test.tsx` cases. Tests 280→285; gates green | `UI_UX_SPEC.md` |
| 2026-08-04 | **Reports anti-busy pass:** all `ChartCard`s switch to the quiet treatment (canvas wash + hairline border + no shadow) so charts stop competing; the global `--shadow-card` token is softened (light + dark); sections get `flex flex-col gap-6` inside and the page `gap-6`→`gap-8`, grid gaps `gap-4`→`gap-6`/`gap-5` (snapshot); `SectionHeading` enlarged `text-xs`→`text-sm`; every chart gained a concise subtitle — **Last 6 months** (Income vs expenses, Income trend, Monthly spending trend, Savings, Budget utilization, Top categories) / **This month** (Expected vs actual, Income sources) / **By category** ("Spending this month"); ReportsInsights quieted to match. **Category-color consistency:** new `lib/accents.ts` `categoryColor()` + `CATEGORY_COLOR_FALLBACK` shared by Planner ExpenseBreakdown and Reports Top Categories, locking every category-colored chart to the Planner palette. 13 new tests (`ReportsVisual.test.tsx` chart-subtitle matrix + quiet chrome, `SectionHeading` size, `categoryColor`). Tests 285→297; gates green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md` |
| 2026-08-05 | **Responsive QA pass (7 fixes, 320px–4K):** (1) Month picker overflowed the page at 320px — the label forced `min-w-32` and "This month" appeared alongside (~297px total vs 272px content); label now `min-w-0 truncate` and the shortcut is `hidden sm:flex`. (2) Reports header MonthPicker+Export cluster was a non-wrapping flex row (~320px) — now `flex-wrap`. (3) The last upcoming expense's row menu was fully clipped — the list container's `overflow-hidden` (for rounding) cut off the `absolute top-full` menu; container no longer clips, first group header takes `rounded-t-2xl`. (4) Category edit/delete buttons were invisible on touch — `opacity-0 group-hover` at all breakpoints; now `sm:opacity-0 sm:group-hover:opacity-100 sm:group-focus-within:opacity-100` (matches recurring rows). (5) Income source rows unusable at 320px — fixed 144px icon picker + icon + trash left the name input ~0px (modal content 240px); row now `flex-wrap` with name `min-w-36` and picker `w-full sm:w-36`. (6) Needs-funding Allocated/Needed/Missing trio stayed 3 columns at 320px (~74px cells colliding) — now `grid-cols-1 sm:grid-cols-3`. (7) Over-budget pills spilled past the card — `truncate` never engaged (flex `min-width:auto`); pills `max-w-full` + name `min-w-0 truncate`. All charts already use `ResponsiveContainer width="100%"`; shell (sidebar/bottom nav/header) verified at 320/375/768/1024/1366/4K; perf paths verified (memoized selectors + 25/page pagination). Tests stay 325; tsc/lint/build green | `UI_UX_SPEC.md` |
| 2026-08-05 | **Production readiness audit + hardening (behavior-preserving):** reconciled `CURRENT_STORAGE_VERSION` 2→3 in `lib/storage.ts` (legacy v2 payloads now snapshot before migration); new `lib/ids.ts` `createId()` (secure-context fallback) replaces 8 raw `crypto.randomUUID()` sites and the duplicate in `validate.ts` — the only crash risk for entity creation in a packaged desktop shell; single `daysBetween` (lib/date.ts) replaces 3 duplicated `DAY_MS` midnight-diff helpers; toast auto-dismiss owned solely by `useToastStore` (duplicate timer removed from `Toast.tsx`); storage writes (persist `setItem`, `saveAppState`, backup snapshots) guarded against quota/security exceptions so boot can't be masked; dead exports removed (`loadAppState`, `removeAppState`, `readStoredTheme`, `currencySymbol`), dead components removed (`KpiStrip`, `Badge`, `Spinner`), 5 unused starter SVGs deleted; `MINOR_UNITS_PER_UNIT` + shared `MAX_*` length constants replace magic numbers; shared `isDeeplyOverBudget`; shared `CATEGORIZATION_KEY`; `toMinorUnits` dead `_currency` param dropped (31 call sites); `RecoveryPanel` defers `URL.revokeObjectURL`; `parseMonth` hoisted out of a render path in ReportsView. Tests 325→324 (dead-symbol spec removed, corrupt-snapshot spec documents v2+corrupt double snapshot); tsc/lint/build green | `ARCHITECTURE.md` |
| 2026-08-05 | **Settings QA pass (4 fixes + validation hardening):** (1) Importing a non-JSON file crashed the app — `confirmImport` ran unguarded `JSON.parse`; parsing moved inside `importState`'s try/catch (it now accepts a JSON string or parsed object) so bad files get a clean error. (2) Deleting a category used by transactions showed a false "Category deleted." success — `CategoryManager` didn't handle the store's `in-use-transactions` reason; now an error toast (matching the budgets/upcoming/rules branches). (3) Clearing the icon picker then saving a category stored an empty icon that `validateAppState` rejects — the next load threw "Saved data is corrupted" (data loss). Category add/edit now require an icon, and the store rejects empty icons/names/bad colors. (4) Category actions hardened: `addCategory`/`renameCategory`/`updateCategory` validate trimmed non-empty name ≤ `MAX_CATEGORY_NAME` (30), non-empty icon, hex color, and reject case-insensitive duplicates; all return booleans; add/edit inputs cap at 30 chars with precise errors. `validateAppState` stays lenient on name length so legacy long names never brick the store. Also: category row edit/delete buttons got accessible names ("Edit X"/"Delete X"). 15 new tests (4 CategoryManager, 2 CategoryEditModal, 9 store). Tests 310→325; tsc/lint/build green | `PROJECT_SPEC.md` |
| 2026-08-05 | **Timeline QA pass (sticky-layout fixes):** the pinned filter bar + pinned table header never aligned — the header's fixed offset (`lg:top-[4.75rem]` = 76px) couldn't match the bar's real height (69px single row; ~125px when filters wrapped on narrower desktops), leaving a 7px gap of scrolling content between them or tucking the header under the bar; on mobile the header pinned at `top-0` behind the opaque app header and the ~180px wrapped bar. The filter row is no longer sticky (no other page pins a toolbar) and the header now uses the planner's proven `sticky top-16 lg:top-0` pattern — flush below the app header on mobile, flush with the viewport on desktop. Also: long unbroken notes could widen the table past the card on desktop (wrapper only scrolls on mobile) — the note cell now wraps mid-word (`break-words`). 2 new `TransactionList.test.tsx` cases (bar in flow + header pin pattern; note wrapping). Tests 308→310; tsc/lint/build green | `UI_UX_SPEC.md` |
| 2026-08-05 | **Reports QA pass (5 calculation/display fixes):** (1) Income trend "Received" was plans-only (`incomeTrendSeries` summed `plan.receivedAmount`), so ledger-funded months plotted $0 while every other income number used `receivedForMonth` — the series now takes transactions and uses the shared `max(ledger, plans)` source. (2) Budget utilization >100% overflowed the `[0,100]` Y axis; bar geometry clamps at 100% (`barPct`) while tooltip/aria keep the true %; the pct calculation moved into `budgetUtilizationSeries` (centralized). (3) ReportsView "Expenses vs last month" caption used the net delta (`monthStats.vsLastMonth.delta` = net − lastNet), misattributing income swings to expenses — now the true expense delta via `totals`. (4) CSV export wrote raw minor-unit integers; Amount now exports currency decimals with a new Currency column. (5) Expected vs actual / Income sources empty states promised "record income" would populate them (sources come from income plans) — copy fixed. 7 new tests (incomeTrendSeries ×3, utilization over-100%, chart aria/empty-state ×2). Tests 303→308; tsc/lint/build green | `PROJECT_SPEC.md`, `UI_UX_SPEC.md` |
| 2026-08-05 | **Planner QA pass (bugfix):** allocation sliders were stuck at 0 when the remaining balance was below one whole unit — the range input used `step={100}` while `max` was smaller, so no value above 0 existed. `AllocationPanel` now adapts the step to the balance (`min(100, remaining)`): a $0.50 balance is allocatable in 50¢ increments, normal balances keep the $1 step. 6 new `AllocationPanel.test.tsx` cases (no budgets → panel hidden, zero-balance message, sub-unit step, whole-unit step, apply raises the budget limit, combined allocations clamp to the balance). Tests 297→303; tsc/lint/build green | `PROJECT_SPEC.md` |
| 2026-08-05 | **Final UI consistency polish (visual only, no redesign):** single surface radius — `Card`/`Disclosure` quiet+brand `rounded-2xl`→`rounded-xl`; single caption scale — all `text-[11px]`→`text-xs` (SummaryCards hint + Edit pill, MonthlyStats labels, AllocationPanel % pill, BackupsManager badge, BottomNav labels, ReportsView snapshot + prediction captions); single motion timing — Disclosure chevron/height `duration-[220ms]`→`duration-200`; Income trend chart empty state upgraded from a plain `<p>` to the shared `EmptyState` (all 6 report charts consistent); missing category colors now resolve via shared `categoryColor()` (replaces raw `#6b7280` fallbacks in AllocationPanel + SettingsView recurring rows); Timeline income-row placeholder `w-11`→`w-10` so Edit/Delete align pixel-perfectly with expense rows; ThemeToggle icons sized by `h-4 w-4` classes instead of 16px SVG attrs; IncomeModal inputs `h-10`→`h-11` (app-wide control height); PageSkeleton blocks `rounded-lg`→`rounded-xl` (skeleton mirrors cards). `TransactionRow.test.tsx` width assertion updated; tests stay 297; tsc/lint/build green | `UI_UX_SPEC.md` |
| 2026-08-05 | **Electron desktop app delivered:** `next.config.ts` `output: "export"` (6 static routes → `out/`, Next 16 `.txt` RSC payloads); `electron/main.cjs` — privileged `app://bundle` protocol (`standard/secure/supportFetchAPI/stream`) with extensionless-route → app-shell fallback, `__next.<route>.__PAGE__.txt` prefetch mapping, path-traversal guard; window 1280×800 min 375×600, `contextIsolation` + `sandbox` on; `--smoke` mode boots the app, clicks `/history` until the router converges, asserts title/body/no renderer errors (works from the packaged exe too); electron-builder NSIS + portable targets (`appId com.budgetplanner.desktop`) → `dist/BudgetPlanner-Setup-0.1.0.exe` + `BudgetPlanner-Portable-0.1.0.exe`; `electron@43` + `electron-builder@26` devDeps; `electron/` eslint-ignored + tracked, `out/`/`dist/` gitignored. Smoke test + packaged-exe smoke + all gates green (tsc/lint/324 tests/build). Gaps: default Electron icon, unsigned executables | `CHANGELOG.md` |
| 2026-08-05 | **Desktop migration step 2 — dev workflow + secure shell:** `npm run dev` launches Next dev server + Electron together (`concurrently` + `wait-on`; `dev:web`/`dev:electron` parts, `ELECTRON_DEV_URL` overridable); Electron `--dev` mode loads `http://localhost:3000` with a server-retry loop and skips the `out/` check; new `electron/preload.cjs` (sandboxed, contextIsolation, no nodeIntegration, no remote module) exposes a feature-less `budgetPlannerDesktop` bridge (`platform` + `getAppInfo()` via `ipcMain.handle("desktop:app-info")`) as the future IPC seam; smoke test asserts the bridge is exposed + answers; scripts `electron`/`dev`/`build`/`dist` (`desktop:package` = build + dist); `scripts/make-icon.mjs` generates `build/icon.ico`+`icon.png` (indigo rounded square, white coin + brand ring, brand-600/700) — no more default icon; NSIS → assisted installer (`oneClick: false`: install dir choice, desktop/start-menu shortcuts, uninstall support). Gates green; dev-server smoke + packaged-exe smoke green | `CHANGELOG.md` |
| 2026-08-05 | **Desktop migration step 3 — SQLite persistence:** browser persistence replaced inside the desktop shell via `better-sqlite3` in the Electron main process (`electron/db.cjs`: `kv` table, WAL, user_version 1, `<userData>/budget-planner.sqlite3`; productName set top-level so the app owns its profile; electron-builder rebuilds + asar-unpacks the native module). Renderer never touches SQLite — sync `desktop:storage:*` IPC channels (get/set/remove/keys/needs-migration/migrate, validated main-side) exposed through the preload bridge; `lib/desktop.ts` types it; `lib/storageAdapter.ts` is the single seam (bridge in Electron, localStorage in browser, no-op in SSR); all call sites migrated (zustand persist, `lib/storage.ts`, categorization, theme bootstrap, disclosure, icon lists; failed bridge writes throw like quota errors). First-launch migration: preload ships localStorage to main, which writes a `budget-planner:backup:migration-browser:*` backup + rows + marker in ONE transaction (atomic, idempotent; backup shows in BackupsManager); failure handling: structured error, rollback keeps browser data intact + retry next launch, native error dialog (skipped in smoke). Native toolchain: Python 3.12 installed (winget) for node-gyp; `postinstall` = `electron-builder install-app-deps`; `npm run db:check` verifies module + migration success/failure paths + real-db dump. Smoke asserts SQLite roundtrip + db file under userData (dev/prod/packaged green). Tests 324→330 (6 storageAdapter cases); tsc/lint/build green | `ARCHITECTURE.md` |
| 2026-08-05 | **Desktop migration step 4 — native desktop features:** native application menu (`electron/menu.cjs`: File Import Ctrl+O / Export Ctrl+S / Back up now Ctrl+B / Restore latest backup Ctrl+Shift+B / Open backup folder Ctrl+Shift+O / Reveal data folder Ctrl+Shift+D / Quit Ctrl+Q; Edit clipboard roles; View reload/devtools/zoom/fullscreen; Help About) — renderer-state actions sent as `desktop:menu:action`, folder actions handled in main; native file dialogs + restricted fs IPC (absolute paths, .json-only writes, 16 MB cap) with composite `desktop:import` (open → destructive confirm → read) and `desktop:export` (save dialog → atomic write); desktop notifications (`desktop:notify`, Notification.isSupported-guarded; used for backup failure + restore results); automatic file backups (`electron/backups.cjs`: atomic temp+rename, content dedupe, prune to newest 30 in `<userData>/backups/`; boot + every 30 min + beforeunload flush via sync IPC); restore-backup workflow (file backups in Settings Data/BackupsManager + menu Restore latest; shared `{state, version}` envelope restored through validated `importState`); Open Backup Folder / Reveal Data Folder (shell IPC + Settings buttons + About/data-folder paths). Renderer wiring: `lib/desktopFeatures.ts` (bridge wrappers + `startAutoBackups`), `lib/desktopBootstrap.ts` (menu dispatcher + auto-backup, init from AppShell). Smoke asserts menu groups/accelerators, full bridge surface, backup create/list/read/delete roundtrip (dev+prod green). Tests 330→337 (+7 `electron/backups.test.ts`); tsc/lint/build green | `ARCHITECTURE.md` |
| 2026-08-05 | **Desktop migration step 5 — startup, identity & updates:** splash screen (`electron/splash.cjs`, frameless `data:`-URL window with inline SVG icon + name + version + animated bar; no preload; closed on main-window `ready-to-show`; skipped in `--smoke`); root loading screen (`app/loading.tsx` → shared `PageSkeleton`); proper app icon — `scripts/make-icon.mjs` rewritten to draw the designed mark (indigo gradient square, white coin + edge shade + drop shadow, three ascending bars) with 4×4 supersampled AA at 512 px, box-downsampled into 7 ICO entries + 512 PNG (fixed ICO layout bug: directory before image data); version info — `lib/version.ts` (from package.json), Settings About card shows `v0.1.0` + Electron/Chromium, native About dialog richer (versions/platform/update feed/folders), `getAppInfo()` now returns `versions`; auto-update scaffold — `electron/updater.cjs` on `electron-updater` (generic feed via `AUTO_UPDATE_URL` env or `<userData>/update-feed.txt`, packaged-only, inert without feed, startup + manual checks, auto-download + install-on-quit + notifications, Help → Check for updates…); installer branding — NSIS installer/uninstaller/header icons + `copyright`. Smoke green dev+prod (splash/updater no-op in smoke); packaged-exe smoke green after dist rebuild; tests stay 337; tsc/lint/build green | `CHANGELOG.md` |
| 2026-08-05 | **Windows icon black-logo bugfix:** every Windows-facing icon (desktop shortcut, taskbar, window, installer, executable, Start Menu) rendered as a black silhouette because `downsample()` in `scripts/make-icon.mjs` divided the alpha-premultiplied RGB sums by the raw 0–255 alpha sum (`r / a`) instead of the normalized weight sum — a ~255× oversized divisor rounded every channel to 0. The 512 px `build/icon.png` master (drawn directly, not downsampled) was fine; all 7 ICO entries (16/24/32/48/64/128/256) were black-RGB with correct alpha. Fixed to divide by `weight` (Σ a/255); regenerated `build/icon.png` + `build/icon.ico` via `npm run icon`; verified by decoding every embedded PNG (transparency + non-black pixels) in the source ICO and in the packaged `Budget Planner.exe` (all 7 sizes) and both NSIS installers (PE resource section). Stale `dist/` cleared before `npm run dist` rebuild. No app code touched; tests stay 337; tsc/lint/build green | `ARCHITECTURE.md` |
| 2026-08-05 | **Needs Funding focus-event race fix (regression):** after saving a budget from the panel's Fund dialog, the `planner:focus-budget` event was dispatched synchronously inside the submit handler — before React committed the new budget — so BudgetList's listener (a closure over the old `monthBudgets`, missing the just-created budget) bailed on the id guard and swallowed the event: the Allocated disclosure never expanded, no scroll/highlight, "Fund does nothing". Listener now reads `useAppStore.getState().state.budgets` at event time (filtered by the `month` prop, effect deps `[month]`), so any dispatch timing is handled correctly. Verified end-to-end: new category + upcoming expense created after mount appear in the panel immediately, Fund opens the dialog preselected/prefilled, saving creates the budget, the disclosure expands and the row highlights with the allocation slider targeted (new `NeedsFundingSection.test.tsx` flow case); plus a sync-dispatch regression case in `BudgetList.focus.test.tsx` (fails against the old closure). Tests 337→339; tsc/lint/build green | `ARCHITECTURE.md` |
| 2026-08-06 | **Needs Funding unified under ONE selector (root-cause redesign):** the app computed funding two incompatible ways — the panel + health checklist used obligation-driven `fundingGaps` (a category appears only with unpaid upcoming expenses in the month), while the header status + insights still used the retired no-budget `needsFunding` — so with no upcoming expenses the panel said "Everything is funded" while the header/insights said "5 categories need funding", and newly created categories never appeared. New `lib/funding.ts` `fundingNeeds(budgets, categories, futureExpenses, month)` is the single source of truth: every expense category with no budget (or limit 0) for the month appears immediately (AC-25), and budgeted categories appear while their obligations exceed their limit (Missing = max(Target − Allocated, 0) > 0); rows show Allocated/Needed/Missing, sorted by Missing desc then name. All four surfaces consume it (NeedsFundingSection, BudgetHealthCard, usePlannerStatus header, insights) — the legacy `needsFunding`/`fundingGaps` exports were deleted, so no surface can ever disagree. Fund: no budget → New Budget dialog preselected with the limit prefilled to Missing (no prefill when there are no obligations); existing budget → scroll/highlight/focus via `planner:focus-budget` (unchanged). Empty-state copy now "Every expense category has a budget this month." Documented data flow: categories → rows, budgets → allocated, futureExpenses → target, month (URL) → scope; expected/received income intentionally does not feed the derivation (obligations are income-independent; income's funding role stays in the Allocated bar via `monthFinance`). Verified live in the running app (browser + Electron with real user data): 5 previously-hidden unfunded categories now appear, and the full create-category → create-expense → fund → save → auto-update flow works end-to-end. Tests 339→342 (13 new `funding.test.ts` cases, panel cases reworked for unified semantics); tsc/lint/build green | `PROJECT_SPEC.md` (AC-25), `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-06 | **Visual redesign — premium desktop minimalism (visual only, zero feature/logic changes):** warm-neutral palette replaces the slate scale (canvas `#f8fafc`→`#f6f5f2`, ink `#0f172a`→`#1a1a1e`, muted `#64748b`→`#71717a`, border `#e2e8f0`→`#e8e6e0`, income `#16a34a`→`#0f9d6d`, expense `#dc2626`→`#e5484d`, warn `#d97706`→`#a85b0b`; dark equivalents tuned); type scale tightened (`text-sm` 14→13 px, `text-base` 16→15 px); `--shadow-card` flattened to 2% alpha (hairlines over shadows) — only overlays (menus/toasts/modals/tooltips) keep `--shadow-pop`; `--ease-premium` curve + entrance keyframes (`page-in`, `dialog-in`, `toast-in`, `menu-in`, `drawer-in`, `list-in`) + `--slider-fill` property for premium range styling; `select-premium` hand-drawn chevron. Primitives: Card (no shadow, header `px-5 pb-2 pt-4` with no divider), Button (md `min-h-10`, 150 ms, `active:scale-[0.98]`), Input/Select (h-10, `rounded-lg`, `ring-brand-500/20`), ProgressBar (h-1.5), Modal (`rounded-2xl`, blur overlay, `dialog-in`), Drawer, Disclosure (15 px title, chevron rotate, `hover:bg-canvas/50`), SectionHeading stays small-caps (`text-xs`), PageHeader (`text-2xl` tight tracking), MonthPicker h-10. Shell: sidebar `w-60` with 1 px active bar + `bg-canvas/80` active pill, bottom nav hairline + 2 px active bar, blurred header. Planner: hero date-line + 48 px title, summary cards without accent bars, hover-lift `shadow-card-hover`, Edit pill, subtle warn “N categories needing funding” chip, hairline budget rows with hover-reveal actions, h-2 chart bars, `/0.08` tint convention app-wide (icon chips, empty-state tiles) and `/0.04–/0.05` insight rows (left border removed), de-uppercased table headers (`text-xs` semibold). Reports: snapshot tiles hairline (no shadow), charts restyled via `chartStyles.ts` (hairline dashed grid, 11.5 px ticks, 4 px bar radii, 1.75 px strokes, 0.16→0 area gradient, surface hairline tooltips), `useChartColors` fallback synced to the warm palette, predictions labels de-uppercased. Settings/Todo: category/recurring/backup rows become plain hover rows, import label matches Button chrome, About icon unshadowed, Todo rows hairline-divided with hover-link. Tests: 2 assertions updated in `ReportsVisual.test.tsx` (SectionHeading `text-xs`, ChartCard shadow absence). Verified live in Electron at 1440×900 (computed styles: body `#f6f5f2`, zero large-element shadows, 40 px control heights, 240 px sidebar, dark theme palette) + 8 fresh screenshots. Tests 342→342; tsc/lint/build green | `UI_UX_SPEC.md` |
| 2026-08-06 | **Reports rebuilt to v2.0 spec (`07_REPORTS_SCREEN.md`):** ReportsView recomposed in spec section order. Header: title/description, **Compare month** toggle (per-metric current-vs-previous deltas), MonthPicker, **Export** menu (CSV via `exportCsv` with decimal amounts + Currency column, PDF via print dialog, Print; outside-click + Escape close). Sections: **Monthly overview** (`MonthlyOverview` KPI tiles - Total income / Total expenses / Net savings / Savings rate - each with a green up / red down trend chip vs the previous month from `monthFinance`, plus a 4-row CompareStrip when Compare month is on); **Financial insights** (`FinancialInsights` editorial card - savings-driven headline, supporting paragraph from top-category share + over-budget count, up to 3 observation tiles for largest increase/decrease, top category, over-budget categories; `EmptyState` fallback); **Spending trend** (dominant chart: height 240->380 px, stroke 1.75->4 px rounded caps, `role="img"` wrapper); **Category analysis** (`CategoryAnalysisChart`: top-6 donut - hover dims inactive segments, center overlay shows total + trend pill vs last month - beside ranking rows with icon chip, name, amount, share %, animated progress bar; hovering a row highlights its segment); **Income vs expenses** (rewritten as ComposedChart: income/expense bars + thin brand **Net** line overlay, legend chips); **Cash flow** (`CashFlowChart`: stacked flow bars - expenses floor, remaining stack with top radius, warn-colored shortfall segment when overspent - custom dark tooltip listing money in/out/remaining); **Forecast** (`ForecastCard`, current month only - quiet explainer card otherwise): projected month-end balance in income/expense color, confidence badge (High >= 20 days in, Medium >= 10, else Early estimate), pace paragraph, 64 px 6-month net sparkline; **Recommendations** (expandable warning/info/success rows, first open by default, action buttons route to Planner/History). **Detailed breakdowns** (ExpectedVsActual + IncomeSource + IncomeTrend + Savings + BudgetUtilization + TopCategories + ExpenseBreakdown) now render only when the selected month has >= 2 months of history, else a hint card. `useChartColors` gained `warn` (`--color-warn`); the old 3-column Predictions card is superseded by ForecastCard; `ReportsInsights` retired (replaced by FinancialInsights + Recommendations, component deleted). 13 new tests (TransactionCard presentation/expand suite, TransactionList expanded actions + long-note wrapping, HistoryView month-summary chips); duplicate-text queries disambiguated (icon + labeled buttons, title == note). Tests 337->350; tsc/lint/build green | `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-06 | **Settings rebuilt to v2.0 spec (`09_SETTINGS_SCREEN.md`) + theme accents/motion:** Settings is now a desktop preferences window — two-column layout (280px nav: 48px items, rounded-xl, icon+label, pastel selected tint + 1px left indicator; mobile horizontal chips), URL-persisted `?section=` (deep-linkable; `?action=import` now actually opens the import picker — RecoveryPanel redirects finally work) with no setState-in-effect (section derived from searchParams). Panels: **Profile** (app card: icon, name+version pill, storage mode/path, current month, counts, runtime), **Appearance** (theme cards + **accent color** swatches — emerald/blue/indigo/amber/slate — and **Reduce animations** toggle; new `lib/theme.ts` accent system: `ACCENTS`, `ACCENT_STORAGE_KEY`/`ANIMATIONS_STORAGE_KEY`, `data-accent` re-maps the whole `--color-brand-*` ramp light+dark, `data-animations="off"` is a universal motion kill-switch; bootstrap + `hooks/useAppearance.ts` read through the desktop bridge so prefs surface in SQLite-backed Electron), **Budget Preferences** (currency + recurring toggle), **Categories** (unified `CategoryModal` create+edit: 64px preview tile, Type segmented control, name, icon picker, preset `ColorSwatches`; `CATEGORY_COLORS` moved to `categoryColors.ts`; rows show transaction counts), **Recurring** (unchanged rules card), **Income Sources** (current month plans as responsive cards with expected/received/difference + per-source `IncomeSourceModal` saving via `setIncomePlan(month, id, patch)`; delete via zero-amount upsert), **Data & Backups** (storage card with location/last-backup/size + Create backup/Restore latest/Export/Import/Open folder buttons, embedded BackupsManager, danger zone), **About** (logo, versions, runtimes, paths). Polish: IconPicker 48px cells + sticky search + selected-state badge; shared `Switch` + `ColorSwatches` primitives; shared Sun/Moon/Monitor icons (ThemeToggle simplified); Modal radius to 24px; Sidebar brand/version from `lib/version.ts` (hardcoded v0.1.0 removed). New `CategoryModal.test.tsx`/`CategoryManager.test.tsx` rework for modal flow. Tests 342→354; tsc/lint/354 tests/build green; Electron `desktop:smoke` green | `09_SETTINGS_SCREEN.md`, `UI_UX_SPEC.md`, `ARCHITECTURE.md` |
| 2026-08-06 | **Design review Phase 1 — high-severity fixes:** chart tooltips are now theme-token-driven (`tooltipContentStyle` reads `colors.tooltip`/`colors.tooltipText` instead of hard-coded `#0f172a`), and the dark-mode `--color-tooltip` was raised from canvas (`#0f172a`) to a true elevated surface (`#1e293b`) so Recharts tooltips + the shared Tooltip pop against the page again; `useChartColors` now watches `data-accent` (in addition to `data-theme`) so switching the accent recolors every chart live instead of after reload; Planner Hero content no longer abuts the card border (`px-7` + `lg:pr-16`) and the Hero greeting dropped from a competing `text-page-title` (40px) to `text-section-title` (24px) so the page reads a single title hierarchy (PageHeader stays the leader) | `09_`: no new panel; verify Settings appearance accent + Reports tooltip in dark. Tests stay 354; tsc/lint/build green | `global.css`, `useChartColors`, `chartStyles`, `Hero` |
| 2026-08-06 | **Typography & layout polish pass (visual only):** responsive KPI/page type via `clamp()` — `--text-kpi` now `clamp(2.25→3rem)` and `--text-page-title` `clamp(2→2.5rem)` so values scale down on smaller widths instead of feeling oversized or overflowing; Forecast balance and Budget Health score get matching `clamp()` sizes (killed the off-scale `text-4xl`/`text-[2.5rem]`); MetricCard KPI wraps safely (`min-w-0` + `break-words`). Uniform card chrome: `ChartCard` snapped to the shared `p-7`/`rounded-xl` contract (was p-8/min-h-340). Row indentation fixed app-wide — Budget/NeedsFunding/Recent rows lost their `px-3` so content aligns exactly with card titles at the 28 px gutter instead of sitting 12 px deeper. Vertical rhythm unified to one page pulse: Planner 40→32, Reports 40→32, Upcoming/Todo 24→32, PageSkeleton →32 — every screen now breathes on the same grid. Progress bars normalized to a single `h-2` (9→10 occurrences); the NeedsFunding pill  "Fund" button demoted from `rounded-full` to the shared `rounded-lg`. Off-scale micro-type removed (`text-[15px]`→`text-base`, 11/10px→`xs`/`micro`); icon tiles unified at h-10 (Budget/Needs/Upcoming) and h-12 (Timeline card, was off-scale h-13 with 16px radius kept 14px); To-do "Resolve" target raised to `min-h-9`; Timeline tile text-xl→text-base. No logic or content changed; tests stay 354; tsc/lint/ build green | `global.css`, `Card/ChartCard`, `MetricCard`, row components` |
| 2026-08-06 | **KPI card typography redesign (visual only):** MetricCard hierarchy reordered to label → primary value → supporting text (was value above label); primary value scaled down ~25% (48 px → `clamp(1.875rem, 1.6rem+1.5vw, 2.25rem)` ≈ 30–36 px) with `whitespace-nowrap` so values never wrap — the type scale, not card width, carries the size; label promoted to a small `caption` muted line above the value, supporting text moved down with `mt-2` and single-line truncation; vertical gaps widened (icon→label, label→value, value→support). Supporting copy rewritten to read naturally and avoid wraps — Expected Income: “Received $2,500.00 · $1,000.00 to collect” / “all expected received” (old “+/-$X to collect” line removed); Savings Rate: “After expenses, you keep +$750.00” (old “of received income · net” removed). Month at a Glance values scaled to the same clamp size for a single KPI voice; Reports KPI tiles inherit the same hierarchy automatically. Tests 354 (1 support-copy assertion updated); tsc/lint/build green | `MetricCard`, `SummaryCards`, `MonthlyStats` |
| 2026-08-06 | **Consistent KPI typography scale (visual only):** new font tokens in `@theme` — `--text-kpi-hero` `clamp(2rem,1.5rem+2vw,2.625rem)` (~32–42px, w700), `--text-kpi-secondary` `clamp(1.5rem,1.25rem+1.5vw,2rem)` (~26–32px, w700), `--text-kpi-tertiary` `clamp(1.25rem,1.125rem+1vw,1.75rem)` (~20–28px, w700), `--text-kpi-micro` `clamp(1.25rem,1.125rem+0.75vw,1.5rem)` (~20–24px, w700). Hierarchy: Planner KPI cards (Expected Income / Remaining / Budgeted / Savings Rate + Report Overview tiles via shared `MetricCard`) = hero 42px; Month at a Glance = secondary 32px; chart/donut totals + Forecast balance = tertiary 28px (`CategoryAnalysisChart` center, BudgetList "committed" center, `ForecastCard`); Budget Health score = micro 24px (no longer bigger than currency values). Card titles (`Card `, `ChartCard `) unified to 16px / 600 (`--text-card-title` 18→16px; unused `--text-chart-title` token removed); Kraken `MetricCard` label raised to 16px semibold ink with `mt-2` to value, supporting text 13px caption at `mt-2.5`; value `whitespace-nowrap` across all tiers so numbers never wrap — tighter tracked (-0.03em) large numerals. Month at a Glance column spacing widened (gap-2→2.5). All type driven by scale tokens, no per-component magic numbers. Calculations/layout/colors/functionality untouched. Tests 354, tsc/lint/build green | `MetricCard`, `MonthlyStats`, `BudgetHealthCard`, `ForecastCard`, `CategoryAnalysisChart`, `BudgetList`, `ChartCard`, `Card` |
| 2026-08-06 | **Global typography & spacing polish pass (visual only):** reigned in the remaining oversized type to match DESIGN_TOKENS/LAYOUT docs. KPIs scaled down again — hero cards `clamp(1.875rem,1.625rem+1vw,2.25rem)` (~30–36px, desktop 36), Month at a Glance `clamp(1.5rem,1.375rem+0.75vw,1.875rem)` (24–30), chart/donut totals + Forecast balance `clamp(1.25rem,1.125rem+0.75vw,1.5rem)` (20–24), Budget Health score ~18–22px — no number dominates its card. `--text-caption` 13→12px (supporting/caption text quieter everywhere); default body text 15→14px; KPI card label softened to 15px semibold `text-secondary` (was 16px ink) with tighter follow spacing. DonutChart center no longer stamps a 36–48px size (consumers own the tier; committed + category totals stay tertiary). Buttons lightened: md `px-5→px-4`, sm `px-4→px-3`, lg `px-6→px-5`, tighter icon gaps. Sidebar: nav groups `gap-2→gap-4`, section labels `pt-4→pt-5`. Page rhythm opened up — Planner/Reports top-level section flow `gap-8→gap-10` (40px per LAYOUT doc "between major sections"). FinancialInsights headline 24px→18px (`text-amount`). Page titles (40px), section titles (24px), card titles (16px), chart legends/axes unchanged (already at doc size). No logic, calculations, layout structure or colors touched. Tests 354, tsc/lint/build green | `MetricCard`, `DonutChart`, `Button`, `Sidebar`, `PlannerView`, `ReportsView`, `FinancialInsights`, `globals.css` |
| 2026-08-06 | **Design-token rewrite — desktop-density scale (visual only).** Audited 00_DESIGN_TOKENS/01_UI_DESIGN_SYSTEM/03_TYPOGRAPHY/04_LAYOUT/05_COMPONENT_LIBRARY and rewrote them: typography collapsed from marketing-scale to productivity-scale (Page 40→32, Section 26→22, Chart/Card titles →22/16, Nav 15→14, Body 16→13, KPI max 42→32, secondary 30→24, small KPI 20, caption 13→11–12, badges 11); spacing capped at 32px (40/48/56/64 removed, 28 retired in favor of 24). Propogated through tokens: `--text-*` rewritten (page-title 32, section 22, card 16, base nav 14, sm 13, caption/secondary 12, xs 11, micro 10, txn-note 12, amount 16, input 15, description 14; KPIs hero 32 / secondary 24 / tertiary 20 / micro 18; dialog 24, empty-title 20, timeline-date 16), body default 13px. Components inherited from the new scale — KPI card 180→152px & padding 28→24, section/report cards p-6, Dialogs/Drawer/Disclosure px-6, MetricCard icons 44px (`MetricIcon` h-11), each icon→20px, recommendation/insight icons h-10, Sidebar 272→240px (w-60/pl-60, nav rows 44px, logo 40px), buttons md 40px/sm 32px/lg 44px (min-h-8/10/11), page surface px-10→8 pt-8→6, section flows gap-10→gap-6 (32→24), hero & chart cards p-6 min-h 300px. Whitespace now lives between cards rather than inside. 1 test pinned the old sm-button min-h-9 and was updated to min-h-8. Docs updated to match. Tests 354, tsc/lint/build green | `globals.css`, `MetricCard`, `MetricIcon`, `Card`, `Button`, `Sidebar`, `AppShell`, `Modal`, `Drawer`, `Disclosure`, `Hero`, `MonthlyStats`, `ChartCard`, `RecommendationCard`, `Recommendations`, `ForecastCard`, `CategoryAnalysisChart`, `FinancialInsights`, `ProfilePanel`, `SummaryCards`, docs 00/01/03/04/05 |
| 2026-08-06 | **KPI card redesign from first principles (visual only).** Rebuilt `MetricCard` proportions instead of nudging font sizes: card `min-h-[140px]`, padding 28→16 (`p-4`), radius 20→16px, fixed structure icon-row → label → value → support → optional progress. Icon well 36×36 (`MetricIcon` h-9, 18px glyphs) so icons read as decorative; label 13px/600 muted (`text-sm text-muted`); primary value 30px desktop (kpi-hero token → `clamp(1.625rem,1.5rem+0.5vw,1.875rem)`, lh 1.05) that never wraps; supporting text 12px with truncation removed. Supporting copy rewritten to fit naturally without wrapping — Expected Income now `100% received` / `All expected received` / `Received ₦… this month` (percent derived from plans, display-only); Budgeted `N budgets · X% of allocatable`; Savings Rate `You keep ₦X after expenses` / `Overspend of ₦X this month`; legacy “+/-$X to collect” phrasing retired. Progress bar now belongs to Remaining only, promoted to its own `progress` prop (thin 3px, rounded-full, low emphasis, 8px below support) instead of living inside truncated support text; `ProgressBar` gained a `thin` variant. All four cards sit on identical heights via grid stretch. Report Overview tiles inherit the same component (icons→18px). Docs 00/03/05 updated (KPI 32→30, card 152→150/16/16, icon 44→36/18). Tests 354 (1 savings-copy assertion updated), tsc/lint/build green | `MetricCard`, `MetricIcon`, `ProgressBar`, `SummaryCards`, `MonthlyOverview`, `globals.css`, docs 00/03/05 |
| 2026-08-06 | **Disclosure collapse animation rework (bugfix):** collapsing a `Disclosure` (e.g. Budget Allocation) left a large empty container where the body used to be. Root cause: `toggle()` captured the JS `getBoundingClientRect().height`, pinned it as an inline px `height`, flipped the store (body swapped to `undefined`/preview), then in a double-rAF re-measured `el.scrollHeight` — but the panel still had that tall px height + `overflow` applied mid-transition, so `scrollHeight` returned a stale large value, the height never animated down to 0, `transitionend` (the settle-to-auto hook) never fired, and the empty tall region stuck around. Fix: removed the JS height measurement entirely; the panel now animates via CSS `grid-template-rows` (`1fr` expanded ↔ `0fr` collapsed, `transition-[grid-template-rows]`) with a `min-h-0 overflow-y-clip` inner wrapper so a collapsed body contributes zero layout height, `preview` still renders when closed, and `motion-reduce:transition-none` snaps instantly. No measurement, no `transitionend` dependency, no lingering space. Tests 354, tsc/lint/build green | `components/ui/Disclosure.tsx` |
| 2026-08-07 | **Category select state-sync bugfix (TransactionForm):** the Category field visibly showed a selected category (e.g. Transport) while validation still failed with "Choose a category." Root cause: `Select` renders a native `<select>` with a controlled `value`, but the option list had no `value=""` entry — when the form state was empty, no option matched the controlled value, so the browser fell back to its native default and visually selected the FIRST option (index 0, Transport) while React state stayed `""`. UI and form state genuinely disagreed. Fix (in the shared `Select`, no form-library involved — forms use plain `useState`): when the controlled value matches no option and is empty, render a placeholder `<option value="">Select…</option>` so the displayed text always equals the stored value; choosing a real category updates state and satisfies the unchanged `!categoryId` validation rule (nothing suppressed, no rule removed, no manual error clearing). 3 new `TransactionForm.test.tsx` cases: empty state shows placeholder (never a false category), selecting a category clears the error and submits, editing seeds both the select and the form from the transaction. Tests 354→357, tsc/lint/build green | `components/ui/Select.tsx`, `components/txn/TransactionForm.tsx` |
